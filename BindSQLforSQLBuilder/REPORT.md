# REPORT

## 2026-04-13
### 0. エグゼクティブサマリー
- 今回の結論: 初回監査のため既存課題はなかった。主要フローは「貼り付け -> 自動変換 -> コピー」まで単一HTMLで完結しているが、文字列リテラルやプレースホルダの扱いで実行SQLの意味が変わる再現可能な不具合がある。
- 最優先課題: SQL文字列リテラル内の空白やプレースホルダを壊さずに変換すること。
- 次回の狙い: 変換の信頼性を先に固め、未置換・余剰バインド・位置バインド不整合を画面上で確認できる状態にする。

### 1. 今回の監査対象
- 対象ファイル:
  - `README.md`
  - `SQLBildMerge.html`
  - `REPORT.md` は今回新規作成
- 対象画面:
  - 単一画面の入力エリア、出力エリア、クリア、コピー、通知表示
- 主な利用フロー:
  - Eclipseなどのデバッグ表示からSQL本文とバインド変数を貼り付ける
  - ツールが名前付きバインドと位置バインドを抽出する
  - SQLに値を埋め込み、キーワード改行を適用する
  - 変換後SQLをコピーしてSQLクライアントで使う
- 確認した観点:
  - 初回表示、入力、実行、エラー、コピー、再利用
  - 変換ロジック、例外処理、状態管理、パフォーマンス
  - READMEと販売訴求、デモ版/有料版の切り分け余地
  - アクセシビリティ、メタ情報、制約環境での使いやすさ

### 2. 既存課題の確認結果
- 前回の上位3件の状態:
  - `REPORT.md` が存在しなかったため、前回上位3件はなし。
- 継続課題:
  - なし。今回が初回記録。
- 今回クローズ可能と判断した課題:
  - なし。
- 重複のため新規化しなかった論点:
  - なし。初回監査のため、各論点は独立Issueとして登録した。
  - ただし「未置換バインドの検知」と「位置バインド番号の扱い」は同じ検証UIに統合して実装できるため、上位3件ではまとめて着手できる形にした。

### 3. 発見した課題一覧
- 分類: バグ
- タイトル: 文字列リテラル内の空白が変換時に失われる
- 種別: 事実
- 要約: `_formatSql()` が文字列リテラルを退避する前に `replace(/\s+/g, " ")` を実行するため、SQL文字列値の連続空白や改行が1つの半角スペースに変わる。実行SQLの値そのものが変わるため、変換ツールとしての信頼性に直結する。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `_formatSql()`, `_maskStringLiterals()`
  - 画面名: 出力エリア
  - 操作手順: `select :memo as memo` と `:memo = "A   B"` を貼り付ける
  - 該当箇所: `SQLBildMerge.html:596-603` で空白正規化後に文字列リテラルをマスクしている
  - 再現結果: Node VM検証で `SELECT 'A B' as memo` となり、`A   B` が `A B` に変わった
- 内容: SQL整形の前処理で全文の空白を畳んでいるため、SQL構文上の空白と文字列値の空白を区別できていない。
- ユーザー影響: メモ、コード、固定長値、LIKE条件などで値が変わり、SQLクライアントで実行した結果がデバッグ時と一致しなくなる。
- 期待効果: 文字列値を保ったまま整形できれば、変換後SQLを安心してコピーできる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- スコア: ユーザー被害5 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21
- 次の行動: 空白正規化より前に単一引用符リテラルを退避し、復元後に値が変わらないテストケースを追加する。

- 分類: バグ
- タイトル: 文字列リテラル内の `:name` や `?` までバインド置換される
- 種別: 事実
- 要約: `_replaceNamedBind()` と位置バインド置換がSQLの構文文脈を見ずに正規表現で全文置換している。文字列リテラル中の `:id` や `?` が値へ変わり、位置バインドでは本来の `?` が未置換のまま残る。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `_replaceNamedBind()`, `_mergeSqlAndBinds()`
  - 画面名: 出力エリア
  - 操作手順: `select ':id' as literal, id from users where id = :id` と `:id = 42` を貼り付ける
  - 該当箇所: `SQLBildMerge.html:530-556`
  - 再現結果: Node VM検証で `SELECT '42' as literal, id FROM users WHERE id = 42` となった。`select '?' as literal, id from users where id = ?` と `[1] = 42` では `SELECT '42' as literal, id FROM users WHERE id = ?` となった
- 内容: 名前付き置換は `(^|[^:]):name\b`、位置置換は `/\?/g` のため、文字列リテラル・コメント・識別子などの領域を区別できない。
- ユーザー影響: デバッグSQLに説明文字列やLIKEパターンが含まれると、結果SQLが別条件になり、誤調査や誤実行につながる。
- 期待効果: 置換対象をSQLコード領域に限定すると、実務ログの複雑なSQLでも信頼性が上がる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- スコア: ユーザー被害5 / 購入理由への寄与4 / 実装しやすさ3 / 壊しにくさ3 / 保守性寄与5 = 20
- 次の行動: 単一引用符文字列、ダブルクォート識別子、行コメント、ブロックコメントをトークンとして退避してからバインド置換する。

- 分類: UX
- タイトル: 未置換・余剰・件数不一致のバインドが画面に出ない
- 種別: 事実
- 要約: 変換後SQLに `:id` や `?` が残っても、画面は通常のSQLとして表示し、コピーもできる。業務利用では「全部置換されたか」を確認できることが購入理由になりやすい。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `_replaceNamedBind()`, `_mergeSqlAndBinds()`, `_copyResult()`
  - 画面名: 出力エリア、コピー操作
  - 操作手順: `select ? as first, ? as second` と `[2] = 200` を貼り付ける
  - 該当箇所: `SQLBildMerge.html:532-537` の `_count` は返されるがUIで使われない。`SQLBildMerge.html:548-556` は不足時に `?` を残す。`SQLBildMerge.html:663-681` は `_lastSql` があればコピー可能にする
  - 再現結果: Node VM検証で `SELECT 200 as first, ? as second` となったが、警告情報は出ない
- 内容: 変換の成功/注意/失敗を判定する状態がなく、出力SQLだけが唯一の結果になっている。
- ユーザー影響: 未置換のままSQLクライアントに貼り付けてエラーになったり、余剰バインドに気付かず調査時間が増える。
- 期待効果: 置換件数、未置換一覧、余剰一覧を出せると、ローカル単体ツールでも「本番前の確認」に使う理由ができる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21
- 次の行動: `_mergeSqlAndBinds()` の戻り値に置換件数、未置換トークン、未使用バインド、位置バインド不足を含め、出力上部にステータスを表示する。

- 分類: バグ
- タイトル: 位置バインド番号の欠番を詰めて置換してしまう
- 種別: 事実
- 要約: `[2] = 200` のように番号付きで渡された値が、配列へ詰められて1番目の `?` に入る。番号の意味が保持されないため、欠番や0始まりログで誤置換が起きる。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `_parseBindVariables()`, `_mergeSqlAndBinds()`
  - 画面名: 出力エリア
  - 操作手順: `select ? as first, ? as second` と `[2] = 200` を貼り付ける
  - 該当箇所: `SQLBildMerge.html:513-527` で `_index` 付き配列をソート後に値だけへ変換している。`SQLBildMerge.html:548-556` は配列の0番目から順に消費している
  - 再現結果: Node VM検証で `SELECT 200 as first, ? as second` になった
- 内容: 位置バインドの番号情報を捨てているため、「2番目の値」を「最初に見つかった値」として扱っている。
- ユーザー影響: 欠番ログ、途中からコピーしたログ、ドライバごとの差異があるログで、条件値が別カラムへ入る。
- 期待効果: 番号を保持すれば、欠番を警告しながら正しい位置にだけ置換できる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- スコア: ユーザー被害4 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 20
- 次の行動: 位置バインドを `Map<index, value>` として保持し、1始まりの番号と `?` の出現番号を対応させる。欠番は警告に回す。

- 分類: UX
- タイトル: 初回画面に入力例がなく、READMEを見ない利用者が形式を判断しにくい
- 種別: 推論
- 要約: 画面には「貼り付け欄」と「実行可能 SQL」だけがあり、対応しているバインド形式やサンプルが表示されていない。READMEを読まずにHTMLだけ渡された現場では、最初の貼り付けで迷いやすい。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`, `README.md`
  - 関数名または要素名: `#raw-input`, `.output`
  - 画面名: 初回表示
  - 操作手順: HTMLを開く
  - 該当箇所: `SQLBildMerge.html:284-327` はラベルと空のtextareaだけ。対応形式は `README.md:11-15` にだけ記載されている
- 内容: 単一HTMLツールとして配布される場合、READMEとHTMLが分離して読まれない可能性がある。
- ユーザー影響: 初見ユーザーが `:id = 4529`、`[1] = 100` のどちらを使えるか判断できず、価値体験までの時間が延びる。
- 期待効果: 画面内に1クリックサンプルやplaceholderを置くと、導入直後に変換の良さを確認できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20
- 次の行動: textareaの近くに短い入力例、サンプル挿入ボタン、対応形式の1行説明を追加する。

- 分類: UX
- タイトル: クリア操作で貼り付け済みログを即時消去する
- 種別: 事実
- 要約: クリアボタンは確認なしで入力欄を空にし、出力も再描画する。長いデバッグログを貼った後に誤タップすると復旧導線がない。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `button[data-action="clear"]`, `_handleClickEvent()`
  - 画面名: 入力エリア
  - 操作手順: 入力後に「クリア」を押す
  - 該当箇所: `SQLBildMerge.html:290-295`, `SQLBildMerge.html:708-713`
- 内容: destructive寄りの操作だが、確認、取り消し、直前入力の一時保持がない。
- ユーザー影響: 制約環境でログの再取得に時間がかかる場合、誤操作がそのまま作業ロスになる。
- 期待効果: 直前入力の復元や確認を入れると、現場で安心して長文ログを扱える。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- スコア: ユーザー被害3 / 購入理由への寄与3 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 19
- 次の行動: 入力が空でない場合だけ「元に戻す」通知を出し、一定時間は直前入力をメモリ上に保持する。

- 分類: 追加機能
- タイトル: 保存・履歴・テンプレートがなく、繰り返し作業の時短価値が弱い
- 種別: 事実
- 要約: 現在の状態は `_lastSql` と通知タイマーだけで、入力履歴やテンプレート保存はない。毎回似たSQLやログ形式を扱う業務では、有料版の差別化候補になる。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`, `README.md`
  - 関数名または要素名: `_state`
  - 画面名: 再利用フロー
  - 操作手順: 変換後にHTMLを閉じて再度開く
  - 該当箇所: `SQLBildMerge.html:399-402` は `_lastSql` と `_flashTimer` のみ。`README.md:5-29` に保存、履歴、テンプレートの説明はない
- 内容: localStorageやファイル読込を使っていないため、セッションをまたぐ再利用ができない。
- ユーザー影響: 毎日同じ種類のログを扱うユーザーほど、ツール導入後の時短が1回の変換に限定される。
- 期待効果: ローカル履歴やテンプレートがあれば、外部送信なしで繰り返し調査を短縮できる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- スコア: ユーザー被害3 / 購入理由への寄与5 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与3 = 18
- 次の行動: 有料版候補として「直近N件のローカル履歴」「入力形式テンプレート」「保存データ全削除」を設計する。

- 分類: 訴求
- タイトル: READMEに変換前後の具体例がなく、価値が作業時間で伝わりにくい
- 種別: 事実
- 要約: READMEは使い方と仕様を説明しているが、実際のデバッグログが何秒で実行SQLになるかを示すbefore/afterがない。販売導線では「貼るだけでSQLクライアントに持っていける」体験を見せる必要がある。
- 証拠:
  - 対象ファイル名: `README.md`
  - 関数名または要素名: README全体
  - 画面名: 導入前説明
  - 操作手順: READMEを読む
  - 該当箇所: `README.md:3-23` は概要、使い方、仕様の箇条書きのみ
- 内容: 読者は対象者と入力形式は理解できるが、手作業置換との比較、失敗しやすい置換の回避、制約環境での価値を読み取れない。
- ユーザー影響: 初見の購入検討者が「自分のログに刺さる」と判断しにくい。
- 期待効果: 変換前/変換後の短い例を入れると、無料版でも価値体験が伝わる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- スコア: ユーザー被害2 / 購入理由への寄与5 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20
- 次の行動: READMEに「Eclipseウォッチ例 -> 変換後SQL -> コピーして実行」の3段例を追加する。

- 分類: 訴求
- タイトル: 製品名とファイル名の表記が揺れている
- 種別: 事実
- 要約: READMEは `SQLBindMerge`、HTMLファイルは `SQLBildMerge.html`、HTML titleは `SQL Bind Merge` で表記が分かれている。販売ページや配布ファイルで名前が揺れると、信頼感と検索性が落ちる。
- 証拠:
  - 対象ファイル名: `README.md`, `SQLBildMerge.html`
  - 関数名または要素名: README見出し、HTML `<title>`、ファイル名
  - 画面名: README、ブラウザタブ、配布ファイル
  - 操作手順: READMEとHTMLファイル名を確認する
  - 該当箇所: `README.md:1` は `SQLBindMerge`、`SQLBildMerge.html:6` は `SQL Bind Merge`、ファイル名は `SQLBildMerge.html`
- 内容: `Bind` と `Bild` の差があり、意図した製品名か誤字かを判断しにくい。
- ユーザー影響: ダウンロード後のファイル名、README、ブラウザタブが一致せず、社内共有時に説明しにくい。
- 期待効果: 表記を統一すると、配布物としての信頼感と再検索性が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- スコア: ユーザー被害2 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 19
- 次の行動: 正式名を1つ決め、README、title、配布ファイル名、LP見出しの表記ルールを合わせる。

- 分類: 訴求
- タイトル: READMEの外部フォント説明が実装と一致していない
- 種別: 事実
- 要約: READMEはボタンアイコンが Material Symbols（Google Fonts）を利用すると書いているが、HTML内に外部フォント読み込みはなく、ボタンはインラインSVGで描画されている。ローカル・制約環境で使える強みの説明が逆に弱くなる。
- 証拠:
  - 対象ファイル名: `README.md`, `SQLBildMerge.html`
  - 関数名または要素名: ボタン内SVG
  - 画面名: README、入力/出力ヘッダー
  - 操作手順: READMEの補足とHTMLの外部リンク有無を確認する
  - 該当箇所: `README.md:29` は Google Fonts 利用と記載。`SQLBildMerge.html:290-323` はインラインSVG。`rg` 確認で `fonts.googleapis` や外部stylesheetは検出なし
- 内容: 説明と実装の差により、オフライン利用や外部通信制限の価値を訴求しにくい。
- ユーザー影響: 外部通信が禁止される現場では、Google Fonts利用の記載だけで導入候補から外される可能性がある。
- 期待効果: 「外部フォント不要、単一HTMLだけで動作」と明記できれば制約環境での採用理由になる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- スコア: ユーザー被害2 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 19
- 次の行動: READMEの補足を実装に合わせて修正し、外部通信なしで使えることを販売訴求へ回す。

- 分類: セキュリティ
- タイトル: 変換後SQLを実行する前の確認注意が画面にもREADMEにもない
- 種別: 推論
- 要約: 画面は出力を「実行可能 SQL」と表現しており、READMEもSQLクライアントでそのまま実行できると説明している。DMLや本番DBでの誤実行を避ける注意書きがない。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`, `README.md`
  - 関数名または要素名: 出力見出し、README概要
  - 画面名: 出力エリア、README
  - 操作手順: 変換後SQLをコピーする
  - 該当箇所: `SQLBildMerge.html:316` は `実行可能 SQL`、`README.md:3` は `SQL クライアントでそのまま実行できる SQL` と説明
- 内容: ツールの価値は実行可能SQLの生成だが、実務では本番接続先やDMLの確認が必要になる。
- ユーザー影響: 利用者がコピー後に接続先やWHERE条件を確認しないまま実行するリスクが残る。
- 期待効果: 「実行前に接続先・WHERE・件数を確認」の短い注意を入れると、業務ツールとしての安心感が増す。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与2 = 19
- 次の行動: 出力エリア下部またはREADMEに、本番実行前の確認ポイントを短く追加する。

- 分類: パフォーマンス
- タイトル: 入力イベントごとに全文を再解析するため大きなログで遅延する可能性がある
- 種別: 仮説
- 要約: `_render()` は入力イベントごとに全文の分割、バインド解析、置換、整形を行う。500件の名前付きバインドではNode VM上で約9.20msだったため現時点で深刻とは断定しないが、数千件ログでは遅延する余地がある。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`
  - 関数名または要素名: `_render()`, `_mergeSqlAndBinds()`
  - 画面名: 入力エリア
  - 操作手順: 500件の名前付きバインドを含むSQLを貼り付ける
  - 該当箇所: `SQLBildMerge.html:543-545` は名前付きバインドごとにSQL全文置換。`SQLBildMerge.html:684-692` は入力ごとに全処理を実行
  - 測定結果: Node VM検証で `500 named binds elapsed_ms=9.20 output_chars=8295`
- 内容: 名前付きバインド数が増えるほど、SQL文字列への正規表現置換回数が増える。
- ユーザー影響: ログ全体を貼る業務で入力中の画面反応が遅れる可能性がある。
- 期待効果: 貼り付け時だけ即時実行し、手入力中は短いdebounceを入れると体感を保ちやすい。
- 緊急度: 低
- 実装コスト: 中
- 確信度: 中
- スコア: ユーザー被害2 / 購入理由への寄与3 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与4 = 16
- 次の行動: 100件、500件、2000件のベンチケースを作り、実測が悪化した場合だけdebounceまたは一括トークン化を入れる。

- 分類: 技術負債
- タイトル: 日本語のバインド見出しに対応していない
- 種別: 推論
- 要約: バインドセクション開始の判定は英語の `bind variables`, `params`, `parameters` のみ。日本語ログやメモで `バインド変数:` と書かれた場合、以後の裸キー形式をバインドとして扱えない。
- 証拠:
  - 対象ファイル名: `SQLBildMerge.html`, `README.md`
  - 関数名または要素名: `BIND_HEADER_PATTERN`, `_isBindDefinitionLine()`
  - 画面名: 入力エリア
  - 操作手順: `バインド変数:` の下に `id = 4529` のような裸キー行を貼り付ける
  - 該当箇所: `SQLBildMerge.html:367` は英語ヘッダーのみ。READMEは日本語ツール説明だが入力ヘッダー例はない
- 内容: 名前付きで `:id = 4529` と書けば動くが、ヘッダー後の裸キー許可は英語ヘッダーに依存する。
- ユーザー影響: 日本語の社内メモやログ整形結果をそのまま貼ると、期待通りにバインド抽出されない可能性がある。
- 期待効果: 日本語ヘッダーを追加すると、日本語利用者向けの導入摩擦が下がる。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 中
- スコア: ユーザー被害2 / 購入理由への寄与3 / 実装しやすさ4 / 壊しにくさ5 / 保守性寄与3 = 17
- 次の行動: `バインド`, `バインド変数`, `パラメータ` をヘッダーとして許可し、READMEの入力例にも1つ追加する。

- 分類: 訴求
- タイトル: デモ版と有料版の価値差がまだ定義されていない
- 種別: 事実
- 要約: READMEとHTMLに無料/有料の切り分けはない。販売を考えるなら、単なる機能制限ではなく「本番利用の安心」「再利用」「時短」で有料版の理由を作る必要がある。
- 証拠:
  - 対象ファイル名: `README.md`, `SQLBildMerge.html`
  - 関数名または要素名: README全体、画面全体
  - 画面名: 導入前説明、単一ツール画面
  - 操作手順: READMEとHTML内テキストを確認する
  - 該当箇所: `README.md:1-29` と `SQLBildMerge.html:284-327` に無料版/有料版の説明はない
- 内容: 現在は単体ツールとして価値がある一方、購入理由は明文化されていない。
- ユーザー影響: 無料で試して満足したユーザーが、有料へ上がる理由を見つけにくい。
- 期待効果: デモ版はサンプル変換と基本コピー、有料版は検証レポート、ローカル履歴、形式プリセット、エクスポートで差を出せる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- スコア: ユーザー被害2 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ5 / 保守性寄与3 = 19
- 次の行動: READMEまたはLP案として、無料版/有料版の体験差を1表にまとめる。

### 4. 上位3件
- 1位
  - 選定理由: 文字列リテラル内の空白が変わる問題は、変換後SQLの意味を直接変える。ユーザー被害5点で、再現結果もある。
  - 今やるべき理由: 実務ログのメモ値、LIKE条件、固定長文字列で結果差が出るため、販売訴求より先に信頼性を確保する必要がある。
  - 実装イメージ: `_formatSql()` の先頭で文字列リテラルを退避し、空白正規化とキーワード改行の後に復元する。必要ならダブルクォート識別子とコメントも同じ退避機構へ統合する。
  - 想定UI: UI変更なし。内部処理のみの小さな修正。
  - 想定ユースケース: `:memo = "A   B"` を含むSQLを貼り付けても、出力SQLの `'A   B'` がそのまま保持される。
  - リスク: 既存の整形結果で、文字列外の余分な空白が残る可能性がある。退避対象外のSQL方言文字列は別途確認が必要。
  - 受け入れ条件: `select :memo as memo` と `:memo = "A   B"` の出力が `'A   B'` を保持する。`select 'A   B' as memo` の出力も `'A   B'` を保持する。既存の基本例 `:id = 4529` は従来通り置換される。
  - 着手順: 文字列リテラル退避を先頭へ移動する -> 最小ケースを手動検証する -> READMEの仕様変更が必要か判断する。
- 2位
  - 選定理由: 文字列リテラル内の `:id` や `?` が置換される問題も、SQL条件や値を直接変える。位置バインドでは後続の実バインドが未置換になる。
  - 今やるべき理由: 文字列退避を導入するタイミングで、置換対象をSQLコード領域に限定する設計へ寄せると再発防止になる。
  - 実装イメージ: 置換前に単一引用符文字列、ダブルクォート識別子、行コメント、ブロックコメントをトークン化し、コード領域だけで名前付き/位置バインドを処理して復元する。
  - 想定UI: UI変更なし。検出できない方言がある場合は、次の3位の検証表示へ警告として出す。
  - 想定ユースケース: `select ':id' as literal, id from users where id = :id` で、`':id'` は保持し、WHERE句の `:id` だけ `42` に変わる。
  - リスク: SQL方言の引用構文をすべて網羅しようとすると大きくなる。次回は単一引用符、ダブルクォート、標準コメントに範囲を絞る。
  - 受け入れ条件: `':id'` と `'?'` は置換されない。WHERE句の `:id` と `?` は置換される。PostgreSQLの `::type` は従来通り壊さない。
  - 着手順: 退避関数を作る -> 名前付き置換を退避後SQLに適用する -> 位置置換を退避後SQLに適用する -> 最小ケースを追加確認する。
- 3位
  - 選定理由: 未置換・余剰・欠番を見せる機能は、バグの見逃し防止と購入理由の両方に効く。単なる整形ツールから「実行前チェック付きツール」へ価値が上がる。
  - 今やるべき理由: 現状でも `_replaceNamedBind()` は `_count` を返しているがUIで使っていないため、既存構造を活かして小さく始められる。
  - 実装イメージ: `_mergeSqlAndBinds()` が `_sql` に加えて `_warnings`, `_replacedNamed`, `_replacedPositional`, `_unusedBinds`, `_missingPositional` を返す。出力パネル上部に「置換済み: 3 / 注意: 未置換 ? 1件」のように表示する。
  - 想定UI: 出力エリアの見出し下に1行ステータスを追加する。注意がある場合はコピー前にも同じ注意を表示し、コピー自体はブロックしない。
  - 想定ユースケース: `[2] = 200` だけ貼った場合、1番目の `?` に誤って入れず、「1番目の位置バインドが不足」と表示する。
  - リスク: 警告が多すぎるとシンプルさが落ちる。初回は件数と短い一覧に限定する。
  - 受け入れ条件: 未置換の `:name` と `?` が画面上で分かる。未使用バインドが画面上で分かる。位置バインド欠番が最初の `?` に詰めて入らない。
  - 着手順: 位置バインドをMapで保持する -> 置換結果のメタ情報を返す -> ステータス表示を追加する -> コピー通知にも注意件数を含める。

### 5. 追加機能アイデア
- 機能名: 変換安全チェック
  - 解決する課題: 未置換、余剰バインド、欠番、文字列内誤置換をユーザーが目視だけで確認している。
  - 概要: 出力上部に置換件数、注意件数、未置換トークン一覧を表示する。
  - ユーザー価値: SQLクライアントへ貼る前に、変換ミスを短時間で確認できる。
  - 差別化ポイント: 単なる置換ではなく、実行前レビューに使える。
  - 実装コスト: 中
  - 優先度: 高
- 機能名: サンプル入力ボタン
  - 解決する課題: 初回利用者が入力形式を画面だけで判断しにくい。
  - 概要: ボタン1つで名前付きバインドと位置バインドの短いサンプルを入力欄へ入れる。
  - ユーザー価値: READMEを読まなくても、期待出力をすぐ確認できる。
  - 差別化ポイント: デモ版の価値体験を画面内で完結できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: ローカル履歴
  - 解決する課題: 毎回似たログを貼り直す必要がある。
  - 概要: 直近の入力と出力をlocalStorageに保存し、端末内だけで再利用する。
  - ユーザー価値: 繰り返し調査の時短になる。外部送信できない現場でも使える。
  - 差別化ポイント: 有料版の「再利用」価値として説明しやすい。
  - 実装コスト: 中
  - 優先度: 中
- 機能名: ログ形式プリセット
  - 解決する課題: Eclipse以外のログ形式を手で整形する必要がある。
  - 概要: `Eclipse`, `params:`, `手入力` などのプリセットを選び、抽出パターンを切り替える。
  - ユーザー価値: 現場のログ形式に合わせて貼り付けやすくなる。
  - 差別化ポイント: 業務現場別の導入理由を作れる。
  - 実装コスト: 中
  - 優先度: 中
- 機能名: 変換レポートコピー
  - 解決する課題: 調査記録やレビューで、何を置換したか残しにくい。
  - 概要: SQL本文に加え、バインド一覧、置換件数、警告をMarkdownまたはテキストでコピーする。
  - ユーザー価値: チケットや調査メモに貼り付けやすい。
  - 差別化ポイント: 有料版の「安心・レビュー」価値として切り分けやすい。
  - 実装コスト: 中
  - 優先度: 中

### 6. 訴求・販売改善案
- 想定ユーザー:
  - Java/Eclipse系の開発者、保守担当、QA担当、SQL調査をする社内SE。
  - 外部サービスへSQLやログを貼れない制約環境の担当者。
- 解決している具体的な痛み:
  - デバッグ表示で分かれているSQLとバインド変数を、手作業で1つずつ置換する時間。
  - 置換漏れやクォートミスでSQLクライアント実行前に手戻りすること。
  - 社外ツールやWebサービスへログを貼れないこと。
- 今でも刺さる強み:
  - 単一HTMLで動作し、ビルドやサーバーが不要。
  - 入力欄1つに混在貼り付けできる。
  - 名前付きバインドと位置バインドの両方に対応している。
  - クリップボードコピーまで画面内で完結する。
- 伝わっていない価値:
  - 外部通信なしで使える可能性。READMEはGoogle Fonts利用と書いており、実装上の強みと逆方向に見える。
  - 手作業置換と比べてどの作業が何秒短縮されるか。
  - 変換ミスを検知できるようになれば、実行前レビューに使えること。
- 買う理由として弱い点:
  - 現状は基本変換だけで、無料ツールとの差が「安心」「再利用」「本番利用」で説明されていない。
  - 未置換・余剰バインドの警告がないため、有料にするには信頼性訴求が不足している。
- READMEに追加すべき説明:
  - 変換前/変換後の具体例。
  - 外部通信なし、単一HTML、ローカル処理の明記。
  - 対応している入力形式と、未対応形式の境界。
  - 実行前に接続先、WHERE条件、DMLを確認する注意。
- LPに追加すべき訴求:
  - 「EclipseのSQLとバインド変数を、1回貼るだけでSQLクライアント用に整形」。
  - 「ログを外部送信しない、ローカル完結の単一HTML」。
  - 「有料版は置換チェック、ローカル履歴、形式プリセット、変換レポートで本番調査に向く」。
- スクリーンショットで見せるべき場面:
  - 左にデバッグログ、右に変換後SQLが出ている画面。
  - 未置換や欠番があるときに注意件数が出る画面。
  - サンプル入力を押して即座に結果が出る画面。
- 無料版で見せるべき体験:
  - サンプルSQLの変換。
  - 基本的な名前付き/位置バインドの変換。
  - コピー成功までの一連の流れ。
- 有料版でしか得られない体験:
  - 変換安全チェックの詳細表示。
  - ローカル履歴とテンプレート。
  - 複数ログ形式プリセット。
  - 変換レポートコピー。
- 制約のある現場で使える理由:
  - 単一HTMLで配布できる。
  - サーバー、ビルド、アカウント登録が不要。
  - 外部通信なしで動く形に寄せれば、社内ログを外部サービスへ貼れない現場でも説明しやすい。
- 他手段より楽な理由:
  - エディタの置換や手作業ではクォート、NULL、TRUE/FALSE、位置順の確認が必要になる。
  - このツールは混在貼り付けから出力とコピーまで1画面で完結する。
- 導入直後に感動できる体験があるか:
  - 現状はREADMEを読んで正しい形式を貼れた場合は体験できる。
  - 画面内サンプルと変換安全チェックがないため、初見の即時体験はまだ弱い。

### 7. 次回おすすめタスク
- 1. `_formatSql()` の文字列リテラル退避順を修正し、文字列内空白を保持する最小テストを行う。
- 2. バインド置換前に文字列・識別子・コメントを退避し、`':id'` と `'?'` を置換しないようにする。
- 3. 未置換、余剰、欠番を返す変換メタ情報を設計し、出力エリアに1行ステータスを追加する。

### 8. 実行ログ
- 今回アプリコードを変更していないこと: はい。`SQLBildMerge.html` は変更していない。
- 今回READMEを変更していないこと: はい。`README.md` は変更していない。
- 今回REPORT.mdのみ更新したこと: 対象ツールディレクトリ内では `REPORT.md` のみ新規作成した。自動化運用要件により、対象外の `$CODEX_HOME/automations/sqlbildmerge/memory.md` は別途更新予定。
- 既存課題との重複確認: `REPORT.md` は存在せず、重複登録はなし。
- ブラウザ確認: Chrome headlessは最小data URLでも `exit=134` で起動できず、実ブラウザのconsole確認は未完了。代替としてNode VM上でHTML内scriptを初期化し、変換ケースを検証した。
- コード修正の有無: なし。
