# REPORT

## 2026-04-13
### 0. エグゼクティブサマリー
- 今回の結論: 初回監査のため既存課題はなかった。主目的は「ローカル完結のコーディング英語学習」だが、外部CDN参照、保存失敗時の未処理、全800語の個別例文未投入が、制約環境での信頼と販売価値を下げる主要因だった。
- 最優先課題: `localStorage` 保存失敗時の例外処理を追加し、アプリ起動・回答・CSV出力が保存不可環境でも破綻しない状態にする。
- 次回の狙い: 制約環境で使える理由を壊す要素を先に潰し、その後に学習価値が伝わる例文・復習導線を強化する。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`（新規作成）, `README.md`, `MustKnowCodingEnglish.html`, `Parts1.html`, `Parts2.html`, `Parts3.html`, `Parts4.html`
- 対象画面: 学習ビュー、テストビュー、結果ビュー、設定モーダル、フッター操作、README上の説明
- 主な利用フロー: 初回表示 -> 単語検索/フィルタ -> ステータス更新 -> 4択テスト開始 -> 回答 -> 結果確認 -> 復習 -> CSV出力/設定/リセット
- 確認した観点: ローカル保存、外部通信、入力導線、エラー時挙動、状態遷移、CSV出力、アクセシビリティ、販売訴求、デモ/有料切り分け、制約環境での利用価値

### 2. 既存課題の確認結果
- 前回の上位3件の状態: `REPORT.md` が存在しない初回監査のため、前回上位3件はなし。
- 継続課題: なし。今回の12件を初回ベースラインとして登録する。
- 今回クローズ可能と判断した課題: なし。
- 重複のため新規化しなかった論点:
  - `localStorage` の破損データ・保存失敗・起動時書き込みは別々に登録せず、保存失敗はISS-002、保存データ境界の検証不足はISS-009に集約した。
  - フィルタボタン、タブ、ステータスボタンの選択状態読み上げ不足は、個別乱立せずISS-008に集約した。
  - README/LP/デモ版/有料版の訴求不足は、販売導線の同一問題としてISS-011に集約した。

### 3. 発見した課題一覧
#### ISS-001
- 分類: 訴求 / 技術負債
- タイトル: 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
- 種別: 事実
- 要約: READMEは外部通信なしと説明しているが、本体HTMLはGoogle Fontsを読み込む。オフライン環境や社内プロキシ環境では「ローカル完結」という購入理由が弱くなる。
- 証拠: `README.md` 33-35行目で `localStorage` と「外部通信なし」を記載。`MustKnowCodingEnglish.html` 7-9行目で `fonts.googleapis.com` / `fonts.gstatic.com` / Material Symbolsを参照。`Parts1.html` 6-9行目、`Parts2.html` 7-10行目、`Parts4.html` 6-9行目はTailwind CDNやGoogle Fontsも参照。
- 内容: 学習データと進捗保存はローカルだが、初回表示時のフォント/アイコンは外部取得に依存している。制約環境で使えることを売りにするなら、外部通信の定義を「学習データ送信なし」に変更するか、アイコンをインラインSVG/テキストに置換して完全オフライン化する必要がある。
- ユーザー影響: 社内ネットワークやオフライン配布でアイコン/フォントが欠け、説明と挙動の差によって信頼を落とす。
- 期待効果: 「1ファイルを配れば動く」「学習データを外に送らない」という訴求を事実ベースで出せる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 次回実装ではMaterial Symbols依存をインラインSVGまたはテキストに置換する。READMEでは「外部通信なし」の意味を、完全オフラインかデータ送信なしのどちらかに合わせる。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-002
- 分類: バグ / UX
- タイトル: `localStorage` 保存失敗時に起動・操作が例外で止まる
- 種別: 事実
- 要約: `_saveState()` は `localStorage.setItem()` を直接呼び、例外を捕捉していない。保存不可環境では初期表示末尾の `_saveState()` だけで例外になり得る。
- 証拠: `MustKnowCodingEnglish.html` 9439-9442行目の `_saveState()` は `try/catch` なし。10300行目で起動直後に `_saveState()` を実行。検証では `localStorage.setItem()` が `quota exceeded` を投げるスタブ環境で、インラインスクリプト実行が `quota exceeded` で停止した。
- 内容: プライベートブラウズ、容量上限、ブラウザ設定、管理端末の制限で `localStorage` 書き込みが失敗すると、保存状態表示や学習継続以前にアプリ操作が崩れる可能性がある。ローカル完結ツールほど保存不可時の退避表示が必要。
- ユーザー影響: 学習開始前に画面が壊れる、または回答・リセット・テーマ変更後に操作結果が反映されない。
- 期待効果: 保存不可時でも「一時利用は可能」「保存できない理由が分かる」状態にでき、制約端末での導入リスクを下げられる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_saveState()` を `try/catch` 化し、失敗時は `_runtime._saveError` とフッター文言に「保存できません。ブラウザ設定または容量を確認してください」を出す。起動時に保存を強制する必要があるかも見直す。
- スコア: ユーザー被害5 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-003
- 分類: 追加機能 / 訴求
- タイトル: 全800語の個別例文が未投入で学習価値が伝わりにくい
- 種別: 事実
- 要約: 800語すべてで `exampleEn` / `exampleJa` / `meaningSub` が空で、表示はテンプレート例文に依存している。英語学習ツールとして、実務文脈で覚えられる理由が弱い。
- 証拠: `MustKnowCodingEnglish.html` の `WORDS` は800件。検証結果は `emptyExampleEn: 800`, `emptyExampleJa: 800`, `emptyMeaningSub: 800`。`_getExampleEn()` 9493-9505行目、`_getExampleJa()` 9515-9530行目で空値時の汎用テンプレートを生成。
- 内容: 例文が `Use "access" in code or documentation...` のような定型文になるため、実際のPR、エラーメッセージ、仕様書、レビューでどう使うかが伝わらない。販売価値としては「実務で迷う英語を短時間で復習できる」証拠が不足する。
- ユーザー影響: 初見ユーザーが「単語帳以上の価値」を判断しにくく、継続利用や購入判断につながりにくい。
- 期待効果: よく出る実務文脈を例文で見せることで、導入直後に「自分の現場で使う語彙だ」と判断しやすくなる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: 次回は全800語ではなく、購入判断に効く上位30語だけ実務例文を入れる。例: `access`, `argument`, `deploy`, `rollback`, `deprecated` など。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ3 / 壊しにくさ5 / 保守性寄与4 = 21

#### ISS-004
- 分類: UX / 改善
- タイトル: 結果画面の「復習を開始する」が優先復習ワードへ移動しない
- 種別: 事実
- 要約: 結果画面には優先復習ワードが表示されるが、主CTAは単に学習ビューへ戻るだけで、ミスした単語や苦手単語を選択しない。
- 証拠: `MustKnowCodingEnglish.html` 1171-1177行目の復習パネル。1177行目のボタンは `data-action="switch-view" data-view="learning"` のみ。10071-10077行目のチップは `select-word-and-study` で単語指定できるため、主CTAだけ導線が弱い。
- 内容: 「優先復習ワード」の横にある主CTAを押しても、現在の学習単語が前回位置のままなら、復習開始の期待と結果がずれる。チップを押す必要があることを初見では判断しにくい。
- ユーザー影響: テスト後に復習したいユーザーが、どの単語から復習すればよいか迷う。
- 期待効果: ミスした単語へ直接移動できれば、テスト -> 復習のループが短くなり、学習ツールとしての体験価値が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 主CTAを `select-word-and-study` 相当にし、優先IDの先頭を `_state.learning.wordId` に設定してから学習ビューへ遷移する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-005
- 分類: UX / バグ
- タイトル: 進行中テストが確認なしで新しいテストに置き換わる
- 種別: 事実
- 要約: テスト中に「新しいテスト」またはヘッダーの「テスト開始」を押すと、回答中データが確認なしで初期化される。
- 証拠: `MustKnowCodingEnglish.html` 980行目と1102行目に `data-action="start-quiz"`。9599-9605行目の `_startQuiz()` は `active`, `order`, `questionIndex`, `answers`, `choiceMap` を上書きする。確認ダイアログや進行中判定はない。
- 内容: 回答中の10問セッションを誤ってリセットしても、直前の回答に戻る導線がない。誤操作しやすいヘッダーに同じ開始ボタンがあるため、実務の短時間学習で損失感が出る。
- ユーザー影響: テスト途中の回答が消え、結果履歴にも残らない。
- 期待効果: 誤操作を防ぎ、テスト結果の信頼性を保てる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_startQuiz()` の先頭で `quiz.active && answers.length > 0` を判定し、確認後だけ初期化する。またはテスト中は「最初からやり直す」とラベル変更する。
- スコア: ユーザー被害4 / 購入理由への寄与3 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-006
- 分類: 追加機能 / 訴求
- タイトル: CSV出力が学習ステータスだけで、テスト履歴や復習理由を持ち出せない
- 種別: 事実
- 要約: CSVは単語・ステータス・レベル・意味のみを出力する。直近の正誤、ミスした日時、復習優先理由が出ないため、業務学習の記録としては弱い。
- 証拠: `MustKnowCodingEnglish.html` 10123-10139行目の `_exportCsv()` はヘッダー `word,status,level,meaning` のみ。`sessions` は9769-9775行目で保存されるがCSVに含まれない。
- 内容: チーム研修、自己学習ログ、買い切り教材の成果確認では、どの単語で間違えたかを出せることが価値になる。現状は進捗のバックアップとしても、復習計画としても情報が足りない。
- ユーザー影響: ブラウザ移行時や研修報告時に、結果履歴を再利用できない。
- 期待効果: 「買ったあとに進捗を持ち出せる」価値を訴求できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: CSV出力を2種類に分ける。`progress.csv` は現状維持、`sessions.csv` は `finishedAt,word,correct,selected,isCorrect,status` を出す。
- スコア: ユーザー被害3 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 20

#### ISS-007
- 分類: アクセシビリティ / UX
- タイトル: 設定モーダルにフォーカストラップと復帰処理がない
- 種別: 事実
- 要約: 設定モーダルは `role="dialog"` と `aria-modal="true"` を持つが、開いた時の初期フォーカス、閉じた後のフォーカス復帰、開いている間のフォーカストラップが実装されていない。
- 証拠: `MustKnowCodingEnglish.html` 1193行目に `role="dialog" aria-modal="true"`。9839-9842行目の `_openSettings()` は `.hidden` と `aria-hidden` の切り替えのみ。10294-10297行目はEscapeで閉じるだけ。
- 内容: キーボード利用者はモーダル外へTab移動できる可能性があり、閉じた後に元の設定ボタンへ戻る保証もない。Web Design Standardsのdialog/focus項目にも反する。
- ユーザー影響: キーボード操作や支援技術利用時に、設定を閉じた後の現在位置が分かりにくい。
- 期待効果: 1ファイルツールでもアクセシビリティの基本品質を担保でき、企業導入時の不安を下げられる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 開いたトリガー要素を `_runtime._settingsTrigger` に保持し、初期フォーカスを閉じるボタンへ移動。Tab循環とclose時のフォーカス復帰を追加する。
- スコア: ユーザー被害4 / 購入理由への寄与3 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 19

#### ISS-008
- 分類: アクセシビリティ / UX
- タイトル: 検索・フィルタ・タブの選択状態が視覚表現に寄っている
- 種別: 事実
- 要約: 検索欄はラベルがなくplaceholder依存で、タブやフィルタは `.is-active` の視覚表現だけで選択状態を示している。
- 証拠: `MustKnowCodingEnglish.html` 990-993行目の検索inputには `<label>` / `aria-label` がない。974-977行目のタブボタンは `aria-selected` や `aria-current` がない。9871-9888行目のフィルタ生成は `aria-pressed` なしのbuttonを返す。
- 内容: 画面を見るユーザーには選択状態が伝わるが、読み上げやキーボード中心の操作では、現在のレベル/ステータス/アルファベット条件が把握しにくい。
- ユーザー影響: 制約のある現場でキーボードのみ・支援技術利用のユーザーがフィルタ状態を判断しにくい。
- 期待効果: 入力条件が読み上げ可能になり、学習対象を絞り込む操作の失敗を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 検索inputに `aria-label="単語を検索"` を追加。タブは `aria-current="page"`、フィルタ/ステータスボタンは `aria-pressed` を状態に応じて出す。
- スコア: ユーザー被害3 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与4 = 19

#### ISS-009
- 分類: セキュリティ / 技術負債
- タイトル: 保存済みセッション値を `innerHTML` に流し込む境界が弱い
- 種別: 推論
- 要約: 本体の単語データは静的だが、`sessions` は `localStorage` から復元される。復元時に回答文字列の型や安全性を検証せず、結果テーブルで `innerHTML` に挿入している。
- 証拠: `MustKnowCodingEnglish.html` 9416-9418行目で `sessions` は「objectかどうか」だけを確認。10044-10056行目で `answer.correct` / `answer.selected` / `answer.wordId` をテンプレート文字列で `innerHTML` に代入。
- 内容: 通常操作だけなら回答は内部生成されるため危険度は限定的。ただし、ブラウザ拡張、手動編集、別版からの移行で `localStorage` が汚れた場合、表示崩れやスクリプト注入の入口になる。
- ユーザー影響: 共有端末や教材配布先で、保存データが壊れたときに結果画面が不正表示になる可能性がある。
- 期待効果: localStorage境界を明確化し、将来のインポート機能追加時の安全性も上げられる。
- 緊急度: 低
- 実装コスト: 中
- 確信度: 中
- 次の行動: `sessions` 復元時に `finishedAt`, `total`, `correct`, `answers[].wordId`, `selected`, `correct`, `isCorrect` を型検証する。結果テーブルはDOM生成またはHTMLエスケープ関数を使う。
- スコア: ユーザー被害3 / 購入理由への寄与3 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与4 = 17

#### ISS-010
- 分類: アクセシビリティ / パフォーマンス
- タイトル: 画面切り替えと進捗バーの動きに `prefers-reduced-motion` 対応がない
- 種別: 事実
- 要約: 画面表示と進捗バーにアニメーション/トランジションがあるが、動きを減らすOS設定を見ていない。
- 証拠: `MustKnowCodingEnglish.html` 214-217行目で `.view-panel` に `fade-slide 0.22s`、549-562行目で進捗バーに `inline-size 0.2s` transition。892-938行目のmedia query群に `prefers-reduced-motion` はない。
- 内容: 動きは短時間で大きくないが、学習ツールは繰り返し画面遷移・回答を行うため、OS設定に従って止めるほうが安全。
- ユーザー影響: 動きに弱いユーザーが、テストや復習を繰り返す時に負担を感じる可能性がある。
- 期待効果: アクセシビリティチェックで説明できる品質になり、導入先の要件に合わせやすい。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: `@media (prefers-reduced-motion: reduce)` で `.view-panel` のanimationと`.meter span`のtransitionを無効化する。
- スコア: ユーザー被害2 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 17

#### ISS-011
- 分類: 訴求
- タイトル: README/LPにデモ版と有料版の価値差がない
- 種別: 事実
- 要約: READMEは機能一覧と使い方中心で、誰がなぜ買うか、無料版でどこまで試せるか、有料版で何が業務価値になるかを書いていない。
- 証拠: `README.md` 1-41行目はファイル、画面構成、アイコン、学習データ、保存、使い方の説明のみ。価格、対象ユーザー、無料/有料差、スクリーンショットで見せる場面の記載はない。
- 内容: 800語、4択、ローカル保存は機能として分かるが、購入判断の言葉になっていない。新人エンジニア、英語PRレビューが苦手な開発者、社内研修担当のどの痛みに刺すかを分ける必要がある。
- ユーザー影響: 初見ユーザーが「無料の単語帳やAnkiではなくこれを買う理由」を判断しにくい。
- 期待効果: LP/READMEに購入理由を置くことで、実装済みのローカル保存・800語・復習導線を販売価値へ変換できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: README更新は今回禁止のため次回候補にする。最小差分として「対象ユーザー」「3分で試せる流れ」「無料版/有料版の差」「制約環境で使える理由」を追加する。
- スコア: ユーザー被害2 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ5 / 保守性寄与3 = 19

#### ISS-012
- 分類: 技術負債 / 訴求
- タイトル: 統合元のPartsファイルが配布物に混ざると旧UIや空ファイルを開かれる可能性がある
- 種別: 事実
- 要約: READMEは `MustKnowCodingEnglish.html` を本体としているが、同じフォルダに `Parts1.html`、`Parts2.html`、空の `Parts3.html`、`Parts4.html` が残っている。
- 証拠: `rg --files` で `Parts1.html`, `Parts2.html`, `Parts3.html`, `Parts4.html` を確認。`Parts3.html` は0行・0バイト。`Parts1.html` 6-9行目、`Parts2.html` 7-10行目、`Parts4.html` 6-9行目はTailwind CDNやGoogle Fontsを参照し、READMEの本体説明と異なる。
- 内容: 開発途中の画面見本としては意味があるが、販売・配布ZIPに入るとユーザーが旧UIや空ファイルを開くリスクがある。特にParts系はCDN/外部画像も含み、ローカル完結の訴求とぶつかる。
- ユーザー影響: 購入者やレビュー担当が誤って旧ファイルを開き、完成版の品質を誤解する。
- 期待効果: 配布対象を明確にでき、サポート問い合わせを減らせる。
- 緊急度: 低
- 実装コスト: 中
- 確信度: 高
- 次の行動: 今回は移動禁止のため記録のみ。次回以降、販売用READMEに「開くファイルは `MustKnowCodingEnglish.html` のみ」と明記するか、配布工程でParts系を除外する。
- スコア: ユーザー被害2 / 購入理由への寄与4 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与3 = 16

### 4. 上位3件
- 1位: ISS-002 `localStorage` 保存失敗時に起動・操作が例外で止まる
  - 選定理由: ユーザー被害が5で、主要フローの起動・回答・テーマ変更・CSV出力の前提になる保存処理に直結する。
  - 今やるべき理由: ローカル完結を売りにするツールでは、保存できない環境での説明がないと信頼を失う。修正範囲は `_saveState()` とフッター表示中心で小さい。
  - 実装イメージ: `_saveState()` に `try/catch` を追加し、失敗時はメモリ上の状態だけで継続。`_renderFooter()` で保存失敗メッセージを出す。
  - 想定UI: フッターに「保存状態: このブラウザでは保存できません。一時利用中です。」を表示。
  - 想定ユースケース: 社内端末のブラウザ設定でlocalStorageがブロックされている状態で、学習カードだけ確認する。
  - リスク: 保存失敗時もユーザーが学習できるため、後で履歴が残っていないことに気づく可能性がある。文言で「一時利用中」を明示する。
  - 着手順: `_runtime._saveError` を追加 -> `_saveState()` をtry/catch化 -> `_renderFooter()` の表示分岐 -> 保存失敗スタブで確認。
  - 受け入れ条件: `localStorage.setItem` が例外を投げても初期表示処理が例外終了しない。保存不可メッセージがフッターに出る。
- 2位: ISS-001 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
  - 選定理由: 購入理由への寄与が5で、制約環境における販売価値とREADMEの信頼性に直結する。
  - 今やるべき理由: 変更はフォント/アイコン依存と説明文の整合が中心で、先に方針を決めると以後の訴求がぶれない。
  - 実装イメージ: 本体のMaterial Symbols参照をなくし、`code` アイコンを既存faviconと同系統のインラインSVGにする。READMEは「外部通信なし」を完全オフラインの意味に合わせる。
  - 想定UI: ヘッダー左のコードアイコンはSVG、フォントはシステムフォントで表示。オンライン/オフラインで見た目の差を小さくする。
  - 想定ユースケース: ネットワーク遮断された研修PCでHTMLファイルを直接開く。
  - リスク: アイコンフォントの置換で視覚印象が少し変わる。SVGサイズと色を既存CSS変数に合わせる。
  - 着手順: 外部フォント依存箇所を確認 -> ヘッダーアイコンをSVG化 -> READMEの「外部通信なし」を実挙動に合わせる -> オフライン相当で表示確認。
  - 受け入れ条件: `MustKnowCodingEnglish.html` 本体に `fonts.googleapis.com` / `fonts.gstatic.com` 参照が残らない。READMEのローカル完結説明と実HTMLが矛盾しない。
- 3位: ISS-003 全800語の個別例文が未投入で学習価値が伝わりにくい
  - 選定理由: 購入理由への寄与が5で、無料単語帳との差別化に直結する。全件対応は重いが、上位30語からなら次回1回で着手できる。
  - 今やるべき理由: 初見ユーザーは数分で価値判断するため、最初に出る語やテストで出やすい語に実務例文があるだけでも印象が変わる。
  - 実装イメージ: `exampleEn` / `exampleJa` にPR、エラー、仕様書、コードコメントで使う短い例文を入れる。全800語ではなく先頭30語または販売デモ用30語に限定する。
  - 想定UI: 学習カードのUsage Exampleに「Please accept this change before merging.」のような文と日本語訳が出る。
  - 想定ユースケース: 新人開発者がPRレビューで見た英単語を検索し、意味と実務例文を1画面で確認する。
  - リスク: 例文品質がばらつくと逆に信頼を落とす。対象語を絞り、短文・実務文脈・日本語訳の3条件で確認する。
  - 着手順: 優先30語を選ぶ -> 例文形式を決める -> `exampleEn` / `exampleJa` を埋める -> 検索とカード表示を確認。
  - 受け入れ条件: 優先30語でテンプレート文ではない個別例文が表示される。空例文のフォールバックは既存語に対して維持される。

### 5. 追加機能アイデア
- 機能名: 復習開始ジャンプ
  - 解決する課題: 結果画面の主CTAが優先復習ワードへ移動しない。
  - 概要: 直近ミスまたは苦手ステータスの先頭単語を選択して学習ビューへ移動する。
  - ユーザー価値: テスト後に迷わず復習を開始できる。
  - 差別化ポイント: 単なる4択テストではなく、ミスから学習カードへ戻る短いループを持てる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: セッションCSV出力
  - 解決する課題: 学習ステータス以外の履歴を持ち出せない。
  - 概要: テスト履歴を `finishedAt,word,correct,selected,isCorrect,status` でCSV化する。
  - ユーザー価値: 研修報告、自己学習ログ、端末移行の材料になる。
  - 差別化ポイント: 買い切りローカルツールでも成果物を外部管理できる。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: 実務例文パック
  - 解決する課題: 個別例文がなく、単語帳との差が伝わりにくい。
  - 概要: PRレビュー、エラー対応、仕様書、CLI操作の4カテゴリで例文を追加する。
  - ユーザー価値: 覚える場面が具体化し、現場で使う理由が増える。
  - 差別化ポイント: コーディング英語に特化した文脈を見せられる。
  - 実装コスト: 中
  - 優先度: 高
- 機能名: バックアップ/復元
  - 解決する課題: localStorageだけでは端末移行や配布先の保存制限に弱い。
  - 概要: 進捗JSONをエクスポート/インポートし、検証してから反映する。
  - ユーザー価値: 学習履歴を失いにくい。
  - 差別化ポイント: 本番利用・再利用の価値を有料版に寄せられる。
  - 実装コスト: 中
  - 優先度: 中

### 6. 訴求・販売改善案
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語のPRレビュー・エラー文・仕様書に苦手意識がある開発者、社内研修で短時間の英語復習を配りたい担当者。
- 解決している具体的な痛み: コーディングで頻出する英単語を、一般英語教材ではなく開発文脈で復習したい。社内端末で外部サービスに学習データを出したくない。短時間で苦手語だけを復習したい。
- 今でも刺さる強み: 800語をレベル別に収録。検索、ステータス、4択テスト、結果、優先復習、CSV出力が1HTML内にある。進捗がlocalStorageに残る。
- 伝わっていない価値: 「なぜ800語なのか」「どの現場文脈で使う語なのか」「オフライン/社内端末で使える範囲はどこまでか」「テスト後に何分短縮できるか」がREADMEにない。
- 買う理由として弱い点: 実務例文が未投入のため、無料の単語表との差が見えにくい。バックアップ/復元やセッション履歴出力がなく、本番利用の安心感が弱い。
- READMEに追加すべき説明: 対象ユーザー、3分の利用手順、保存されるデータと外部通信の正確な説明、制約環境での使い方、CSVで出せる内容、現在の制限。
- LPに追加すべき訴求: 「PRレビュー・エラー対応・ドキュメントで見る英語を1HTMLで復習」「学習データを外部サービスへ送らない」「テスト後に苦手語へ戻れる」「研修PCに配りやすい」。
- スクリーンショットで見せるべき場面: 検索で単語を絞る画面、実務例文つきカード、4択回答後の正誤フィードバック、結果画面の優先復習ワード、CSV出力後のファイル例。
- 無料版で見せるべき体験: 100語程度、検索、学習カード、5問テスト、直近結果、数語の実務例文。良さは伝わるが研修や本番復習には語数・履歴出力が不足する状態にする。
- 有料版でしか得られない体験: 800語、実務例文パック、10問テスト、セッションCSV、バックアップ/復元、苦手語の復習導線、社内配布向けの完全オフライン版。
- 制約のある現場で使える理由: 1HTMLで配布できる。学習データはブラウザ内保存にできる。外部通信を完全排除するならフォント/アイコンCDNを外す必要がある。
- 他手段より楽な理由: 辞書、Anki、スプレッドシートを往復せず、検索・意味確認・4択・復習対象確認を同じ画面内で完結できる。
- 導入直後に感動できる体験があるか: 現状は「800語が入っている」ことは分かるが、例文が汎用テンプレートのため感動は弱い。最初の30語に実務例文を入れると、導入直後の価値判断が改善する。
- 強み: 1ファイルSPA、800語、レベル/ステータス/アルファベット検索、テストと結果の往復、localStorage保存。
- 弱み: 外部通信説明の矛盾、例文未投入、復習主CTAの弱さ、履歴CSV/バックアップ不足。
- 使う理由: 外部サービスに学習データを出さず、開発文脈の頻出英語だけを短時間で復習するため。
- デモ版/有料版の切り分け案: デモ版は100語と5問テストで体験を見せる。有料版は800語、実務例文、履歴持ち出し、バックアップ、完全オフラインで「本番利用・再利用・安心」を提供する。

### 7. 次回おすすめタスク
- 1. `_saveState()` の例外処理と保存不可時のフッター表示を追加し、保存不可スタブで起動継続を確認する。
- 2. `MustKnowCodingEnglish.html` のGoogle Fonts/Material Symbols依存を外すか、READMEの「外部通信なし」を実態に合わせる方針を決めて修正する。
- 3. 優先30語に実務例文を追加し、学習カードでテンプレート例文ではない文が出ることを確認する。

### 8. 実行ログ
- 今回アプリコードを変更していないこと: 変更していない。
- 今回READMEを変更していないこと: 変更していない。
- 今回REPORT.mdのみ更新したこと: リポジトリ内では `REPORT.md` のみ新規作成した。
- 確認コマンド: `rg --files`, `wc -l *.html README.md`, `rg` によるHTML/README静的確認、Nodeによる単語データ件数確認、インラインJSの `node --check`、スタブDOMでのインラインJS初期実行。
- 起動確認の制約: Playwright/puppeteer/jsdomはローカル未導入。Headless Chromeでの `--dump-dom` は出力前にcode -1で終了したため、実ブラウザのコンソールエラー完全確認は未完了。代替としてインラインJS構文チェックとスタブDOMでの初期実行は成功した。

## 2026-04-14
### 0. エグゼクティブサマリー
- 今回の結論: 前回上位3件（保存失敗、外部通信説明、実務例文未投入）は未解決のまま。追加監査では、フィルタ対象が4語未満のテストで全800語へ黙って戻る導線、CSVのExcel運用弱さ、アプリ内ヘルプ不足が次点の打ち手として見えた。
- 最優先課題: ISS-002の `localStorage` 読み書き例外処理。今回、`setItem` だけでなく `getItem` が例外を投げる環境でも起動前に停止する再現を追加確認した。
- 次回の狙い: まず制約環境で起動継続できる状態にし、その後「狙った単語を復習できる」「CSVをそのまま業務で使える」導線を小さく強化する。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`, `Parts1.html`, `Parts2.html`, `Parts3.html`, `Parts4.html`
- 対象画面: 学習ビュー、検索/フィルタ、テストビュー、結果ビュー、設定モーダル、フッター操作、README説明
- 主な利用フロー: 初回表示 -> 検索/フィルタ -> 単語確認 -> ステータス更新 -> フィルタ条件つきテスト開始 -> 回答 -> 結果確認 -> 復習 -> CSV出力 -> 設定/リセット
- 確認した観点: 既存Issue重複、保存/読込失敗、4語未満フィルタ、テスト出題の再現性、CSV出力、アクセシビリティ、CSSルール、README/販売訴求、配布ファイルの混在

### 2. 既存課題の確認結果
- 前回の上位3件の状態:
  - ISS-002: 未解決。`_saveState()` は `localStorage.setItem()` を `try/catch` なしで呼ぶ状態が継続。さらに `_loadState()` の `localStorage.getItem()` も `try/catch` の外にあり、読み取り禁止環境で起動停止する追加証拠を確認した。
  - ISS-001: 未解決。`README.md` は「外部通信なし（ローカル完結）」を記載し、本体HTMLはGoogle Fonts / Material Symbolsを参照する状態が継続。
  - ISS-003: 未解決。Node抽出で `WORDS` は800件、`exampleEn: 800件空`, `exampleJa: 800件空`, `meaningSub: 800件空` を再確認した。
- 継続課題:
  - ISS-004: 結果画面の主CTAが優先復習語へジャンプしない。
  - ISS-005: 進行中テストが確認なしで新規テストに置き換わる。
  - ISS-006: CSVにテスト履歴が含まれない。
  - ISS-007: 設定モーダルのフォーカストラップ/復帰がない。
  - ISS-008: 検索・フィルタ・タブのARIA状態が不足。
  - ISS-009: `sessions` 復元データの境界検証が弱い。
  - ISS-011: README/LPの購入理由説明が不足。
  - ISS-012: Parts系ファイルが配布物に混ざる可能性。
- 今回クローズ可能と判断した課題: なし。今回は監査のみで、アプリ本体・READMEは変更していないため未解決扱い。
- 重複のため新規化しなかった論点:
  - `localStorage.getItem()` 例外、破損JSONの黙殺、キャンセル後も `_saveState()` が走る点は、ISS-002の追加証拠として扱う。
  - 復元済み `sessions.finishedAt` が不正値の場合に `_formatDateTime()` が例外化し得る点は、ISS-009の追加証拠として扱う。
  - タブ/フィルタ/ステータスボタンの状態読み上げ不足は、ISS-008の追加証拠として扱う。
  - READMEに対象ユーザーや有料版価値がない点は、ISS-011の追加証拠として扱う。

### 3. 発見した課題一覧
#### ISS-001（更新）
- 分類: 訴求 / 技術負債
- タイトル: 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
- 種別: 事実
- 要約: 外部通信説明と本体HTMLのフォント/アイコン取得がまだ一致していない。制約環境で使える理由を売るには、実HTMLとREADMEの文言を合わせる必要がある。
- 証拠: `README.md` 33-35行目に `localStorage` と「外部通信なし（ローカル完結）」。`MustKnowCodingEnglish.html` 7-9行目に `fonts.googleapis.com` / `fonts.gstatic.com` / Material Symbols参照。`MustKnowCodingEnglish.html` 971行目に `material-symbols-rounded` の `code` アイコン。
- 内容: 学習データはローカル保存だが、初期表示のフォント/アイコンは外部取得に依存する。完全オフラインを言うなら依存削除、学習データ送信なしを言うならREADME文言修正が必要。
- ユーザー影響: ネットワーク遮断PCや社内プロキシ環境で表示差が出て、購入前の説明と挙動のずれが信頼を下げる。
- 期待効果: 「1HTML配布」「学習データを送らない」「完全オフライン対応」のどれを売るか明確にできる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 本体HTMLのMaterial Symbols依存をSVG/テキストへ置換するか、READMEを「学習データの外部送信なし」に言い換える。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-002（更新）
- 分類: バグ / UX
- タイトル: `localStorage` 読み書き失敗時に起動・操作が例外で止まる
- 種別: 事実
- 要約: 保存失敗だけでなく、読み取り失敗でも起動前に例外終了する。制約端末で使えることを売るなら、保存不可時の一時利用モードが必要。
- 証拠: `MustKnowCodingEnglish.html` 9352行目で `localStorage.getItem()` を `try/catch` の外で呼ぶ。9439-9442行目の `_saveState()` は `localStorage.setItem()` を直接呼ぶ。10300行目で起動直後に `_saveState()` を実行。Nodeスタブで `getItem()` が `blocked getItem` を投げると `GETITEM_THROW_REPRODUCED`、`setItem()` が `quota exceeded` を投げると `SETITEM_THROW_REPRODUCED` を確認。
- 内容: 管理端末、プライベートブラウズ、容量上限、ブラウザ設定でlocalStorageが使えない場合、アプリの学習画面確認すら不安定になる。破損JSON時も起動直後の保存で初期状態を書き戻すため、復旧案内が出ない。
- ユーザー影響: 初回表示、回答、検索、テーマ変更、リセットキャンセル後の保存処理で例外化し、学習継続や信頼に直接影響する。
- 期待効果: 保存不可端末でも「一時利用中」と明示して学習カード確認・CSV出力など一部利用を継続できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_loadState()` の `getItem()` と `_saveState()` の `setItem()` を `try/catch` 化し、`_runtime._storageError` をフッターに表示する。破損JSON時は初期化前に「保存データを読み込めませんでした」を出す。
- スコア: ユーザー被害5 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与5 = 22

#### ISS-003（更新）
- 分類: 追加機能 / 訴求
- タイトル: 全800語の個別例文が未投入で学習価値が伝わりにくい
- 種別: 事実
- 要約: 800語すべての個別例文が空で、カード表示は汎用テンプレートに依存している。無料単語表との差別化が伝わりにくい。
- 証拠: Node抽出で `WORDS.total: 800`, `empty.exampleEn: 800`, `empty.exampleJa: 800`, `empty.meaningSub: 800`。`MustKnowCodingEnglish.html` 9505-9530行目の `_getExampleEn()` / `_getExampleJa()` が空値時テンプレートを返す。先頭15語は `accept`, `access`, `account`, `algorithm`, `allow`, `alternative`, `application`, `apply`, `argument`, `array`, `attribute`, `audio`, `author`, `available`, `avoid`。
- 内容: 初回表示に近い語で実務文脈が出ないため、PRレビュー、エラー対応、仕様書で使う英語を覚えられることが画面から判断しにくい。
- ユーザー影響: 初見ユーザーが「単語帳以上の価値」を数分で判断できず、購入理由が弱くなる。
- 期待効果: 上位30語だけでも実務例文を入れると、導入直後に「自分の現場で使う語だ」と判断しやすくなる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: 先頭30語または販売デモで見せたい30語に、英語1文・日本語訳1文の実務例文を追加する。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ3 / 壊しにくさ5 / 保守性寄与4 = 21

#### ISS-006（更新）
- 分類: 追加機能 / 訴求
- タイトル: CSV出力が学習ステータスだけで、テスト履歴や復習理由を持ち出せない
- 種別: 事実
- 要約: セッション履歴は内部に最大30件保存されるが、CSVは単語ステータスだけを出す。研修報告や端末移行に使う情報が不足している。
- 証拠: `MustKnowCodingEnglish.html` 9769-9778行目で `sessions` に `finishedAt`, `total`, `correct`, `answers` を保存。10123-10138行目の `_exportCsv()` は `word,status,level,meaning` のみを出力。
- 内容: 「どの単語で間違えたか」「いつ実施したか」「正答率がどう変わったか」がCSVに出ないため、買い切り教材の成果物として弱い。
- ユーザー影響: ブラウザ外での復習計画、研修報告、学習履歴バックアップに使いにくい。
- 期待効果: セッションCSVを追加すると、本番利用・再利用・安心の価値を有料版の理由として出せる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `progress.csv` は現状維持し、別ボタンまたは設定内で `sessions.csv` を `finishedAt,word,correct,selected,isCorrect,status` 形式で出す。
- スコア: ユーザー被害3 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 20

#### ISS-007（更新）
- 分類: アクセシビリティ / UX
- タイトル: 設定モーダルにフォーカストラップと復帰処理がない
- 種別: 事実
- 要約: `role="dialog"` / `aria-modal="true"` はあるが、開閉時のフォーカス管理がない。キーボード利用時に現在位置を見失いやすい。
- 証拠: `MustKnowCodingEnglish.html` 1193行目に `role="dialog" aria-modal="true"`。9839-9842行目の `_openSettings()` は `.hidden` と `aria-hidden` の切り替えのみ。10294-10297行目はEscapeで閉じるだけ。
- 内容: 開いた直後の初期フォーカス、Tab循環、閉じた後のトリガー復帰が実装されていない。
- ユーザー影響: 制約環境でマウスが使いにくいユーザーや支援技術利用者が設定操作後に迷う。
- 期待効果: 企業導入時の基本アクセシビリティ説明がしやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 開いたボタンを `_runtime._settingsTrigger` に保持し、閉じるボタンへ初期フォーカス、Tab循環、close時復帰を追加する。
- スコア: ユーザー被害4 / 購入理由への寄与3 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 19

#### ISS-008（更新）
- 分類: アクセシビリティ / UX
- タイトル: 検索・フィルタ・タブの選択状態が視覚表現に寄っている
- 種別: 事実
- 要約: タブ/フィルタ/ステータスの状態がクラス中心で、読み上げに必要な属性が不足している。検索欄もplaceholder依存のまま。
- 証拠: `MustKnowCodingEnglish.html` 974-977行目のタブに `aria-current` / `aria-selected` なし。990-993行目の検索inputに `<label>` / `aria-label` なし。9868-9888行目のフィルタ生成に `aria-pressed` なし。9945-9948行目のステータスボタン選択も `.is-selected` のみ。
- 内容: 画面を見れば状態は分かるが、読み上げやキーボード中心操作では現在の絞り込み条件や選択中ステータスが把握しにくい。
- ユーザー影響: フィルタ条件を誤認し、意図しない単語セットで学習やテストを開始する可能性がある。
- 期待効果: 状態が支援技術に伝わり、制約のある現場でも使える根拠が増える。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 検索に `aria-label`、タブに `aria-current`、フィルタ/ステータスに `aria-pressed` を付与する。
- スコア: ユーザー被害3 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与4 = 19

#### ISS-009（更新）
- 分類: セキュリティ / 技術負債
- タイトル: 保存済みセッション値を表示する境界検証が弱い
- 種別: 推論
- 要約: `sessions` はlocalStorageから復元されるが、回答値や日時の型検証が浅い。表示時に `innerHTML` と `Intl.DateTimeFormat` へそのまま渡す箇所がある。
- 証拠: `MustKnowCodingEnglish.html` 9416-9418行目は `session && typeof session === "object"` のみで復元。9815-9826行目の `_formatDateTime()` は `new Date(dateInput)` を `Intl.DateTimeFormat().format()` に渡す。10044-10056行目は `answer.wordId`, `answer.correct`, `answer.selected` を `innerHTML` に挿入。
- 内容: 通常操作では内部生成値なので危険度は限定的。ただし保存データの手動編集、別版からの移行、壊れたlocalStorageでは、結果画面の例外やHTML注入の入口になり得る。
- ユーザー影響: 共有端末や移行時に結果画面が壊れ、履歴確認ができない可能性がある。
- 期待効果: 将来のバックアップ/復元機能追加時にも使える境界検証になる。
- 緊急度: 低
- 実装コスト: 中
- 確信度: 中
- 次の行動: `sessions` 復元時に `finishedAt`, `total`, `correct`, `answers[].wordId`, `selected`, `correct`, `isCorrect` を検証し、結果テーブルはDOM生成またはエスケープ関数を使う。
- スコア: ユーザー被害3 / 購入理由への寄与3 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与5 = 18

#### ISS-011（更新）
- 分類: 訴求
- タイトル: README/LPにデモ版と有料版の価値差がない
- 種別: 事実
- 要約: READMEは機能説明中心で、誰がなぜ買うか、無料版で何を体験し、有料版で何が業務価値になるかがまだ書かれていない。
- 証拠: `README.md` 1-41行目はファイル、画面構成、アイコン、学習データ、保存、使い方の説明のみ。対象ユーザー、無料/有料差、価格、導入直後の体験、スクリーンショット指示はない。
- 内容: 800語や4択テストは機能として分かるが、PRレビュー・エラー対応・社内研修での痛みに接続されていない。
- ユーザー影響: 初見ユーザーが「無料の単語表やAnkiではなくこれを使う理由」を判断しにくい。
- 期待効果: 実装済みの検索、4択、復習、CSV、ローカル保存を購入理由へ変換できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 次回README更新で「対象ユーザー」「3分で試せる流れ」「無料版/有料版の差」「制約環境で使える理由」「現在の制限」を追加する。
- スコア: ユーザー被害2 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ5 / 保守性寄与3 = 19

#### ISS-012（更新）
- 分類: 技術負債 / 訴求
- タイトル: 統合元のPartsファイルが配布物に混ざると旧UIや空ファイルを開かれる可能性がある
- 種別: 事実
- 要約: 本体以外のParts系ファイルが同階層に残っている。販売ZIPや社内配布時に誤って開かれるリスクがある。
- 証拠: `find . -maxdepth 1 -type f` で `Parts1.html`, `Parts2.html`, `Parts3.html`, `Parts4.html` を確認。`wc` では `Parts3.html` は0行・0バイト。README 5-6行目では本体が `MustKnowCodingEnglish.html` とだけ記載。
- 内容: 開発資料としては残せるが、配布対象が明確でないと、購入者やレビュー担当が旧UI/空ファイルを完成版と誤認する。
- ユーザー影響: 最初に空ファイルや旧UIを開き、品質を誤解する可能性がある。
- 期待効果: 配布物の迷いを減らし、サポート問い合わせを減らせる。
- 緊急度: 低
- 実装コスト: 中
- 確信度: 高
- 次の行動: 今回は移動禁止のため記録のみ。次回、READMEに「開くファイルは `MustKnowCodingEnglish.html` のみ」を強調するか、販売用パッケージからParts系を除外する。
- スコア: ユーザー被害2 / 購入理由への寄与4 / 実装しやすさ3 / 壊しにくさ4 / 保守性寄与3 = 16

#### ISS-013
- 分類: バグ / UX
- タイトル: フィルタ対象が4語未満のテストで全800語へ黙って戻る
- 種別: 事実
- 要約: 現在のフィルタ結果が1-3語の場合、テストは説明なしで全単語を出題元にする。狙った苦手語やアルファベットで復習する期待とずれる。
- 証拠: `MustKnowCodingEnglish.html` 9586-9589行目で `_filtered.length >= 4 ? _filtered : WORDS`。テスト画面の1107-1111行目は「現在のフィルタを優先して10問まで出題します」と説明。Node抽出では `Z: ["zip"]`, `K: ["key"]`, `X: ["XML"]`, `Q: ["query","queue"]`, `J: ["join","JSON"]`, `Y: ["you","your"]` で4語未満。全体フォールバック時の先頭10語は `accept` から `array`。
- 内容: 例としてAlphabetを `Z` に絞って「この条件で開始」を押しても、`zip` だけでなく全体先頭側の単語が出得る。苦手ステータスが1-3語のときも同じ問題が起きる。
- ユーザー影響: ピンポイント復習をしたつもりが別単語のテストになり、学習意図と結果がずれる。
- 期待効果: フィルタ条件への信頼が上がり、短時間で苦手だけを確認できる購入理由につながる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 1-3語の場合は「対象語 + 誤答候補は全体から補完」とし、出題語自体はフィルタ結果を維持する。UIには「対象語が少ないため選択肢だけ全体から補完」と表示する。
- スコア: ユーザー被害4 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-014
- 分類: UX / 改善
- タイトル: 新しいテストを繰り返しても同じ先頭10語になりやすい
- 種別: 推論
- 要約: テスト順序は `randomCursor` に依存するが、`_startQuiz()` はカーソルを進めない。初回ユーザーは同じ条件でテスト開始を繰り返すと同じ語が出やすい。
- 証拠: `MustKnowCodingEnglish.html` 9586-9596行目の `_buildQuizOrder()` は `_state.learning.randomCursor` でロールする。9599-9605行目の `_startQuiz()` は `randomCursor` を更新しない。初期の先頭10語は `accept`, `access`, `account`, `algorithm`, `allow`, `alternative`, `application`, `apply`, `argument`, `array`。
- 内容: deterministic設計自体は保守しやすいが、テスト開始ごとにカーソルを動かさないため、ユーザーは「新しいテスト」と感じにくい。
- ユーザー影響: 800語収録の広さが体験されにくく、デモ時にも同じ語だけを見て価値判断される。
- 期待効果: 既存の再現性を保ったまま、テストごとに出題範囲を広げられる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: `_startQuiz()` 成功時に `randomCursor` を出題数分だけ進めるか、「前回の続きから出題」モード名に変える。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与3 = 18

#### ISS-015
- 分類: UX / 訴求
- タイトル: CSVにBOMがなくExcelで日本語が文字化けする可能性がある
- 種別: 推論
- 要約: CSVはUTF-8としてBlob化されるがBOMが付いていない。日本語の意味・ステータスを含むため、Excelダブルクリック運用では文字化けリスクがある。
- 証拠: `MustKnowCodingEnglish.html` 10123-10130行目の `_exportCsv()` は `word,status,level,meaning` を生成し、`new Blob([_csv], { type: "text/csv;charset=utf-8" })` を使う。`\uFEFF` やBOM付与はない。CSVセルには `覚えた`, `ベーシック`, `受諾する、受け入れる` など日本語が入る。
- 内容: 業務ツールではCSVをExcelでそのまま開くユーザーが多い。文字化けが起きると、出力機能の価値が低く見える。
- ユーザー影響: 研修報告や進捗共有でCSVを開いた時に、意味やステータスが読めない可能性がある。
- 期待効果: 1行のBOM付与でCSV出力の実務利用感が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: Blob内容を `new Blob(["\uFEFF", _csv], { type: "text/csv;charset=utf-8" })` に変更し、Excel/Numbers/テキストエディタで確認する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-016
- 分類: アクセシビリティ / UX
- タイトル: 4択回答後の正誤フィードバックがライブ領域になっていない
- 種別: 事実
- 要約: 回答後に「正解です」「不正解」と表示されるが、`aria-live` がない。画面を見ない操作では結果が通知されにくい。
- 証拠: `MustKnowCodingEnglish.html` 1120行目の `<p class="feedback" id="quiz-feedback"></p>` に `aria-live` なし。10002-10012行目で `_dom.$quizFeedback.textContent` を更新。`rg` で `aria-live` は検出なし。
- 内容: 4択テストは回答直後のフィードバックが主要体験だが、支援技術へ変化を伝える属性がない。
- ユーザー影響: キーボード/スクリーンリーダー利用者が正誤を把握しにくく、次へ進む判断が遅れる。
- 期待効果: `aria-live="polite"` 追加だけで回答フローのアクセシビリティを改善できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `#quiz-feedback` に `role="status"` または `aria-live="polite"` を付与し、正誤表示更新時に読み上げられるようにする。
- スコア: ユーザー被害3 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与4 = 19

#### ISS-017
- 分類: UX / 訴求
- タイトル: フッターの「ヘルプ / 設定」がヘルプ内容を表示しない
- 種別: 事実
- 要約: フッターにはヘルプと書かれているが、開くモーダルはテーマ・データ管理・バージョンのみ。初見ユーザー向けの使い方や保存範囲の説明がない。
- 証拠: `MustKnowCodingEnglish.html` 1187-1189行目に `CSV出力`, `ヘルプ / 設定`, `学習状態リセット`。1193-1233行目の設定モーダルにはAppearance、Data Management、Informationのみ。`README.md` 37-41行目には使い方があるがアプリ内にはない。
- 内容: 初見ユーザーが「この条件で開始」「CSV出力」「履歴の消去」の意味をアプリ内だけで確認できない。
- ユーザー影響: 配布先でREADMEを読まずにHTMLだけ開いた場合、保存場所やCSVの用途が伝わりにくい。
- 期待効果: アプリ単体で導入直後の迷いを減らし、1HTML配布の価値が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: モーダルに「3分の使い方」「保存されるデータ」「CSVで出せる内容」「完全オフライン化の状態」を短く追加する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-018
- 分類: 技術負債 / UX
- タイトル: CSSルールと現行スタイルに小さな不一致がある
- 種別: 事実
- 要約: リポジトリルールでは色定義に `oklch` を使うが、ライトテーマにHEX色が残っている。加えて文字間が負値で、UI基準変更時の調整箇所になり得る。
- 証拠: `MustKnowCodingEnglish.html` 43-50行目に `#FCFCFC`, `#FFFFFF`, `#F5F5F5`, `#2C3137`, `#636A72`, `#E5E7EA`。75-80行目に `letter-spacing: -0.31px`。Node抽出でHEX色6種類、negative letter-spacing 1件を確認。
- 内容: 見た目の即時バグではないが、今後のデザイン調整やダーク/ライトテーマ差分確認で基準がぶれやすい。
- ユーザー影響: 日本語を含む画面で文字間が詰まり、長い意味文やCSV説明を追加した時に読みにくくなる可能性がある。
- 期待効果: 色定義と文字間の基準を統一すると、将来のUI変更時に差分理由を説明しやすい。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: 機能修正とは別の小タスクとして、ライトテーマのHEXをoklchへ置換し、bodyの `letter-spacing` を0に戻して表示確認する。
- スコア: ユーザー被害2 / 購入理由への寄与2 / 実装しやすさ3 / 壊しにくさ3 / 保守性寄与4 = 14

### 4. 上位3件
- 1位: ISS-002 `localStorage` 読み書き失敗時に起動・操作が例外で止まる
  - 選定理由: ユーザー被害5。保存不可だけでなく読み取り不可でも起動停止を再現し、制約端末での主要価値に直撃する。
  - 今やるべき理由: 1HTMLローカルツールの信頼土台で、修正範囲が `_loadState()`, `_saveState()`, `_renderFooter()` に限定できる。
  - 実装イメージ: `getItem` / `setItem` を `try/catch` 化し、失敗時はメモリ上で継続。フッターへ「一時利用中、保存不可」を表示する。
  - 想定UI: `保存状態: このブラウザでは保存できません。一時利用中です。`
  - 想定ユースケース: 社内端末でlocalStorageがブロックされているが、HTMLファイルで単語確認だけ行う。
  - リスク: 保存不可でも操作できるため、履歴が残らないことを見落とす可能性がある。文言で「一時利用中」を明示する。
  - 着手順: `_runtime._storageError` 追加 -> `_loadState()` guard -> `_saveState()` guard -> フッター分岐 -> `getItem` / `setItem` 例外スタブ確認。
  - 受け入れ条件: `localStorage.getItem` または `setItem` が例外を投げても初期表示処理が例外終了しない。保存不可メッセージがフッターに出る。
- 2位: ISS-001 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
  - 選定理由: 購入理由への寄与5。制約環境で使える価値とREADMEの信頼性に直結する。
  - 今やるべき理由: 外部通信の定義を先に固定しないと、LP/README/有料版訴求がぶれる。
  - 実装イメージ: Material SymbolsをインラインSVGまたはテキストに置換し、必要ならREADMEの「外部通信なし」を実態に合わせる。
  - 想定UI: ヘッダー左のコードアイコンはSVG、フォントはシステムフォント。オンライン/オフラインで表示差を小さくする。
  - 想定ユースケース: ネットワーク遮断された研修PCでHTMLを直接開く。
  - リスク: フォント置換で見た目が少し変わる。SVGサイズと色を既存CSS変数に合わせて差分を抑える。
  - 着手順: 外部参照洗い出し -> ヘッダーアイコン置換 -> README文言修正 -> ネットワークなし相当で表示確認。
  - 受け入れ条件: 本体HTMLに `fonts.googleapis.com` / `fonts.gstatic.com` 参照が残らないか、READMEの表現が「学習データの外部送信なし」に修正されている。
- 3位: ISS-003 全800語の個別例文が未投入で学習価値が伝わりにくい
  - 選定理由: 購入理由への寄与5。無料単語表との差別化に直結し、先頭30語なら次回1回で着手できる。
  - 今やるべき理由: 初見ユーザーは最初の数分で価値判断するため、導入直後に実務文脈の例文が出ることが重要。
  - 実装イメージ: 先頭30語または販売デモ用30語に `exampleEn` / `exampleJa` を追加する。
  - 想定UI: Usage ExampleにPRレビュー、エラー対応、仕様書、CLI操作の短文が表示される。
  - 想定ユースケース: 新人開発者がPRで見た `argument` や `attribute` を検索し、意味と使い方を1画面で確認する。
  - リスク: 例文品質が低いと教材の信頼を落とす。短文・実務文脈・日本語訳の3条件で対象語を絞る。
  - 着手順: 30語選定 -> 例文フォーマット固定 -> `exampleEn` / `exampleJa` 追加 -> 検索とカード表示確認。
  - 受け入れ条件: 対象30語でテンプレートではない個別例文が表示され、未対応語は既存フォールバックで表示される。
- 次点: ISS-013。フィルタ対象4語未満の全体フォールバックはユーザー被害4で、苦手語だけ復習する期待に反するため、上位3件後すぐ着手候補。

### 5. 追加機能アイデア
- 機能名: 少数フィルタ対応テスト
  - 解決する課題: フィルタ対象が1-3語のとき全体出題へ黙って戻る。
  - 概要: 出題語はフィルタ結果を維持し、誤答選択肢だけ全体から補完する。
  - ユーザー価値: 苦手語や特定アルファベットだけを短時間で確認できる。
  - 差別化ポイント: 「狙った語だけ復習できる」体験を保証できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: Excel向けCSV出力
  - 解決する課題: 日本語CSVがExcelで文字化けする可能性。
  - 概要: UTF-8 BOMを付与し、必要ならファイル名に日付を入れる。
  - ユーザー価値: 出力後にそのまま研修報告や進捗確認に使いやすい。
  - 差別化ポイント: 業務ツールとしての「出した後に使える」価値が増える。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: アプリ内3分ヘルプ
  - 解決する課題: `ヘルプ / 設定` にヘルプ内容がない。
  - 概要: 設定モーダルに使い方、保存範囲、CSV出力、制約環境での注意を短く追加する。
  - ユーザー価値: READMEを読まなくてもHTML単体で使い始められる。
  - 差別化ポイント: 1ファイル配布時の初期理解が早くなる。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: テスト出題カーソル更新
  - 解決する課題: 新しいテストが同じ先頭10語になりやすい。
  - 概要: テスト開始成功時に `randomCursor` を進め、次回は続きから出題する。
  - ユーザー価値: 800語の広さを短時間で体験しやすい。
  - 差別化ポイント: ランダムではなく再現性のある巡回式として説明できる。
  - 実装コスト: 小
  - 優先度: 中

### 6. 訴求・販売改善案
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語PRレビューやエラー文に苦手意識がある開発者、社内研修で外部サービスを使いにくい教育担当者。
- 解決している具体的な痛み: 開発文脈で頻出する英単語だけを、辞書・Anki・スプレッドシートを往復せずに確認したい。学習履歴を外部サービスに送らず、社内端末で配布したい。
- 今でも刺さる強み: 800語、レベル/ステータス/アルファベット検索、4択テスト、結果、優先復習、localStorage保存、CSV出力が1HTMLにまとまっている。
- 伝わっていない価値: 実務例文がないため「開発現場の英語」感が弱い。フィルタ少数時のテスト挙動も、狙った復習ができる価値を損ねる。
- 買う理由として弱い点: 完全オフライン説明と外部フォント参照の矛盾。CSVが履歴/Excel運用に弱い。READMEに無料版/有料版の差がない。
- READMEに追加すべき説明: 対象ユーザー、3分の体験手順、保存されるデータ、外部通信の正確な定義、CSVで出せる内容、現在の制限、配布時に開くファイル名。
- LPに追加すべき訴求: 「PRレビュー・エラー対応・仕様書で見る英語を1HTMLで復習」「学習データを外へ送らない」「苦手語だけを短時間で再確認」「研修PCに配りやすい」。
- スクリーンショットで見せるべき場面: 検索で `argument` を出したカード、実務例文つきUsage Example、4択回答後の正誤、結果画面の優先復習チップ、CSVをExcelで開いた場面。
- 無料版で見せるべき体験: 100語、5問テスト、検索、数語の実務例文、直近結果、CSVのサンプル1種。良さは伝わるが本番運用には語数・履歴出力・バックアップが不足する範囲。
- 有料版でしか得られない体験: 800語、実務例文パック、10問テスト、少数フィルタ対応、セッションCSV、バックアップ/復元、完全オフライン版、社内配布向けREADME。
- 制約のある現場で使える理由: 1HTMLで配布できる。進捗はブラウザ内に保存できる。完全オフラインを言うにはフォント/アイコン外部参照を外す必要がある。
- 他手段より楽な理由: 単語検索、意味確認、4択テスト、復習候補、CSV出力が同じ画面内にあり、教材・辞書・表計算を切り替えなくて済む。
- 導入直後に感動できる体験があるか: 現状は800語の量は見えるが、例文が汎用テンプレートで弱い。先頭30語の実務例文と少数フィルタ対応が入ると、初回3分で価値を感じやすい。
- 強み: 1ファイルSPA、800語、ローカル保存、フィルタ、4択、結果、復習、CSV。
- 弱み: 保存不可時の例外、外部通信説明の矛盾、個別例文なし、少数フィルタのテスト挙動、CSV履歴不足、アプリ内ヘルプ不足。
- 使う理由: 外部サービスに学習データを出さず、開発文脈の頻出英語を短時間で復習するため。
- デモ版/有料版の切り分け案: デモ版は100語・5問・数語例文・進捗CSVまで。有料版は800語・実務例文・セッションCSV・バックアップ/復元・完全オフライン・社内配布説明で「時短、安心、再利用、本番利用」を売る。

### 7. 次回おすすめタスク
- 1. `_loadState()` / `_saveState()` のlocalStorage例外処理と保存不可フッター表示を実装し、`getItem` / `setItem` 例外スタブで起動継続を確認する。
- 2. `MustKnowCodingEnglish.html` のGoogle Fonts / Material Symbols依存を外すか、READMEの「外部通信なし」を実態に合わせる。
- 3. 優先30語に実務例文を追加する。次点でISS-013の少数フィルタ対応テストを入れると、苦手語復習の体験価値が上がる。

### 8. 実行ログ
- 今回アプリコードを変更していないこと: 変更していない。
- 今回READMEを変更していないこと: 変更していない。
- 今回REPORT.mdのみ更新したこと: リポジトリ内では `REPORT.md` のみ追記した。
- 確認コマンド: `sed`, `nl`, `rg`, `wc`, `find`, Nodeによる `WORDS` 抽出、インラインJS構文確認、スタブDOM初期実行、localStorage例外スタブ再現。
- 起動確認の結果: インラインJS構文確認は `SCRIPT_PARSE_OK`。スタブDOM初期実行は `STUB_DOM_START_OK` で `#word-term` が `accept`、保存状態が表示された。
- 起動確認の制約: `/Applications/Google Chrome.app` は存在したが、ヘッドレスの `--dump-dom` はcode -1、CDP接続試行はタイムアウト。実ブラウザのコンソールエラー完全確認は未完了として扱う。

## 2026-04-15
### 0. エグゼクティブサマリー
- 今回の結論: 前回上位3件（localStorage例外、外部通信説明、個別例文不足）は未解決。加えて、現在のデフォルトテストは800問セッションになり、初見ユーザーが短時間で結果画面まで到達しにくいことを確認した。
- 最優先課題: ISS-002の `localStorage` 読み書き例外処理。`getItem` / `setItem` 例外スタブで起動停止を再現でき、制約端末での利用価値に直接影響する。
- 次回の狙い: 起動継続性を先に確保し、その次に「10問程度で結果が出る短時間テスト」を入れて、導入直後に価値を判断できる流れへ寄せる。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`, `Parts1.html`, `Parts2.html`, `Parts3.html`, `Parts4.html`
- 対象画面: 学習ビュー、検索/フィルタ、テストビュー、結果ビュー、設定モーダル、フッター操作、README説明、配布フォルダ構成
- 主な利用フロー: 初回表示 -> 単語検索/フィルタ -> ステータス更新 -> テスト開始 -> 4択回答 -> 結果確認 -> 優先復習 -> CSV出力 -> 設定/履歴削除
- 確認した観点: 前回Issueの重複確認、保存/読込失敗、テスト出題数、少数フィルタ、回答後UI、結果履歴、CSV出力、アクセシビリティ、README/販売訴求、制約環境での利用価値

### 2. 既存課題の確認結果
- 前回の上位3件の状態:
  - ISS-002: 未解決。`_loadState()` の `localStorage.getItem()` は `try/catch` の外、`_saveState()` の `localStorage.setItem()` も直接呼び出しのまま。Nodeスタブで `GETITEM_THROW_REPRODUCED blocked getItem` と `SETITEM_THROW_REPRODUCED quota exceeded` を再確認した。
  - ISS-001: 未解決。`README.md` は「外部通信なし（ローカル完結）」を記載し、本体HTMLはGoogle Fonts / Material Symbolsを参照する状態が継続。
  - ISS-003: 未解決。Node抽出で `WORDS.total: 800`, `empty.exampleEn: 800`, `empty.exampleJa: 800`, `empty.meaningSub: 800` を再確認した。
- 継続課題:
  - ISS-004: 結果画面の主CTAが優先復習語へジャンプしない。
  - ISS-005: 進行中テストが確認なしで新しいテストに置き換わる。
  - ISS-006: CSVにテスト履歴が含まれない。
  - ISS-007: 設定モーダルのフォーカストラップ/復帰がない。
  - ISS-008: 検索・フィルタ・タブのARIA状態が不足。
  - ISS-009: `sessions` 復元データの境界検証が弱い。
  - ISS-011: README/LPの購入理由説明が不足。
  - ISS-012: Parts系ファイルが配布物に混ざる可能性。
  - ISS-013: フィルタ対象が4語未満のテストで全体出題へ戻る。
  - ISS-015: CSVにBOMがなくExcel運用で文字化けする可能性。
  - ISS-016: 正誤フィードバックにライブ領域がない。
  - ISS-017: `ヘルプ / 設定` にヘルプ内容がない。
  - ISS-018: HEX色と負のletter-spacingが残る。
- 今回クローズ可能と判断した課題: なし。今回は監査のみで、アプリ本体・READMEは変更していない。
- 重複のため新規化しなかった論点:
  - 検索入力のたびに `localStorage.setItem()` が走り、保存不可環境で入力操作も止まり得る点はISS-002の追加影響として扱う。
  - READMEの `Inter` 方針とHTMLの実フォント読み込み不足は、外部参照/表記整合の問題としてISS-001またはISS-018に含め、新規Issue化しない。
  - デフォルト800問によりlocalStorage保存量が増える点は、主要原因がテスト出題数と保存例外の組み合わせのためISS-019とISS-002に含める。
  - テスト前に対象条件が見えない点は、ISS-019の想定UIに「出題数プレビュー」として含める。

### 3. 発見した課題一覧
#### ISS-002（更新）
- 分類: バグ / UX
- タイトル: `localStorage` 読み書き失敗時に起動・操作が例外で止まる
- 種別: 事実
- 要約: 読み取り禁止・書き込み禁止のどちらでも例外を捕捉できていない。制約端末で「一時利用はできる」状態にならず、ローカル完結の信頼を落とす。
- 証拠: `MustKnowCodingEnglish.html` 9352行目で `localStorage.getItem(STORAGE_KEY)` を `try/catch` 外で実行。9439-9442行目の `_saveState()` は `localStorage.setItem()` を直接呼ぶ。10300行目で起動直後に `_saveState()` を呼ぶ。Nodeスタブで `getItem()` 例外と `setItem()` 例外の停止を再現。
- 内容: 管理端末、プライベートブラウズ、容量上限、ブラウザ設定でlocalStorageが使えない場合、初回表示、検索入力、テーマ変更、CSV出力後処理、リセットキャンセル後の保存で例外化し得る。
- ユーザー影響: HTMLを開いた直後に止まる、または操作後に画面更新されず、制約環境で使えるという購入理由が崩れる。
- 期待効果: 保存不可でも学習カード確認や一時テストを継続でき、保存できない理由を画面上で説明できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_loadState()` と `_saveState()` を `try/catch` 化し、`_runtime._storageError` を追加。フッターに「保存不可。一時利用中」を表示し、`getItem` / `setItem` 例外スタブで起動継続を確認する。
- スコア: ユーザー被害5 / 購入理由への寄与4 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与5 = 22

#### ISS-019
- 分類: UX / 訴求 / 改善
- タイトル: デフォルトのテストが800問になり初回体験が重すぎる
- 種別: 事実
- 要約: フィルタなしでテストを開始すると、`WORDS` 全800語が1セッションの出題対象になる。短時間で正答率・復習ワードを見る導入体験と合わない。
- 証拠: `README.md` 10行目は「現在のフィルタ対象を全問」と記載。`MustKnowCodingEnglish.html` 1109行目も「全問を出題します」と表示。9586-9596行目の `_buildQuizOrder()` は `_source.map()` 全件を返し、9718行目の `total` は `_state.quiz.order.length`。Node確認でデフォルト `defaultOrderLength: 800`。
- 内容: 800語収録は販売上の強みだが、初回のテストが800問だと結果画面、優先復習、CSV価値まで到達しにくい。途中で学習へ戻ってもセッション結果は作られないため、導入直後の「できた」体験が薄い。
- ユーザー影響: 初見ユーザーが数問で離脱し、結果分析や復習導線の価値を確認できない。
- 期待効果: 10問または20問の標準テストにすると、3分以内に正答率・ミス単語・復習導線を体験できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 標準テストは `_buildQuizOrder().slice(0, 10)` 相当にし、UIに「10問テスト」「対象800語から10問」と表示する。全問テストは後続機能または設定内の明示モードに分ける。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ5 / 壊しにくさ4 / 保守性寄与4 = 22

#### ISS-001（更新）
- 分類: 訴求 / 技術負債
- タイトル: 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
- 種別: 事実
- 要約: READMEのローカル完結説明と本体HTMLの外部フォント/アイコン取得が一致していない。制約環境で使えることを売るには、実挙動と説明のどちらかを合わせる必要がある。
- 証拠: `README.md` 3行目に「ローカル完結」、35行目に「外部通信なし（ローカル完結）」。`MustKnowCodingEnglish.html` 7-9行目に `fonts.googleapis.com`, `fonts.gstatic.com`, Material Symbols参照。971行目に `material-symbols-rounded` の `code` アイコン。
- 内容: 学習データは外部送信していないが、画面表示は外部リソースに依存している。Parts系ファイルもTailwind CDN、Google Fonts、外部画像を含むため、配布フォルダ全体ではさらに説明がぶれやすい。
- ユーザー影響: ネットワーク遮断PCや社内プロキシ環境で表示差が出る。購入前説明と実挙動の差が信頼を落とす。
- 期待効果: 完全オフラインか「学習データ送信なし」かを明確にでき、LP/READMEの訴求を事実ベースにできる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 本体HTMLのMaterial Symbols依存をインラインSVG/テキストへ置換する。READMEは「完全オフライン」か「学習データの外部送信なし」のどちらかに表現を統一する。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-003（更新）
- 分類: 追加機能 / 訴求
- タイトル: 全800語の個別例文が未投入で学習価値が伝わりにくい
- 種別: 事実
- 要約: `exampleEn` / `exampleJa` / `meaningSub` が全件空で、画面のUsage Exampleは汎用テンプレートに依存している。無料単語表との差が初回画面で伝わりにくい。
- 証拠: Node抽出で `WORDS.total: 800`, `empty.exampleEn: 800`, `empty.exampleJa: 800`, `empty.meaningSub: 800`。`MustKnowCodingEnglish.html` 9505-9530行目で空値時のテンプレート例文を返す。9942-9943行目で学習カードへ表示。
- 内容: 先頭30語は `accept`, `access`, `account`, `algorithm`, `allow`, `alternative`, `application`, `apply`, `argument`, `array`, `attribute`, `audio`, `author`, `available`, `avoid`, `backup`, `based`, `bit`, `blank`, `block`, `boolean`, `branch`, `browser`, `bug`, `byte`, `cache`, `cancel`, `capacity`, `case`, `character`。この範囲に実務例文が入るだけでも導入直後の価値判断が変わる。
- ユーザー影響: 初見ユーザーが「開発文脈で覚える教材」という価値を確認できず、購入理由が弱くなる。
- 期待効果: PRレビュー、エラー対応、仕様書、CLI操作の例文があると、単語の意味だけでなく使う場面が伝わる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: 全800語ではなく、先頭30語または販売デモ用30語に英語1文・日本語訳1文の実務例文を追加する。
- スコア: ユーザー被害4 / 購入理由への寄与5 / 実装しやすさ3 / 壊しにくさ5 / 保守性寄与4 = 21

#### ISS-013（更新）
- 分類: バグ / UX
- タイトル: フィルタ対象が4語未満のテストで全800語へ黙って戻る
- 種別: 事実
- 要約: Alphabet `Z` など1-3語の条件でテストを開始すると、出題対象が全800語へ戻る。狙った単語だけ復習する期待に反する。
- 証拠: `MustKnowCodingEnglish.html` 9587-9589行目で `_filtered.length >= 4 ? _filtered : WORDS`。Node確認で `Z` は `["zip"]` だが `zOrderLength: 800`。4語未満の文字は `J`, `K`, `Q`, `X`, `Y`, `Z`。
- 内容: 誤答候補を4択分確保する目的は理解できるが、出題語まで全体へ戻すため、フィルタの意味が失われる。
- ユーザー影響: 苦手語や特定アルファベットだけ確認したいユーザーが、意図しない800語テストを始める。
- 期待効果: 出題語はフィルタ結果を維持し、誤答候補だけ全体から補完すれば、短時間復習の信頼が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 1-3語の場合は出題語リストを `_filtered` のままにし、選択肢生成だけ `WORDS` から補完する。テスト前に「対象1語、選択肢は全体から補完」と表示する。
- スコア: ユーザー被害4 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ4 / 保守性寄与4 = 21

#### ISS-004（更新）
- 分類: UX / 改善
- タイトル: 結果画面の「復習を開始する」が優先復習ワードへ移動しない
- 種別: 事実
- 要約: 結果画面に優先復習ワードのチップはあるが、主CTAは学習ビューへ戻るだけで復習対象を選ばない。
- 証拠: `MustKnowCodingEnglish.html` 1171-1177行目に優先復習パネル。1177行目のボタンは `data-action="switch-view" data-view="learning"`。10071-10077行目のチップは `select-word-and-study` で単語指定できる。
- 内容: 画面上の主CTA文言は「復習を開始する」だが、実際にはミス単語へ移動しない。チップ操作を知っているユーザーだけが直接復習できる。
- ユーザー影響: テスト後にどの単語から見ればよいか迷い、復習ループが切れる。
- 期待効果: ミス単語の先頭へジャンプできれば、テスト結果から学習カードへ戻る価値が明確になる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 主CTAを押した時、優先IDの先頭を `_state.learning.wordId` に設定してから学習ビューへ遷移する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-005（更新）
- 分類: UX / バグ
- タイトル: 進行中テストが確認なしで新しいテストに置き換わる
- 種別: 事実
- 要約: 回答中に「新しいテスト」やヘッダーの「テスト開始」を押すと、回答済みデータが確認なしで初期化される。現在は800問テストになり得るため、途中損失の影響が大きい。
- 証拠: `MustKnowCodingEnglish.html` 980行目、1102行目、1111行目に `data-action="start-quiz"`。9599-9605行目の `_startQuiz()` は `active`, `order`, `questionIndex`, `answers`, `choiceMap` を上書きする。進行中判定や確認ダイアログはない。
- 内容: 何問か回答した後に開始ボタンを押すと、セッション結果に残らないまま回答が消える。
- ユーザー影響: 学習時間が失われ、結果履歴への信頼が下がる。
- 期待効果: 確認またはラベル変更により、誤操作による回答消失を防げる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_startQuiz()` の先頭で `quiz.active && answers.length > 0` を判定し、確認後だけ初期化する。テスト中はボタン文言を「最初からやり直す」に変える。
- スコア: ユーザー被害4 / 購入理由への寄与3 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-006（更新）
- 分類: 追加機能 / 訴求
- タイトル: CSV出力が学習ステータスだけで、テスト履歴や復習理由を持ち出せない
- 種別: 事実
- 要約: セッション履歴は内部保存されるが、CSVは単語ステータスだけを出す。研修報告や復習計画に使う情報が不足している。
- 証拠: `MustKnowCodingEnglish.html` 9770-9778行目で `sessions` に `finishedAt`, `total`, `correct`, `answers` を保存。10123-10138行目の `_exportCsv()` は `word,status,level,meaning` のみ出力。
- 内容: どの単語をいつ間違えたか、正答率がどう変わったかをブラウザ外へ出せない。
- ユーザー影響: チーム研修、自己学習ログ、端末移行で結果履歴を再利用しにくい。
- 期待効果: セッションCSVを追加すると、本番利用・再利用・安心の価値を有料版の理由にできる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 既存の進捗CSVとは別に、`finishedAt,word,correct,selected,isCorrect,status` の履歴CSVを出す。
- スコア: ユーザー被害3 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 20

#### ISS-015（更新）
- 分類: UX / 訴求
- タイトル: CSVにBOMがなくExcelで日本語が文字化けする可能性がある
- 種別: 推論
- 要約: CSVはUTF-8としてBlob化されるがBOMを付けていない。日本語の意味・ステータスを含むため、Excelダブルクリック運用で文字化けする可能性がある。
- 証拠: `MustKnowCodingEnglish.html` 10129-10130行目で `new Blob([_csv], { type: "text/csv;charset=utf-8" })`。CSVセルには `覚えた`, `ベーシック`, `受諾する、受け入れる` などの日本語が入る。
- 内容: 業務現場ではCSVをExcelでそのまま開くことが多い。文字化けすると出力機能の価値が下がる。
- ユーザー影響: 研修報告や進捗共有でCSVを開いた時に、日本語が読めない可能性がある。
- 期待効果: BOM付与だけで、CSVの実務利用感が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: Blob内容を `["\uFEFF", _csv]` にし、Excel/Numbers/テキストエディタで確認する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-017（更新）
- 分類: UX / 訴求
- タイトル: フッターの「ヘルプ / 設定」がヘルプ内容を表示しない
- 種別: 事実
- 要約: フッターにはヘルプとあるが、モーダル内容はテーマ・データ管理・バージョンのみ。HTML単体で配布された時に、保存範囲やテストの使い方が伝わらない。
- 証拠: `MustKnowCodingEnglish.html` 1187-1189行目に `CSV出力`, `ヘルプ / 設定`, `学習状態リセット`。1193-1233行目のモーダルはAppearance、Data Management、Informationのみ。README 37-41行目の使い方はアプリ内にない。
- 内容: 特にデフォルトテストが800問である現状では、初見ユーザーが「まず何問試すべきか」「保存はどこに残るか」「CSVは何を出すか」をアプリ内で確認できない。
- ユーザー影響: READMEを読まずにHTMLだけ開いた購入者が、操作目的を理解しにくい。
- 期待効果: 1HTML配布時の導入支援になり、制約環境でも使い始めやすい。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: モーダルに「3分の使い方」「標準テストの問題数」「保存されるデータ」「CSVの内容」「外部通信の状態」を短く追加する。
- スコア: ユーザー被害3 / 購入理由への寄与4 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 20

#### ISS-020
- 分類: 追加機能 / 訴求 / UX
- タイトル: 最大30件のテスト履歴を保存しているが過去回を見返せない
- 種別: 事実
- 要約: `sessions` は最大30件まで保存されるが、画面表示は最新1件だけ。履歴削除の機能名に対して、履歴を確認する導線がない。
- 証拠: `MustKnowCodingEnglish.html` 9770-9778行目で `sessions` をpushし30件にslice。9579-9583行目の `_getLatestSession()` は末尾1件だけ返す。10021-10080行目の `_renderResults()` は `_getLatestSession()` のみ表示。1221行目には「履歴の消去（結果セッションのみ）」ボタンがある。
- 内容: 履歴を保存している事実はあるが、ユーザーは前回・前々回の正答率やミス単語を比較できない。
- ユーザー影響: 学習の伸びや研修成果を画面上で確認しにくく、継続利用の動機が弱くなる。
- 期待効果: 直近5件の履歴一覧だけでも、学習の積み上がりと再利用価値を見せられる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 結果画面に「最近のテスト」リストを追加し、日時、正答率、問題数、ミス数を表示する。詳細表示は後回しにする。
- スコア: ユーザー被害3 / 購入理由への寄与5 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 20

#### ISS-007（更新）
- 分類: アクセシビリティ / UX
- タイトル: 設定モーダルにフォーカストラップと復帰処理がない
- 種別: 事実
- 要約: `role="dialog"` / `aria-modal="true"` はあるが、開閉時のフォーカス管理がない。キーボード利用時に現在位置を失いやすい。
- 証拠: `MustKnowCodingEnglish.html` 1193行目に `role="dialog" aria-modal="true"`。9839-9842行目の `_openSettings()` は `.hidden` と `aria-hidden` の切り替えのみ。10294-10297行目はEscapeで閉じるだけ。
- 内容: 開いた直後の初期フォーカス、Tab循環、閉じた後のトリガー復帰が実装されていない。
- ユーザー影響: キーボード操作や支援技術利用時に、設定操作後の位置が分からなくなる。
- 期待効果: 企業導入時のアクセシビリティ説明がしやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 開いたボタンを `_runtime._settingsTrigger` に保持し、閉じるボタンへ初期フォーカス、Tab循環、close時復帰を追加する。
- スコア: ユーザー被害4 / 購入理由への寄与3 / 実装しやすさ4 / 壊しにくさ4 / 保守性寄与4 = 19

#### ISS-008（更新）
- 分類: アクセシビリティ / UX
- タイトル: 検索・フィルタ・タブの選択状態が視覚表現に寄っている
- 種別: 事実
- 要約: 検索欄はplaceholder依存で、タブ/フィルタ/ステータスはクラス中心の状態表現になっている。読み上げで現在条件を把握しにくい。
- 証拠: `MustKnowCodingEnglish.html` 974-977行目のタブに `aria-current` / `aria-selected` なし。992行目の検索inputに `<label>` / `aria-label` なし。9871-9888行目のフィルタ生成に `aria-pressed` なし。9945-9948行目のステータスボタン選択も `.is-selected` のみ。
- 内容: 視覚的には状態が分かるが、支援技術では現在のビュー、絞り込み、ステータス選択が伝わりにくい。
- ユーザー影響: 意図しない条件で学習・テストを始める可能性がある。
- 期待効果: 状態が支援技術へ伝わり、制約のある現場で使える根拠が増える。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 検索に `aria-label`、タブに `aria-current`、フィルタ/ステータスに `aria-pressed` を付与する。
- スコア: ユーザー被害3 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与4 = 19

#### ISS-016（更新）
- 分類: アクセシビリティ / UX
- タイトル: 4択回答後の正誤フィードバックがライブ領域になっていない
- 種別: 事実
- 要約: 回答後に正誤文言は更新されるが、`aria-live` や `role="status"` がない。画面を見ない操作では結果が通知されにくい。
- 証拠: `MustKnowCodingEnglish.html` 1120行目の `<p class="feedback" id="quiz-feedback"></p>` にライブ属性なし。10002-10012行目でテキストを更新。`rg` で `aria-live` は検出なし。
- 内容: 4択テストの主要フィードバックが支援技術に伝わらない。
- ユーザー影響: スクリーンリーダー利用者が正誤を把握しにくく、次へ進む判断が遅れる。
- 期待効果: 小さな属性追加で回答体験のアクセシビリティを改善できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `#quiz-feedback` に `role="status"` または `aria-live="polite"` を付与する。
- スコア: ユーザー被害3 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与4 = 19

#### ISS-021
- 分類: UX / アクセシビリティ
- タイトル: 回答後も4択ボタンが押せる見た目のまま残る
- 種別: 事実
- 要約: 回答後、正解/不正解の色は付くが選択肢ボタンはdisabledにならない。再クリックしても `_answerQuiz()` が早期returnするだけで、操作不能になった理由が見えない。
- 証拠: `MustKnowCodingEnglish.html` 9743-9747行目の `_answerQuiz()` は既回答ならreturn。9987-9999行目の選択肢生成は回答後も `disabled` / `aria-disabled` を付けない。10017行目では次ボタンだけdisabled制御している。
- 内容: 視覚的には正誤が分かるが、ボタンとしては押せるように見えるため、再選択できるのか迷う。
- ユーザー影響: 回答変更できると誤認し、同じ選択肢や別選択肢を押しても反応しないことで操作不信につながる。
- 期待効果: 回答後に選択肢をdisabled化し、次の問題ボタンへ意識を移せる。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_vm.answer` がある時は選択肢ボタンへ `disabled` を付ける。必要なら正解/選択済みだけ `aria-label` に判定を含める。
- スコア: ユーザー被害2 / 購入理由への寄与2 / 実装しやすさ5 / 壊しにくさ5 / 保守性寄与3 = 17

### 4. 上位3件
- 1位: ISS-002 `localStorage` 読み書き失敗時に起動・操作が例外で止まる
  - 選定理由: ユーザー被害5。制約端末での初回表示と操作継続に直撃し、ローカル完結の購入理由を損なう。
  - 今やるべき理由: 修正範囲が `_loadState()`, `_saveState()`, `_renderFooter()` に限定でき、次回1回で完了しやすい。
  - 実装イメージ: `getItem` / `setItem` を `try/catch` 化し、失敗時もメモリ上の状態で継続する。保存不可状態を `_runtime._storageError` に保持する。
  - 想定UI: フッターに `保存状態: このブラウザでは保存できません。一時利用中です。` を表示。
  - 想定ユースケース: 社内PCでlocalStorageがブロックされているが、HTMLを開いて単語確認だけ行う。
  - リスク: 保存不可でも操作できるため、履歴が残らないことを見落とす可能性がある。フッター文言で「一時利用中」を明示する。
  - 着手順: `_runtime._storageError` 追加 -> `_loadState()` guard -> `_saveState()` guard -> フッター分岐 -> `getItem` / `setItem` 例外スタブ確認。
  - 受け入れ条件: `localStorage.getItem` または `setItem` が例外を投げても初期表示処理が例外終了しない。保存不可メッセージがフッターに出る。
- 2位: ISS-019 デフォルトのテストが800問になり初回体験が重すぎる
  - 選定理由: 購入理由への寄与5。初回3分で結果・復習・CSV価値に到達できるかを左右する。
  - 今やるべき理由: 800語収録の強みを見せる前に、800問完了が必要な体験になっている。標準問題数を切るだけなら実装が小さい。
  - 実装イメージ: 標準テストを10問に制限し、全問テストは後続の明示モードに分ける。少数フィルタ時は対象語数分だけ出題する。
  - 想定UI: テスト開始画面に `現在の条件: 全800語から10問`、`結果まで約3分`、`全問テストは設定から` のように表示。
  - 想定ユースケース: 初見ユーザーがHTMLを開き、10問だけ解いて結果画面で苦手語を確認する。
  - リスク: 既存READMEの「全問」説明と変わるため、README更新が必要。全問を望むユーザー向けには後続で全問モードを残す。
  - 着手順: 標準問題数定数を追加 -> `_buildQuizOrder()` の戻り値を標準問題数に制限 -> テスト画面文言更新 -> defaultで10問、Zで1問になることを確認。
  - 受け入れ条件: フィルタなしのテストが10問程度で結果画面へ到達できる。画面文言が出題数を明示する。
- 3位: ISS-001 「外部通信なし」とGoogle Fonts/CDN参照が矛盾している
  - 選定理由: 購入理由への寄与5。制約環境で使える価値とREADMEの信頼性に直結する。
  - 今やるべき理由: 外部通信の定義が曖昧なままだと、LP/README/有料版の訴求がぶれる。
  - 実装イメージ: Material SymbolsをインラインSVGまたはテキストに置換し、必要ならREADMEの文言を「学習データの外部送信なし」に変える。
  - 想定UI: ヘッダー左のコードアイコンはSVG、フォントはシステムフォント。オンライン/オフラインで表示差を小さくする。
  - 想定ユースケース: ネットワーク遮断された研修PCでHTMLを直接開く。
  - リスク: アイコンフォント置換で見た目が少し変わる。SVGサイズと色を既存CSS変数に合わせる。
  - 着手順: 外部参照洗い出し -> ヘッダーアイコン置換 -> README文言修正 -> ネットワークなし相当で表示確認。
  - 受け入れ条件: 本体HTMLに `fonts.googleapis.com` / `fonts.gstatic.com` 参照が残らないか、READMEの表現が「学習データの外部送信なし」に修正されている。
- 次点: ISS-003とISS-013。ISS-003は購入理由への寄与5、ISS-013は短時間復習の信頼性に直結するため、上位3件後に続けて着手する価値がある。

### 5. 追加機能アイデア
- 機能名: 10問標準テスト
  - 解決する課題: デフォルト800問で結果画面へ到達しにくい。
  - 概要: 標準は10問、対象語が10語未満なら対象語数分だけ出題する。
  - ユーザー価値: 初回3分で正答率、ミス、復習ワードを確認できる。
  - 差別化ポイント: 800語の量を維持しつつ、短時間トレーニングとして使える。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 出題数プレビュー
  - 解決する課題: テスト開始前に現在条件と問題数が分からない。
  - 概要: テスト開始画面に対象語数、実際の出題数、少数フィルタ時の補完方針を表示する。
  - ユーザー価値: 800問や意図しない全体出題を避けられる。
  - 差別化ポイント: 業務の隙間時間で使う前提を説明できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 最近のテスト履歴
  - 解決する課題: 最大30件保存している履歴を画面で見返せない。
  - 概要: 結果画面に直近5件の日時・正答率・問題数・ミス数を表示する。
  - ユーザー価値: 学習の積み上がりを確認できる。
  - 差別化ポイント: 単発クイズではなく継続学習ツールとして見せられる。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: 実務例文スターターパック
  - 解決する課題: 個別例文が全件空で教材価値が伝わりにくい。
  - 概要: 先頭30語にPRレビュー、エラー対応、仕様書、CLI操作の短文例を入れる。
  - ユーザー価値: 意味だけでなく使う場面が分かる。
  - 差別化ポイント: コーディング英語に特化した教材だと初回画面で伝わる。
  - 実装コスト: 中
  - 優先度: 高
- 機能名: セッションCSV
  - 解決する課題: テスト履歴や復習理由を持ち出せない。
  - 概要: 進捗CSVとは別に、テスト回ごとの回答履歴をCSV出力する。
  - ユーザー価値: 研修報告、復習計画、端末移行に使える。
  - 差別化ポイント: ローカル完結でも成果物を外部管理できる。
  - 実装コスト: 小
  - 優先度: 中

### 6. 訴求・販売改善案
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語PRレビューやエラー文に苦手意識がある開発者、外部サービスを使いにくい社内研修担当者。
- 解決している具体的な痛み: 開発現場で見る英単語だけを、辞書・Anki・スプレッドシートを往復せず短時間で確認したい。学習履歴を外部サービスに送らず、社内端末で配布したい。
- 今でも刺さる強み: 800語、レベル/ステータス/アルファベット検索、4択テスト、結果、優先復習、localStorage保存、CSV出力が1HTMLにまとまっている。
- 伝わっていない価値: 初回テストが800問で、結果分析まで到達しにくい。実務例文が空のため、開発文脈の教材であることが画面から伝わりにくい。
- 買う理由として弱い点: 完全オフライン説明と外部フォント参照の矛盾。CSVが履歴/Excel運用に弱い。READMEに無料版/有料版の差、対象ユーザー、3分体験がない。
- READMEに追加すべき説明: 対象ユーザー、標準テストの問題数、3分で試せる流れ、保存されるデータ、外部通信の正確な定義、CSVで出せる内容、現在の制限、配布時に開くファイル名。
- LPに追加すべき訴求: 「PRレビュー・エラー対応・仕様書で見る英語を1HTMLで復習」「10問で苦手語が分かる」「学習データを外へ送らない」「研修PCへ配りやすい」。
- スクリーンショットで見せるべき場面: `argument` の実務例文つきカード、10問テスト開始画面、回答後の正誤、結果画面の優先復習ワード、最近のテスト履歴、CSVをExcelで開いた場面。
- 無料版で見せるべき体験: 100語、10問ではなく5問テスト、数語の実務例文、直近結果、進捗CSV。良さは伝わるが本番研修には語数・履歴CSV・バックアップが不足する範囲。
- 有料版でしか得られない体験: 800語、実務例文パック、10問標準テスト、少数フィルタ対応、セッションCSV、最近のテスト履歴、バックアップ/復元、完全オフライン版、社内配布向けREADME。
- 制約のある現場で使える理由: 1HTMLで配布できる。進捗はブラウザ内に保存できる。完全オフラインを言うにはフォント/アイコン外部参照を外し、保存不可時も一時利用できる必要がある。
- 他手段より楽な理由: 単語検索、意味確認、4択テスト、復習候補、CSV出力が同じ画面内にあり、教材・辞書・表計算を切り替えなくて済む。
- 導入直後に感動できる体験があるか: 現状は800語の量は見えるが、800問テストと汎用例文により初回価値が分散する。10問で結果が出て、ミス語へ戻れる流れが入ると導入直後の判断材料が増える。
- 強み: 1ファイルSPA、800語、ローカル保存、フィルタ、4択、結果、復習、CSV。
- 弱み: 保存不可時の例外、外部通信説明の矛盾、デフォルト800問、個別例文なし、少数フィルタの全体フォールバック、CSV履歴不足、アプリ内ヘルプ不足。
- 使う理由: 外部サービスに学習データを出さず、開発文脈の頻出英語を短時間で復習するため。
- デモ版/有料版の切り分け案: デモ版は100語・5問・数語例文・直近結果まで。有料版は800語・実務例文・10問標準テスト・セッションCSV・履歴比較・バックアップ/復元・完全オフラインで「時短、安心、再利用、本番利用」を売る。

### 7. 次回おすすめタスク
- 1. `_loadState()` / `_saveState()` のlocalStorage例外処理と保存不可フッター表示を実装し、`getItem` / `setItem` 例外スタブで起動継続を確認する。
- 2. 標準テストを10問程度に制限し、テスト開始画面に対象語数と出題数を表示する。READMEの「全問」表現も挙動に合わせる。
- 3. 本体HTMLのGoogle Fonts / Material Symbols依存を外すか、READMEの「外部通信なし」を実態に合わせる。続けて優先30語の実務例文を追加する。

### 8. 実行ログ
- 今回アプリコードを変更していないこと: 変更していない。
- 今回READMEを変更していないこと: 変更していない。
- 今回REPORT.mdのみ更新したこと: リポジトリ内では `REPORT.md` のみ追記した。
- 確認コマンド: `sed`, `nl`, `rg`, `wc`, `git status`, Nodeによる `WORDS` 抽出、インラインJS構文確認、localStorage例外スタブ再現、テスト出題数の静的計算。
- 起動確認の結果: インラインJS構文確認は `SCRIPT_PARSE_OK scripts=1`。localStorage例外スタブは `GETITEM_THROW_REPRODUCED blocked getItem` と `SETITEM_THROW_REPRODUCED quota exceeded`。
- 起動確認の制約: Google Chrome 147.0.7727.56 は存在したが、ヘッドレス `--dump-dom` はcode -1で出力なし。実ブラウザのコンソールエラー完全確認は未完了として扱う。

## 2026-04-20
### 0. エグゼクティブサマリー
- 今回の結論: 前回上位課題の `localStorage` 例外、標準800問テスト、外部通信説明、個別例文不足はコード上まだ残っている。今回の追加監査では、学習状態リセット後も結果履歴が残る点と、設定の「キャンセル」がテーマ変更を取り消さない点を新規の実務リスクとして確認した。
- 最優先課題: `localStorage.getItem()` / `setItem()` の例外処理と保存不可表示。制約端末では初回表示・検索・回答・リセット後処理が止まり得るため、ローカル完結の前提に直結する。
- 次回の狙い: 保存不可でも一時利用できる状態にしたうえで、10問標準テストとデータ消去導線を入れ、初回3分体験と共有端末での安心を同時に上げる。
- 今回の監査では実装は行っていない: 行っていない。リポジトリ内で追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 学習ビュー、検索/フィルタ、テストビュー、結果ビュー、設定モーダル、フッター操作、README説明
- 主な利用フロー: 初回表示 -> 検索/フィルタ -> ステータス更新 -> テスト開始 -> 4択回答 -> 結果確認 -> 復習 -> CSV出力 -> 設定変更 -> リセット/履歴削除 -> 再利用
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語のPRレビューやエラー文に苦手意識がある開発者、外部サービスを使いにくい社内研修担当者
- ツールの主目的: 開発現場で頻出する英単語800語を、1HTML内で検索・確認・4択テスト・復習・CSV出力できるローカル学習ツールとして提供すること。
- 確認した観点: 既存Issue重複、保存/読込失敗、データ消去、テスト出題数、少数フィルタ、結果履歴、CSV出力、設定モーダル、アクセシビリティ、Web Design Standards、制約環境での運用適性。

### 2. 既存課題の確認結果
- 継続課題: ISS-002, ISS-019, ISS-001, ISS-003, ISS-013, ISS-005, ISS-006, ISS-007, ISS-008, ISS-009, ISS-015, ISS-016, ISS-017, ISS-020, ISS-021 は、今回の静的確認では未解決として扱う。
- 更新した Issue: ISS-002, ISS-019, ISS-013, ISS-009, ISS-020, ISS-006, ISS-003, ISS-007, ISS-016, ISS-021。
- 重複のため新規登録しなかった論点:
  - `localStorage` 読み取り失敗、書き込み失敗、検索入力ごとの保存失敗は、根本原因が保存境界の例外未処理なのでISS-002の更新に集約した。
  - フィルタなし800問、初回結果到達の遅さ、READMEの「全問」説明は、標準テスト設計の問題なのでISS-019の更新に集約した。
  - 4語未満フィルタで全体へ戻る問題は、ISS-013の追加証拠として扱い、新規化しなかった。
  - CSVのBOM不足はISS-015の継続課題として扱い、今回はCSV持ち出し価値の主課題であるISS-006を更新した。
  - Parts系ファイル混入のISS-012は、今回の `rg --files` では対象ディレクトリに確認できなかった。ただし `git status` 上は既存の削除差分があり、今回の監査では解決判断を保留した。

### 3. 発見した課題一覧

#### Issue: 20260420-01
- 状態: 更新
- 分類: バグ / UX
- タイトル: `localStorage` 読み書き失敗時に起動・操作が例外で止まる
- 種別: 事実
- 要約: `localStorage.getItem()` は `try/catch` の外で呼ばれ、`setItem()` も直接実行される。保存不可環境では初回表示、検索、回答、設定変更、CSV後処理、リセット後処理が止まり得る。
- 証拠: `MustKnowCodingEnglish.html` の `_loadState()` 9352行目で `localStorage.getItem(STORAGE_KEY)` を直接呼ぶ。`_saveState()` 9439-9442行目で `localStorage.setItem()` を直接呼ぶ。10300行目で起動直後に `_saveState()` を実行する。10278-10280行目の検索入力でも `_saveState()` と `_render()` を毎回呼ぶ。
- 内容: 管理端末、プライベートブラウズ、容量上限、ブラウザ設定で保存が使えない場合、学習カードの閲覧だけしたいユーザーも操作不能になる可能性がある。保存できないことをUIで説明する退避状態がない。
- ユーザー影響: 初回表示や入力中に止まり、ローカル完結ツールとしての信頼が下がる。
- 制約環境での影響: 社内端末や研修PCではストレージ制限があり得るため、「HTMLを配れば使える」という導入理由が崩れる。
- 期待効果: 保存不可でも一時利用でき、履歴が残らない理由をフッターで説明できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_loadState()` と `_saveState()` を `try/catch` 化し、`_runtime._storageError` を追加する。フッターに「保存不可。一時利用中」を表示し、`getItem` / `setItem` 例外スタブで初期表示継続を確認する。

#### Issue: 20260420-02
- 状態: 更新
- 分類: UX / 改善 / 訴求
- タイトル: 標準テストが800問になり初回3分体験に到達しにくい
- 種別: 事実
- 要約: フィルタなしでテストを開始すると、全800語が1セッションの出題対象になる。結果画面、優先復習、CSV価値まで短時間で到達しにくい。
- 証拠: `README.md` 10行目に「現在のフィルタ対象を全問」とある。`MustKnowCodingEnglish.html` 1109行目も「現在のフィルタを優先して全問を出題します」と表示する。`_buildQuizOrder()` 9586-9596行目は対象語の全IDを返す。Node抽出で `WORDS` は800件、フィルタなしの出題長は800件相当。
- 内容: 800語収録は販売上の強みだが、初回操作で800問完了が必要に見えると、結果分析や復習導線の価値を確認する前に離脱しやすい。
- ユーザー影響: 初見ユーザーが数問で中断し、直近結果や復習ワードを見ないまま評価する。
- 制約環境での影響: 研修や昼休みなど短時間利用では、全問前提のUIは使い始めの負荷になる。
- 期待効果: 10問標準なら3分以内に結果・ミス語・復習導線を確認できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `DEFAULT_QUIZ_SIZE = 10` を追加し、標準テストは `_buildQuizOrder()` の結果を10件に制限する。テスト開始画面に「対象800語から10問」のように実出題数を表示し、全問モードは後続の明示操作に分ける。

#### Issue: 20260420-03
- 状態: 新規
- 分類: セキュリティ / UX
- タイトル: 学習状態リセット後も結果履歴が残り共有端末で誤解や残存データが起きる
- 種別: 事実
- 要約: フッターの「学習状態リセット」は、ステータスと進行中テストを消すが、完了済みセッション履歴は残す。共有端末や教材デモ後に、利用者が全データ削除済みと誤解する可能性がある。
- 証拠: `MustKnowCodingEnglish.html` 1189行目のフッター操作は「学習状態リセット」。`_resetProgress()` 10147-10156行目は `_state.statuses`, `_state.quiz`, `_state.learning` を初期化するが `_state.sessions` は変更しない。`_getLatestSession()` 9579-9583行目と `_renderResults()` 10021-10080行目は残った最新セッションを表示する。設定内の1220-1221行目には別途「履歴の消去」がある。
- 内容: 設定内の文言は「ステータスとテスト進行」と補足があるが、フッターの主要導線では履歴が残ることが分からない。結果画面には前回の正答率・回答一覧・ミス語が残る。
- ユーザー影響: リセット後も過去のミスや実施日が見え、次の利用者に前利用者の学習履歴が見える可能性がある。
- 制約環境での影響: 共有PC、研修会場、貸与端末では、ローカル保存データの残存は導入時の不安になる。
- 期待効果: データ削除の範囲が明確になり、共有端末でも安心して使える。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: フッター文言を「進捗リセット（履歴は残る）」へ変更するか、設定内に「すべてのデータを消去」を追加して `statuses`, `quiz`, `learning`, `sessions` を同時に初期化する。次回は後者を小タスクとして実装し、結果画面が空になることを確認する。

#### Issue: 20260420-04
- 状態: 更新
- 分類: バグ / UX
- タイトル: フィルタ対象が4語未満のテストで全800語へ黙って戻る
- 種別: 事実
- 要約: Alphabet `Z` など1-3語の条件でテストを開始すると、出題語自体が全800語へ戻る。選択肢補完の都合が、出題対象のすり替わりとして現れている。
- 証拠: `_buildQuizOrder()` 9587-9589行目は `_filtered.length >= 4 ? _filtered : WORDS` を使う。Node抽出で4語未満の文字は `J:2`, `K:1`, `Q:2`, `X:1`, `Y:2`, `Z:1`。テスト画面1109行目は「現在のフィルタを優先」と説明している。
- 内容: ユーザーは「Zだけ」「苦手2語だけ」を確認するつもりで開始するが、実際には全体テストになる。誤答候補を全体から補うことと、出題語を全体へ戻すことは分けられる。
- ユーザー影響: ピンポイント復習の期待が外れ、短時間学習の信頼が下がる。
- 制約環境での影響: 業務の隙間時間に特定語だけ確認したい場面で、意図しない長いテストに入る。
- 期待効果: 少数フィルタでも狙った語だけ出題され、選択肢だけ全体から補完される。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 1-3語の場合も出題語リストは `_filtered` のまま返す。選択肢生成は既存どおり `WORDS` から誤答候補を取る。テスト開始画面に「対象1語、選択肢は全体から補完」と表示する。

#### Issue: 20260420-05
- 状態: 新規
- 分類: UX
- タイトル: 設定の「キャンセル」がテーマ変更を取り消さない
- 種別: 事実
- 要約: テーマのラジオ変更は即時保存されるが、設定フッターには「キャンセル」と「保存して閉じる」が並ぶ。キャンセルで変更が戻るように見えるが、実際は戻らない。
- 証拠: `MustKnowCodingEnglish.html` 1206-1213行目にテーマラジオがある。10283-10291行目のchangeイベントは `_setTheme($target.value)`, `_saveState()`, `_render()` を即実行する。1232行目の「キャンセル」と1233行目の「保存して閉じる」はどちらも `data-action="close-settings"` で、10178-10180行目では閉じるだけ。
- 内容: UI文言が「保存前のキャンセル」を示す一方、実装は即時反映・即時保存になっている。どちらの方針でもよいが、文言と挙動を一致させる必要がある。
- ユーザー影響: テーマを試しただけのつもりでキャンセルしても変更が残り、設定操作への信頼が下がる。
- 制約環境での影響: 共有端末や研修デモでは、前利用者のテーマ変更が残った理由を説明しにくい。
- 期待効果: 設定操作の結果が予測しやすくなり、誤操作後の混乱を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 最小実装として、設定フッターを「閉じる」1ボタンに統一し、テーマは選択時に保存される旨を短く表示する。取り消し機能を残す場合は別タスクで `_runtime._draftTheme` を導入する。

#### Issue: 20260420-06
- 状態: 更新
- 分類: セキュリティ / 技術負債 / バグ
- タイトル: 保存済みセッションの境界検証が浅く、不正日時で結果画面が例外化し得る
- 種別: 事実
- 要約: `sessions` は `localStorage` から復元されるが、日時・件数・回答値の検証が浅い。不正な `finishedAt` が入ると日付整形で例外になる。
- 証拠: `_loadState()` 9416-9418行目は `session && typeof session === "object"` のみで `sessions` を復元する。`_formatDateTime()` 9815-9826行目は `new Date(dateInput)` を `Intl.DateTimeFormat().format()` に渡す。Node確認で `new Intl.DateTimeFormat("ja-JP").format(new Date("not-a-date"))` は `RangeError: Invalid time value` を投げた。`_renderResults()` 10044-10056行目は復元値を `innerHTML` に流す。
- 内容: 通常操作では内部生成値なので頻度は高くない。ただし手動編集、別版からの移行、破損localStorage、将来のインポート機能で壊れた値が入ると、結果画面が表示不能になる可能性がある。
- ユーザー影響: 過去結果を見ようとした時に画面が壊れ、学習履歴への信頼が下がる。
- 制約環境での影響: 端末移行やバックアップ復元が必要な現場で、壊れた保存データから回復しにくい。
- 期待効果: 復元境界を固めることで、将来のバックアップ/復元機能も安全に追加しやすくなる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: `sessions` 復元時に `finishedAt` が有効日時か、`total` / `correct` が安全な数値か、`answers[].wordId` が存在語かを検証する。`_formatDateTime()` は不正日時なら `"-"` を返す。

#### Issue: 20260420-07
- 状態: 更新
- 分類: 追加機能 / UX / 訴求
- タイトル: 最大30件のテスト履歴を保存しているが過去回を見返せない
- 種別: 事実
- 要約: 完了セッションは最大30件まで保存されるが、画面表示は最新1件だけである。履歴を消す導線はあるのに、履歴を見る導線がない。
- 証拠: `_finishQuiz()` 9770-9778行目で `sessions` に追加し30件へ制限する。`_getLatestSession()` 9579-9583行目は最新1件だけ返す。`_renderResults()` 10021-10080行目は最新セッションのみ表示する。設定1221行目には「履歴の消去（結果セッションのみ）」がある。
- 内容: 内部には過去回があるため、直近5件の正答率・問題数・ミス数を出すだけで学習の積み上がりを見せられる。
- ユーザー影響: 成長や研修成果を画面で確認できず、継続利用の動機が弱くなる。
- 制約環境での影響: 外部学習管理サービスを使えない現場では、ローカル画面内で履歴比較できることが価値になる。
- 期待効果: 結果画面だけで進捗の変化を説明でき、有料版の継続学習価値が伝わる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 結果画面に「最近のテスト」一覧を追加し、直近5件の日時、正答率、問題数、ミス数を表示する。詳細展開は後回しにする。

#### Issue: 20260420-08
- 状態: 更新
- 分類: 追加機能 / 訴求
- タイトル: CSV出力が学習ステータスだけでテスト履歴を持ち出せない
- 種別: 事実
- 要約: セッション履歴は内部保存されるが、CSVは単語ステータスだけを出す。研修報告や復習計画に必要な「いつ、どの語を間違えたか」が外へ出せない。
- 証拠: `_finishQuiz()` 9770-9775行目で `finishedAt`, `total`, `correct`, `answers` を保存する。`_exportCsv()` 10123-10130行目のヘッダーは `word,status,level,meaning` だけで、セッション情報を含まない。
- 内容: 進捗CSVはバックアップとして一部役立つが、結果分析や研修報告には不足している。既存のISS-015でBOM不足も残っているため、CSVを実務で使うには出力種類とExcel運用の両方を整える必要がある。
- ユーザー影響: ブラウザ外での復習計画、チーム研修、端末移行に履歴を再利用しにくい。
- 制約環境での影響: 外部クラウドへ学習履歴を送れない現場ほど、CSVで持ち出せることが重要になる。
- 期待効果: ローカル完結でも成果物をファイルで共有でき、導入担当者へ説明しやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 既存の進捗CSVとは別に、`sessions.csv` を追加する。列は `finishedAt,word,correct,selected,isCorrect,status` とし、BOM付与はISS-015の小修正として同時に確認する。

#### Issue: 20260420-09
- 状態: 更新
- 分類: 追加機能 / 訴求
- タイトル: 個別例文と補足意味が全件空で、開発文脈の教材価値が伝わりにくい
- 種別: 事実
- 要約: 800語すべてで `exampleEn`, `exampleJa`, `meaningSub` が空で、画面はテンプレート例文に依存している。さらに同じ日本語意味の語が複数あり、使い分けの補足がない。
- 証拠: Node抽出で `WORDS.total: 800`, `emptyExampleEn: 800`, `emptyExampleJa: 800`, `emptyMeaningSub: 800` を確認した。`_getExampleEn()` 9505-9517行目、`_getExampleJa()` 9519-9530行目は空値時テンプレートを返す。重複する `meaningMain` は13種類あり、例として `delete/remove`, `perform/execute/run`, `select/choose`, `append/add`, `invoke/call` が同じ日本語意味になっている。
- 内容: 単語と意味は確認できるが、PRレビュー、エラー対応、仕様書、CLI操作でどう使うかが画面から分かりにくい。同義語に近い語の使い分けも補足されない。
- ユーザー影響: 無料の単語表や汎用単語帳との差が伝わりにくく、購入判断や継続利用の理由が弱くなる。
- 制約環境での影響: READMEや外部説明を読めない配布環境では、画面内例文が教材価値の主な証拠になる。
- 期待効果: 優先30語だけでも実務例文と使い分け補足を入れると、導入直後に「開発現場向け」と判断しやすい。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 先頭30語または販売デモ用30語に `exampleEn` / `exampleJa` を追加する。重複意味ペアは `meaningSub` に「deleteは意図的な削除、removeは取り除く操作」など1文補足を入れる。

#### Issue: 20260420-10
- 状態: 更新
- 分類: アクセシビリティ / UX
- タイトル: 設定モーダルに初期フォーカス・トラップ・復帰処理がない
- 種別: 事実
- 要約: モーダルは `role="dialog"` と `aria-modal="true"` を持つが、開閉時のフォーカス管理が実装されていない。
- 証拠: `settings-modal` は1193行目で `role="dialog" aria-modal="true"` を持つ。`_openSettings()` 9839-9842行目は `.hidden` と `aria-hidden` の切り替えだけを行う。10294-10297行目はEscapeで閉じるだけで、Tab循環やトリガー復帰はない。Web Design Standardsのアクセシビリティ基準では、dialogは初期フォーカス、フォーカストラップ、復帰が必要。
- 内容: キーボード利用者はモーダル外へ移動でき、閉じた後に元の「設定」ボタンへ戻る保証がない。
- ユーザー影響: 設定変更後に現在位置を失い、キーボード操作の負担が増える。
- 制約環境での影響: 企業導入や教育現場でアクセシビリティ要件を確認された時に説明しにくい。
- 期待効果: 1HTMLツールでも基本的なdialog品質を満たせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_runtime._settingsTrigger` に開いたボタンを保持し、開いたら閉じるボタンへフォーカスする。Tab循環を追加し、閉じたらトリガーへ戻す。

#### Issue: 20260420-11
- 状態: 更新
- 分類: アクセシビリティ / UX
- タイトル: 4択回答後の結果が支援技術に通知されず、選択肢も押せる見た目で残る
- 種別: 事実
- 要約: 回答後に正誤テキストは更新されるがライブ領域ではない。選択肢ボタンもdisabledにならず、再押下できる見た目のまま残る。
- 証拠: `quiz-feedback` は1120行目で `<p class="feedback" id="quiz-feedback"></p>` だが `role="status"` / `aria-live` がない。`_renderTest()` 10002-10012行目で正誤文言を更新する。`_answerQuiz()` 9743-9747行目は既回答ならreturnするだけ。選択肢生成9987-9999行目は回答後も `disabled` / `aria-disabled` を付けない。
- 内容: 画面を見ているユーザーには色と文言で伝わるが、スクリーンリーダー利用者には回答結果が通知されにくい。見た目上はボタンが押せるため、回答変更できるか迷う。
- ユーザー影響: 回答後の次操作が分かりにくく、テスト体験の信頼が下がる。
- 制約環境での影響: キーボード中心の研修端末や支援技術利用環境で、テストが使いにくい。
- 期待効果: 正誤が即時に伝わり、回答後は次の問題へ進むことが明確になる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `#quiz-feedback` に `role="status"` または `aria-live="polite"` を付与する。`_vm.answer` がある時は選択肢ボタンに `disabled` を付け、正解/選択済みの `aria-label` へ判定を含める。

### 4. 追加機能アイデア
- 機能名: 保存不可モード
  - 解決する課題: `localStorage` が使えない制約端末で起動や操作が止まる。
  - 概要: 保存失敗を捕捉し、メモリ上の一時利用へ切り替え、フッターに保存不可理由を表示する。
  - ユーザー価値: 社内PCでも単語確認だけは継続できる。
  - 制約環境での実現性: 外部依存なし、既存の `_loadState()` / `_saveState()` 周辺だけで実装可能。
  - 差別化ポイント: ローカル完結ツールとして、保存できない環境でも壊れず説明できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 10問標準テストと出題数プレビュー
  - 解決する課題: 初回テストが800問で結果画面へ到達しにくい。
  - 概要: 標準は10問にし、開始前に対象語数と実出題数を表示する。
  - ユーザー価値: 3分以内に正答率、ミス語、復習導線を体験できる。
  - 制約環境での実現性: 既存の出題順生成後に件数を制限するだけで成立する。
  - 差別化ポイント: 800語の量と短時間学習を両立できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: すべてのデータを消去
  - 解決する課題: 学習状態リセット後も結果履歴が残る。
  - 概要: ステータス、進行中テスト、学習位置、結果履歴をまとめて消す明示ボタンを設定に追加する。
  - ユーザー価値: 共有端末やデモ後に前利用者の履歴を残さず使える。
  - 制約環境での実現性: localStorage内の既存状態を初期化するだけで、外部依存は不要。
  - 差別化ポイント: ローカル保存の安心と削除可能性を同時に説明できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 最近のテスト履歴とセッションCSV
  - 解決する課題: 保存済み履歴を画面でもファイルでも再利用しにくい。
  - 概要: 結果画面に直近5件の一覧を表示し、別CSVで回答履歴を出力する。
  - ユーザー価値: 学習の伸び、研修報告、復習計画に使える。
  - 制約環境での実現性: 既存 `sessions` 配列を表示・CSV化するだけで実現できる。
  - 差別化ポイント: 外部サービスなしで学習成果を持ち出せる。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: 実務例文スターターパック
  - 解決する課題: 個別例文がなく開発文脈の教材価値が伝わりにくい。
  - 概要: 優先30語にPR、エラー、仕様書、CLI操作の短い英語例文と日本語訳を入れる。
  - ユーザー価値: 意味だけでなく使う場面まで理解できる。
  - 制約環境での実現性: 静的データ追加だけで、ネットワークやビルドは不要。
  - 差別化ポイント: 汎用英単語帳ではなくコーディング英語教材として見せられる。
  - 実装コスト: 中
  - 優先度: 中
- 機能名: 進捗JSONバックアップ/復元
  - 解決する課題: localStorage削除やブラウザ変更で進捗が失われる。
  - 概要: 状態JSONをエクスポートし、復元時は型検証してから反映する。
  - ユーザー価値: 端末移行や研修PC入れ替えでも履歴を持ち運べる。
  - 制約環境での実現性: ファイル入出力だけで完結し、クラウド不要。
  - 差別化ポイント: ローカル完結を保ちながらデータ可搬性を出せる。
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `_loadState()` / `_saveState()` の例外処理と保存不可フッター表示を実装し、`getItem` / `setItem` 例外スタブで初期表示と検索入力が止まらないことを確認する。
- 2. 標準テストを10問に制限し、開始画面に対象語数・出題数を表示する。同時に1-3語フィルタでは出題語をフィルタ結果に維持する。
- 3. 設定に「すべてのデータを消去」を追加し、ステータス・進行中テスト・結果履歴がまとめて消えることを確認する。フッターのリセット文言も履歴が残る場合は明示する。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでのコンソールエラー完全確認。Google Chromeのヘッドレス `--dump-dom` は今回もcode -1で出力がなく、実ブラウザ確認は未完了として扱う。
- 仮説のまま残った点: Excelダブルクリック時のCSV文字化けは、今回Excel実機で再現していないためISS-015の確信度は中のまま。外部フォント遮断時の見た目崩れも実ネットワーク遮断では未確認。
- 次回追加で見るべき箇所: 破損 `localStorage` からの復旧、リセット後の結果画面、テーマ変更の保存タイミング、出題数プレビュー文言、CSVをExcel/Numbersで開いた結果。
- 実行ログ: `cat`, `ls`, `rg --files`, `rg`, `nl`, `wc`, `git status`, NodeによるインラインJS構文確認、Nodeによる `WORDS` 抽出、Nodeによる不正Date検証、スタブDOMでの初期実行確認を行った。
- 起動確認の結果: スタブDOM環境では `STUB_OPEN_OK`、初期単語 `accept`、表示件数 `800語を表示中 / 全800語`、保存状態表示の更新を確認した。実ブラウザのコンソールエラー完全確認は未完了。
- 今回変更したファイル: `REPORT.md` のみ。`MustKnowCodingEnglish.html`, `README.md`, 設定ファイル、画像、テスト、ビルド設定は変更していない。

## 2026-04-21
### 0. エグゼクティブサマリー
- 今回の結論: 前回までの上位課題のうち、保存不可モード、標準10問、少数フィルタ、全データ削除、履歴CSV、最近のテスト、モーダルフォーカス、回答後のライブ通知は現行HTMLで実装済みだった。一方で、検索0件時の全体出題、同じ10語の反復、破損保存データの警告消失、表示内容と検索対象の不一致が残っている。
- 最優先課題: 検索/フィルタ0件時にテスト開始すると全800語から10問へ切り替わる挙動を止める。
- 次回の狙い: テスト出題範囲の信頼性と、保存データ異常時の説明を先に固める。
- 今回の監査では実装は行っていない: 行っていない。リポジトリ内で追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 学習ビュー、検索/フィルタ、テスト開始画面、テスト実行画面、結果ビュー、最近のテスト、CSV出力、設定モーダル、フッター、README説明
- 主な利用フロー: 初回表示 -> 検索/フィルタ -> テスト開始 -> 4択回答 -> 結果確認 -> 最近の履歴確認 -> CSV出力 -> 設定でデータ管理 -> 再利用
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語のエラー文やPRレビューに慣れていない開発者、外部サービスを使いにくい社内研修担当者
- ツールの主目的: 開発現場で頻出する英単語800語を、1HTML内で検索・学習・4択テスト・結果確認・CSV出力できるローカル学習ツールとして提供すること。
- 確認した観点: 既存Issue重複、検索0件、出題順、保存データ復元、CSV導線、履歴の説明力、画面内ヘルプ、教材データの充実度、CSSルール、アクセシビリティ、制約環境での運用適性。

### 2. 既存課題の確認結果
- 継続課題:
  - 「新しいテストを繰り返しても同じ先頭10語になりやすい」は、`randomCursor` がテスト完了時に進まないため継続。
  - 「個別例文と補足意味が全件空」は、30件は投入済みになったが、770件は空のため更新として継続。
  - 「フッターのヘルプ / 設定がヘルプ内容を表示しない」は、設定モーダル内にバージョン情報しかないため継続。
  - 「CSSルールと現行スタイルに小さな不一致がある」は、ライトテーマのHEXと `letter-spacing: -0.31px` が残るため継続。
- 更新した Issue:
  - 標準10問化の副作用として、出題範囲の0件フォールバックと同じ10語反復を新しい証拠で更新した。
  - CSV履歴出力は実装済みだが、1ボタンで2ファイル連続ダウンロードする導線を新しい論点として分離した。
  - 保存不可モードは実装済みだが、破損JSON復元時の警告が起動直後の保存で消える論点を新規登録した。
- 重複のため新規登録しなかった論点:
  - Google Fonts/CDN依存は現行HTMLで外部参照が確認できなかったため、新規登録しなかった。
  - `localStorage` 読み書き失敗の未捕捉は `_loadState()` / `_saveState()` の `try/catch` 実装を確認したため、新規登録しなかった。
  - フィルタ1-3語で全800語へ戻る問題は `_getQuizPlan()` が1件以上ならフィルタ結果を使うため、新規登録しなかった。
  - 結果履歴の全削除、最近のテスト、履歴CSV、CSV BOM、設定キャンセル文言、モーダルフォーカス、回答後ライブ通知、回答ボタンdisabledは実装済みとして扱い、同じ内容では登録しなかった。

### 3. 発見した課題一覧

#### Issue: 20260421-01
- 状態: 新規
- 分類: バグ / UX
- タイトル: 検索やフィルタが0件でもテスト開始時に全体10問へ切り替わる
- 種別: 事実
- 要約: 検索結果が0件の状態でも、テスト開始画面は全800語から10問を出題できる。ユーザーが「該当なし」を確認した後に開始すると、意図しない範囲のテストになる。
- 証拠: `MustKnowCodingEnglish.html` の `_getQuizPlan()` 9757-9766行目は `_filtered.length > 0 ? _filtered : WORDS` を使う。`_renderLearning()` 10129-10138行目は0件時に「該当する単語がありません」と表示するが、`_renderTest()` 10209-10213行目は「全体語彙から出題します」と表示して開始ボタンを残す。Node確認では検索 `__no_such_word__` 時に `filteredCount: 0`, `sourceCount: 800`, `quizSize: 10`, 先頭 `accept` から出題相当になった。
- 内容: 条件0件のときは、ユーザーの意図は「条件を変える」または「出題不可を理解する」であり、「全体語彙で開始」ではない可能性が高い。少数フィルタ補完の解決とは別問題で、0件時だけ出題対象が全体にすり替わる。
- ユーザー影響: 特定語を探して見つからなかった後に、無関係な10問を解くことになり、検索/フィルタへの信頼が下がる。
- 制約環境での影響: 研修中や短時間学習では、意図しない出題に入ると時間を戻しにくい。
- 期待効果: 条件0件時は開始不可にでき、テスト範囲の予測可能性が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_getQuizPlan()` に「フィルタまたは検索が有効で0件」の判定を追加し、`quizSize: 0` と空の `source` を返す。`_renderTest()` では開始ボタンをdisabledにし、「条件に一致する単語がありません。検索条件を変更してください」と表示する。

#### Issue: 20260421-02
- 状態: 更新
- 分類: UX / 改善
- タイトル: 新しいテストを繰り返しても同じ先頭10語になりやすい
- 種別: 事実
- 要約: 標準10問化は完了しているが、テスト開始時に出題カーソルが進まない。フィルタなしで繰り返すと、`accept` から始まる同じ10語を再度出題しやすい。
- 証拠: `DEFAULT_QUIZ_SIZE = 10` は9352行目。`_buildQuizOrder()` 9777-9779行目は `_state.learning.randomCursor` を起点に10件を返す。`randomCursor` を増やす箇所は `_jumpRandomWord()` 10091行目だけで、`_startQuiz()` 9782-9801行目と `_finishQuiz()` 9957-9983行目では増えない。Node確認でも初期状態の先頭10語は `accept, access, account, algorithm, allow, alternative, application, apply, argument, array` だった。
- 内容: 10問標準は初回体験を軽くする一方、出題カーソルを進めないと800語の広さに届きにくい。出題順を毎回ランダムにする必要はないが、「次の10問」へ進む仕組みは必要。
- ユーザー影響: 毎回同じ語を解いている印象になり、800語収録の価値が伝わりにくい。
- 制約環境での影響: 短時間で継続利用する現場では、前回の続きに進めないと学習範囲の管理を手作業で行う必要がある。
- 期待効果: 繰り返し利用で自然に出題範囲が進み、フィルタなしでも800語全体を少しずつ確認できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_startQuiz()` で出題順を確定した直後、または `_finishQuiz()` で完了時に `_state.learning.randomCursor += _state.quiz.order.length` 相当を反映する。UI文言は「現在の条件から次の10問」に変更し、リセット時だけ0へ戻す。

#### Issue: 20260421-03
- 状態: 新規
- 分類: バグ / UX / 技術負債
- タイトル: 破損した保存データの警告が起動直後の自動保存で消える
- 種別: 事実
- 要約: `localStorage` のJSONが壊れている場合、読み込み時には警告文を設定するが、起動末尾の `_saveState()` が初期状態を保存して警告を消す。ユーザーには「ローカル保存済み」と表示され、初期化されたことが伝わらない。
- 証拠: `_loadState()` 9598-9600行目は破損JSONで `_runtime._storageError = "保存データが壊れていたため初期化しました..."` を設定する。`_saveState()` 9604-9608行目は保存成功時に `_runtime._storageError = ""` とする。起動末尾10761行目で `_saveState()` を必ず実行する。スタブDOM確認では壊れたJSON入力後のフッターが `保存状態: ローカル保存済み` となり、初期状態260 bytesが保存された。
- 内容: 壊れた保存データを初期化すること自体は必要だが、通知が消えるため、ユーザーは進捗や履歴が消えた理由を把握できない。復旧やバックアップ判断の機会もない。
- ユーザー影響: 保存済み進捗が突然消えたように見え、ローカル保存への信頼が下がる。
- 制約環境での影響: 端末移行、ブラウザ設定変更、容量整理後など、保存データが壊れた可能性を利用者自身が説明しにくい。
- 期待効果: 初期化理由が1回は明示され、必要なら破損値の退避や再開判断ができる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_runtime._storageError` と別に `_runtime._loadWarning` を追加し、保存成功では消さない。破損時は起動直後の自動保存を避けるか、`STORAGE_KEY + "-broken-backup"` に生文字列を退避してから初期化する。

#### Issue: 20260421-04
- 状態: 新規
- 分類: UX / バグ
- タイトル: 画面に表示される補足意味や自動例文が検索対象に入らない
- 種別: 事実
- 要約: 画面では略語や頭字語に補足文と自動例文が表示されるが、検索処理は元データの空欄だけを参照する。表示されている語句で検索しても該当しない。
- 証拠: `_getMeaningSub()` 9653-9674行目は空欄時に「略語表現」「頭字語」を返す。`_getExampleEn()` 9676-9688行目と `_getExampleJa()` 9690-9702行目も空欄時に自動文を返す。`_renderLearning()` 10173-10176行目はこれらの戻り値を表示する。一方、`_getFilteredWords()` 9727行目の検索プールは `word.meaningSub`, `word.exampleEn`, `word.exampleJa` の生値だけで、生成後の表示文を含まない。Node確認では「頭字語」検索と「略語表現」検索がどちらも0件だった。
- 内容: 検索は画面に見えている内容と一致する必要がある。特に略語70語、頭字語30語は補足ラベルが教材上の分類理解に関わるため、検索対象外だと初見ユーザーが探しにくい。
- ユーザー影響: 画面で見た言葉を検索しても見つからず、検索機能が不完全に見える。
- 制約環境での影響: READMEや外部説明を開けない環境では、画面内検索だけで内容を探すため不一致が目立つ。
- 期待効果: 表示内容と検索結果が一致し、略語/頭字語の探索がしやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_getFilteredWords()` の検索プールを `[word.term, word.meaningMain, _getMeaningSub(word), _getExampleEn(word), _getExampleJa(word)]` に変更する。性能確認として800語検索で入力遅延が出ないか確認する。

#### Issue: 20260421-05
- 状態: 新規
- 分類: UX / 改善
- タイトル: CSV出力が1クリックで2ファイルを連続ダウンロードする
- 種別: 推論
- 要約: フッターの「CSV出力」は1つの操作に見えるが、進捗CSVと履歴CSVを連続で保存する。履歴がない初回でもヘッダーだけの履歴CSVが出る。
- 証拠: フッター1266行目のボタン文言は `CSV出力`。`_exportCsv()` 10502-10505行目は `_exportProgressCsv()` と `_exportSessionsCsv()` を連続実行する。`_exportSessionsCsv()` 10489-10499行目はセッション0件でもヘッダー行だけで `_downloadCsv("MustKnowCodingEnglish-sessions.csv", _rows)` を呼ぶ。README 38-41行目には2種類のCSVが記載されているが、画面ボタンは1つ。
- 内容: ブラウザや管理端末の設定によっては、1クリックで複数ファイルのダウンロードが警戒される可能性がある。ユーザーも1ファイルだけを想定しやすい。
- ユーザー影響: ダウンロード結果が予測しにくく、履歴なしの空CSVを誤って成果物として扱う可能性がある。
- 制約環境での影響: 会社PCでは複数ダウンロードがブロック、確認、保存先固定の対象になりやすい。
- 期待効果: 出力対象を選べるようになり、ファイル運用の混乱が減る。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: フッターまたは設定に「進捗CSV」「履歴CSV」の2ボタンを分ける。履歴0件なら履歴CSVボタンをdisabledにし、「テスト完了後に出力できます」と表示する。

#### Issue: 20260421-06
- 状態: 新規
- 分類: UX / 訴求
- タイトル: 自動例文の引用符が二重になり表示品質が下がる
- 種別: 事実
- 要約: 自動生成される英語例文には語を囲む引用符が含まれるが、表示時にさらに全体を引用符で囲んでいる。略語や頭字語で不自然な英語表示になる。
- 証拠: `_getExampleEn()` 9676-9688行目は `Use "param"...` や `"API" is used...` を返す。`_renderLearning()` 10175行目は `_dom.$wordExampleEn.textContent = "\"${_getExampleEn(_currentWord)}\""` と全体を再度引用符で囲む。Node確認では `API` が `""API" is used as a standard technical acronym in documentation."`、`param` が `"Use "param" as a shorthand in code comments when context is clear."` 相当になった。
- 内容: 単語学習の中心である例文欄に不自然な記号が出る。機能不具合ではないが、有料教材や配布物としての品質判断に直結する。
- ユーザー影響: 例文が機械生成のままに見え、教材としての信頼が下がる。
- 制約環境での影響: 画面だけで教材価値を判断する配布環境では、例文欄の表記ミスが目立つ。
- 期待効果: 例文欄が自然な英文になり、学習カードの品質が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 表示側の外側引用符を削除し、必要ならCSSで例文らしさを表現する。既存30件と自動生成例文の両方で引用符が重複しないことを確認する。

#### Issue: 20260421-07
- 状態: 更新
- 分類: 追加機能 / 訴求
- タイトル: 個別例文と補足意味が770語で未投入のまま残っている
- 種別: 事実
- 要約: 前回の「全件空」状態から30語は改善されているが、800語中770語は `exampleEn`, `exampleJa`, `meaningSub` が空である。画面は自動補完文に依存している。
- 証拠: Node抽出で `total: 800`, `withExampleEn: 30`, `withExampleJa: 30`, `withMeaningSub: 30`, `emptyExampleEn: 770`, `emptyExampleJa: 770`, `emptyMeaningSub: 770` を確認した。`_getExampleEn()` 9676-9688行目と `_getExampleJa()` 9690-9702行目は空欄時テンプレートを返す。重複意味は `perform/execute/run`, `delete/remove`, `select/choose`, `append/add`, `invoke/call` などが残る。
- 内容: 30語の投入でデモ価値は上がったが、検索や通常学習では多くの語が汎用文になる。使い分けが必要な類義語ほど、補足文がないと開発文脈の教材価値が伝わりにくい。
- ユーザー影響: 無料の単語表との差が限定的に見え、継続利用や購入判断の根拠が弱くなる。
- 制約環境での影響: 外部辞書や翻訳サイトを併用できない現場では、画面内の具体例不足がそのまま学習効率に影響する。
- 期待効果: 頻出語から順に実務例文と使い分け補足を入れることで、1HTML単体の教材価値が上がる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 次回は全770語ではなく、重複意味グループから15語、初学者が迷いやすい略語/頭字語から15語を選び、合計30語へ `exampleEn`, `exampleJa`, `meaningSub` を追加する。

#### Issue: 20260421-08
- 状態: 新規
- 分類: 追加機能 / UX
- タイトル: テスト履歴に出題範囲やフィルタ条件が残らない
- 種別: 事実
- 要約: 最近のテストは日時、正答率、ミス数を表示するが、その回がどの条件で実施されたかは保存されない。フィルタ学習を続けると履歴の意味を読み取りにくい。
- 証拠: `_finishQuiz()` 9965-9970行目で保存するセッションは `finishedAt`, `total`, `correct`, `answers` のみ。`_renderRecentSessions()` 10279-10309行目は日時、正答率、ミス数だけを表示する。`_getQuizPlan()` 9757-9767行目にはフィルタ後の件数があるが、セッションへ保存していない。
- 内容: 最近のテスト機能は実装済みだが、履歴比較に必要な「Basicだけ」「Zだけ」「苦手だけ」などの範囲が残らない。直近5件が並んでも、点数差の理由を判断しにくい。
- ユーザー影響: 学習の進み具合を後から説明しにくく、研修報告や復習計画に使いにくい。
- 制約環境での影響: 外部LMSを使えない現場では、ローカル画面内の履歴情報が成果確認の主資料になる。
- 期待効果: 履歴だけで何を解いたか分かり、フィルタ別の復習に戻りやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_finishQuiz()` で `scopeLabel` または `filtersSnapshot` をセッションに保存する。最近のテストには「全800語から10問」「Basic 10問」「Z 1問」のような短い表示を追加する。

#### Issue: 20260421-09
- 状態: 更新
- 分類: UX / 訴求
- タイトル: 「ヘルプ / 設定」を開いても使い方とデータ仕様が画面内にない
- 種別: 事実
- 要約: フッターは「ヘルプ / 設定」と表示しているが、モーダル内は外観、データ管理、バージョン情報だけである。初見ユーザー向けの操作手順やCSV内容の説明が画面内にない。
- 証拠: フッター1267行目は `ヘルプ / 設定`。設定モーダル1272-1316行目にはテーマ、データ管理、`Version 1.0.0 SPA`、`Parts1-Parts4を統合したローカル完結版です。` があるが、「3分で試す手順」「保存されるデータ」「CSVが2種類出ること」「履歴削除と進捗リセットの違い」はない。README 43-47行目には使い方があるため、画面内だけが不足している。
- 内容: READMEを読んでいない利用者や、HTMLだけ受け取った利用者は、最短の使い方とデータ管理の違いを画面から把握しにくい。
- ユーザー影響: 初回操作の迷い、CSV出力の誤解、リセット範囲の取り違えが起きやすい。
- 制約環境での影響: READMEを配布しない、またはブラウザだけで使わせる研修環境では、画面内ヘルプが唯一の案内になる。
- 期待効果: HTML単体配布でも、操作開始とデータ管理の説明が完結する。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 設定モーダルに「3分で試す: 1. テスト開始 2. 結果確認 3. 復習」「保存: localStorage」「CSV: 進捗と履歴の2種類」「削除: 進捗/履歴/全データの違い」を各1行で追加する。

#### Issue: 20260421-10
- 状態: 新規
- 分類: アクセシビリティ / UX
- タイトル: 破壊的操作がすべてブラウザ標準confirmに依存している
- 種別: 事実
- 要約: 進行中テスト破棄、進捗リセット、履歴消去、全データ消去は `window.confirm()` で確認している。既存モーダルのフォーカス管理が整った一方、重要な確認だけブラウザ依存の体験になっている。
- 証拠: `_startQuiz()` 9783-9788行目、`_resetProgress()` 10533-10537行目、`_clearHistory()` 10552-10556行目、`_clearAllData()` 10564-10568行目で `window.confirm()` を使う。Web Design StandardsのAccessibilityはdialogの初期フォーカス、トラップ、復帰を求め、Baseline UIは破壊的操作にAlertDialogを求めている。現行の `settings-modal` は `_openSettings()` 10044-10070行目とkeydown 10724-10758行目でフォーカス管理を持つ。
- 内容: `confirm` は実装コストが低いが、文面の構造化、削除対象の明示、フォーカス復帰、ボタンラベルの統一をアプリ側で制御しにくい。全データ消去のような操作は、専用の確認UIの方が誤操作を減らせる。
- ユーザー影響: 削除対象を読み違えたり、ブラウザごとの文言/表示差で操作判断がしにくくなる。
- 制約環境での影響: 管理ブラウザや研修端末では標準ダイアログの見た目や抑止挙動が環境依存になりやすい。
- 期待効果: 重要操作の確認が画面内で一貫し、削除範囲を明示できる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 既存設定モーダルの仕組みを流用し、`_runtime._pendingConfirmAction` を持つ小さな確認ダイアログを追加する。最初は「すべてのデータを消去」だけ対象にし、問題なければ他操作へ広げる。

#### Issue: 20260421-11
- 状態: 更新
- 分類: 技術負債 / アクセシビリティ
- タイトル: ライトテーマのHEX色と負の文字間がルールに残っている
- 種別: 事実
- 要約: CSS layersは使われているが、ライトテーマの一部色がHEX定義のままで、本文に負のletter-spacingが設定されている。リポジトリルールとWeb Design StandardsのTypography方針に合わない。
- 証拠: `MustKnowCodingEnglish.html` 41-47行目で `--bg`, `--surface`, `--text`, `--line` などがHEX。77行目で `letter-spacing: -0.31px`。README 28-31行目も `-0.31px` を方針として記載している。AGENTS.mdは色定義に `oklch` を求め、Web Design Standardsはletter-spacing変更を避ける方針。
- 内容: 見た目の即時バグではないが、今後のテーマ調整やコントラスト確認で基準が分かれやすい。負の文字間は日本語と英単語が混在する教材で読みやすさに影響する可能性がある。
- ユーザー影響: 長時間学習時の可読性が下がる可能性があり、デザイン監査時にルール逸脱として扱われる。
- 制約環境での影響: 社内配布前のチェックリストでCSSルールを機械確認する場合、HEXと負の文字間が検出対象になる。
- 期待効果: テーマ定義とタイポグラフィ基準が統一され、後続修正の判断がぶれにくくなる。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: ライトテーマのHEXを同等の `oklch()` に置換し、`body` の `letter-spacing` を `0` にする。READMEのタイポグラフィ方針も、実装変更後に「文字間は標準値」に更新する。

### 4. 追加機能アイデア
- 機能名: 0件条件のテスト開始ガード
  - 解決する課題: 検索/フィルタ0件時に全体10問へ切り替わる。
  - 概要: 条件0件では開始ボタンをdisabledにし、検索条件の変更を促す。
  - ユーザー価値: テスト範囲が予測しやすくなる。
  - 制約環境での実現性: 既存 `_getQuizPlan()` と `_renderTest()` の小変更で実現できる。
  - 差別化ポイント: ローカル単語帳でも誤操作を防ぐ業務向け品質になる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 次の10問カーソル
  - 解決する課題: 繰り返しテストで同じ10語になりやすい。
  - 概要: テスト完了ごとに出題開始位置を出題数分進める。
  - ユーザー価値: 800語全体を短時間セッションで順に回せる。
  - 制約環境での実現性: `localStorage` に既存保存される `randomCursor` を使える。
  - 差別化ポイント: 量と短時間学習を両立できる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 保存データ異常通知
  - 解決する課題: 破損JSONが初期化されたことがユーザーに伝わらない。
  - 概要: 読み込み時警告を保存状態とは別に保持し、起動後に表示する。
  - ユーザー価値: 進捗消失の理由を把握できる。
  - 制約環境での実現性: 外部依存なしでフッターまたは設定内に表示できる。
  - 差別化ポイント: ローカル保存ツールとして障害時の説明責任を持てる。
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 出力CSVの選択
  - 解決する課題: 1クリックで2ファイルが出る。
  - 概要: 進捗CSVと履歴CSVを別ボタンにし、履歴0件では履歴CSVを無効化する。
  - ユーザー価値: ファイル運用の予測がしやすい。
  - 制約環境での実現性: 既存の2関数を別アクションに割り当てるだけで実装できる。
  - 差別化ポイント: 管理端末でも扱いやすい出力導線になる。
  - 実装コスト: 小
  - 優先度: 中
- 機能名: テスト範囲ラベル付き履歴
  - 解決する課題: 最近のテストに出題条件が残らない。
  - 概要: セッション保存時にフィルタ条件と範囲ラベルを保存し、最近のテストとCSVに出す。
  - ユーザー価値: 学習履歴の意味を後から確認できる。
  - 制約環境での実現性: 既存セッションオブジェクトに文字列を追加するだけで始められる。
  - 差別化ポイント: 外部LMSなしでも研修結果を説明しやすい。
  - 実装コスト: 小
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `_getQuizPlan()` を修正し、検索/フィルタ0件時は出題不可にする。同時にテスト完了時の `randomCursor` 更新を入れて、次回が次の10問になることを確認する。
- 2. 破損 `localStorage` 読み込み時の警告を保存成功で消さないようにし、壊れたJSON入力のスタブでフッターまたは設定内に初期化警告が残ることを確認する。
- 3. CSV出力を「進捗CSV」「履歴CSV」に分け、履歴0件時は履歴CSVをdisabledにする。設定モーダルにCSVの2種類とデータ削除範囲を1行ずつ追加する。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでのコンソールエラー完全確認。Google Chrome headlessは `chrome_status=134` でDOM出力とstderrが空だったため、実ブラウザ確認は未完了。
- 仮説のまま残った点: 1クリック2ファイルCSVが管理端末でブロックされるかは今回実機再現していない。`window.confirm()` の環境差も、管理ブラウザ実機では未確認。
- 次回追加で見るべき箇所: 0件検索からのテスト開始、2回連続テストの出題範囲、破損localStorage復旧、CSV出力のブラウザ挙動、画面内ヘルプの文言、例文引用符の表示。
- 実行ログ: `rg --files`, `rg`, `sed`, `nl`, `wc`, `git status`, `date`, Nodeによるスクリプト構文確認、Nodeによる `WORDS` 抽出、NodeスタブDOMでの初期実行確認、破損JSONスタブ確認、Chrome headless起動確認を行った。
- 起動確認の結果: NodeスタブDOMでは `STUB_OPEN_OK`、初期単語 `accept`、表示件数 `800語を表示中 / 全800語`、テスト開始表示 `現在の条件の800語から10問を出題します。`、保存状態表示の更新を確認した。実ブラウザのコンソールエラー完全確認は未完了。
- 今回変更したファイル: `REPORT.md` のみ。`MustKnowCodingEnglish.html`, `README.md`, 設定ファイル、画像、テスト、ビルド設定は変更していない。

## 2026-04-22
### 0. エグゼクティブサマリー
- 今回の結論: 前回までの大きな修正は進んでいるが、現行版は「画面に見えている情報」と「検索・履歴・CSVに残る情報」がまだ一致していない。特に自動生成した補足意味/例文の検索不可、4択の正解位置固定、履歴CSVの現在ステータス混入が、学習品質と実務再利用性を下げている。
- 最優先課題: 学習画面で表示している補足意味・自動例文を、そのまま検索対象に含める。現在の表示内容で再検索できない状態は発生頻度が高く、修正範囲も小さい。
- 次回の狙い: 画面表示、検索、テスト履歴、CSV出力の整合を先に揃え、短時間学習でも結果の信頼性を上げる。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 学習ビュー、検索導線、テストビュー、結果ビュー、最近のテスト、優先復習導線、CSV出力、設定モーダル
- 主な利用フロー: 初回表示 -> 検索/フィルタ -> 学習カード確認 -> テスト開始 -> 回答 -> 結果確認 -> 復習開始 -> CSV出力 -> 再利用
- 想定ユーザー: 日本語話者の新人/若手エンジニア、英語のPR/仕様書/エラー文に苦手意識がある開発者、外部LMSを使いにくい研修担当者
- ツールの主目的: 1HTMLで開発現場の頻出英語を検索、理解、4択確認、復習、履歴持ち出しまでローカル完結で回すこと
- 確認した観点: 既存Issueの解消状況、表示内容と検索/保存/出力の整合、4択品質、履歴の実務再利用性、誤操作耐性、制約環境での運用適性

### 2. 既存課題の確認結果
- 継続課題:
  - `20260421-01` 検索/フィルタ0件でも全体10問へ切り替わる
  - `20260421-03` 破損した保存データの警告が起動直後の自動保存で消える
  - `20260421-04` 画面に表示される補足意味や自動例文が検索対象に入らない
  - `20260421-07` 個別例文と補足意味が770語で未投入のまま残っている
  - `20260421-08` テスト履歴に出題範囲やフィルタ条件が残らない
  - `20260421-09` 画面内ヘルプ不足
  - `20260421-10` 破壊的操作が標準confirm依存
  - `20260421-11` ライトテーマのHEX色と負の文字間
- 更新した Issue:
  - `20260421-03`: 読み込み警告が `_saveState()` 成功で消える箇所を特定した
  - `20260421-04`: 自動表示される `略語表現` / `頭字語` / 自動例文が検索0件になることを件数で確認した
  - `20260421-07`: 空データは770語まで減ったが、まだ大半が汎用文のまま残ることを確認した
  - 旧 `結果画面の「復習を開始する」が優先復習ワードへ移動しない`: 主CTA自体は改善済みだが、現行フィルタ次第で復習対象へ着地しない残課題がある
- 今回クローズ判断した論点:
  - `localStorage` 保存失敗で起動停止する論点は、`_loadState()` / `_saveState()` の `try/catch` 追加で解消済み
  - 外部フォント/CDN依存は、現行本体HTMLでは解消済み
  - 履歴CSV不足は、進捗CSVと履歴CSVの2系統出力追加で一部解消済み
  - 設定モーダルの初期フォーカス、トラップ、復帰は解消済み
  - 回答後のライブ領域不足と選択肢disabled不足は解消済み
- 重複のため新規登録しなかった論点:
  - `CSV出力` が1クリックで2ファイルを出す論点は `20260421-05` の継続として扱い、新規化しなかった
  - 最近のテストに出題範囲が残らない論点は `20260421-08` の継続として扱った
  - 画面内ヘルプ不足は `20260421-09` の継続として扱った
  - `window.confirm()` 依存は `20260421-10` の継続として扱った

### 3. 発見した課題一覧

#### Issue: 20260422-01
- 状態: 更新
- 分類: バグ / UX
- タイトル: 破損した保存データの警告が起動直後の自動保存で消える
- 種別: 事実
- 要約: 壊れた `localStorage` を検知した時の警告は `_loadState()` で設定されるが、起動直後の `_saveState()` 成功で上書きされる。初期化された理由がユーザーに残らない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_loadState()`, `_saveState()`, `_renderFooter()`; 画面 保存状態フッター; 操作手順 1. 壊れた保存データを持つ状態で起動 2. 初期化メッセージが設定される 3. 起動末尾の自動保存で成功すると警告が消える; 該当箇所 9507行目の読み込み失敗警告、9599行目の破損JSON警告、9608行目の成功時クリア、10761行目の起動直後 `_saveState()`。
- 内容: 現状は「保存不可」警告だけが残るが、「壊れていたので初期化した」警告は残らない。原因説明が消えると、進捗が消えた理由をユーザーが判断できない。
- ユーザー影響: ブラウザ移行、手動バックアップ復元、旧版からの持ち込み後に、データが初期化された理由が分からない。
- 制約環境での影響: 外部サポートなしでHTMLだけ配る現場では、保存異常の説明を画面内で完結させる必要がある。
- 期待効果: データ初期化の理由が残り、保存異常時の問い合わせと不信感を減らせる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_runtime._storageError` を「読み込み警告」と「保存不可警告」に分ける。起動後の自動保存では読み込み警告を消さず、ユーザーが再読み込みまたは明示dismissするまで表示を残す。

#### Issue: 20260422-02
- 状態: 更新
- 分類: UX / 改善
- タイトル: 画面に表示される補足意味と自動例文の大半が検索対象に入らない
- 種別: 事実
- 要約: 学習カードでは自動生成の補足意味と例文を表示しているが、検索は生データだけを対象にしている。見えている語句で再検索できない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_getMeaningSub()`, `_getExampleJa()`, `_getFilteredWords()`; 画面 学習ビュー検索; 操作手順 1. 略語または頭字語カードを開く 2. 画面上の補足意味 `略語表現` または `頭字語` を確認 3. 検索欄に同語句を入力 4. 0件になる; 該当箇所 9653-9701行目の自動生成、9704-9728行目の検索処理。Node確認で `rawSearch_ryakugo: 0 / uiMeaningSub: 70`, `rawSearch_toujigo: 0 / uiAcronym: 30`, `rawSearch_phrase: 0 / uiExample: 70` を確認した。
- 内容: ユーザーはカードで見た補足意味や例文を手掛かりに再検索しようとするが、現在はその導線が切れている。表示と検索対象の不一致で、カード確認後の再利用性が落ちる。
- ユーザー影響: さっき見た単語を文脈で探し直せず、検索体験への信頼が下がる。
- 制約環境での影響: 外部辞書や別検索ツールを併用しにくい現場では、HTML内検索が主導線になるため影響が大きい。
- 期待効果: 画面に見えている文脈から、そのまま再検索できるようになる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_getFilteredWords()` の検索プールを生データではなく `_getMeaningSub(word)`, `_getExampleEn(word)`, `_getExampleJa(word)` を使って構成する。略語/頭字語/空例文フォールバックの検索再現を確認する。

#### Issue: 20260422-03
- 状態: 新規
- 分類: バグ / UX
- タイトル: 4択の正解位置が乱数ではなく問題番号と語長で固定されている
- 種別: 事実
- 要約: 正解選択肢の位置は `questionIndex + word.term.length` の剰余で決まる。テスト順が近い回では、答えの位置を覚える学習に寄りやすい。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_generateChoicesForQuestion()`; 画面 テストビュー; 操作手順 1. 同条件でテストを複数回開始 2. 同語または同順序の語で正解位置が再現される; 該当箇所 9877-9878行目。Node確認で先頭10語の正解位置は `accept -> 2`, `access -> 3`, `account -> 1`, `algorithm -> 0`, `allow -> 1`, `alternative -> 0` だった。
- 内容: 誤答候補の質はある程度保たれているが、位置だけは固定式なので、繰り返し学習で「意味ではなく位置」で答える余地が残る。短時間反復を売りにするツールほど、評価の信頼性に効く。
- ユーザー影響: 正答率が語彙理解ではなく出題パターン記憶で上がる可能性がある。
- 制約環境での影響: 研修や自己学習で同じPC・同じ条件を繰り返す運用では、スコアが実力以上に見えやすい。
- 期待効果: テスト結果が語彙理解に近づき、最近のテストやCSV履歴の説得力が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: テスト開始時にセッション単位の小さなseedを確定し、各問題の4択順をseed付きshuffleで決める。少なくとも同一語が同じ位置に固定されないことを確認する。

#### Issue: 20260422-04
- 状態: 新規
- 分類: UX / 追加機能
- タイトル: 履歴CSVの `status` 列が回答時点ではなく出力時点の現在ステータスを出している
- 種別: 事実
- 要約: 履歴CSVはセッション保存時のステータスを持たず、CSV出力時に現在の `_getStatus()` を参照している。過去の回答に対する状態が後から変わる。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_finishQuiz()`, `_exportSessionsCsv()`; 画面 フッターCSV出力; 操作手順 1. テストを実施 2. その後カードで単語ステータスを変更 3. 履歴CSVを出力 4. CSVの `status` は現在値になる; 該当箇所 9965-9969行目では `finishedAt`, `total`, `correct`, `answers` だけ保存し、10489-10497行目では `STATUS_LABELS[_getStatus(_word.id)]` をCSVへ出している。
- 内容: 履歴CSVは出るようになったが、現状の `status` 列は「回答時の状態」ではなく「今の状態」である。履歴として読むときに、ミス直後の復習理由や改善過程が消える。
- ユーザー影響: 研修報告や自己学習ログで、過去にどの状態だったかを説明しにくい。
- 制約環境での影響: 外部LMSを使わずCSVを唯一の証跡にする運用では、履歴の整合性が不足する。
- 期待効果: CSVをそのまま履歴証跡として扱えるようになる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_answerQuiz()` または `_finishQuiz()` で `statusAfterAnswer` を保存し、履歴CSVはそのスナップショットを出す。必要なら `statusBeforeAnswer` は後続タスクに分ける。

#### Issue: 20260422-05
- 状態: 更新
- 分類: バグ / UX
- タイトル: 優先復習CTAが現在フィルタを引き継ぐため復習対象へ着地しない場合がある
- 種別: 事実
- 要約: 結果画面の「復習を開始する」は優先語IDを設定するが、検索/レベル/ステータス/アルファベット条件は解除しない。現行フィルタに弾かれると空画面か別単語へ移る。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_startPriorityReview()`, `_renderLearning()`; 画面 結果ビュー -> 学習ビュー; 操作手順 1. 検索やフィルタを狭くしたままテスト 2. 結果で `復習を開始する` を押す 3. 条件により対象語が表示されない; 該当箇所 10507-10530行目では `wordId` だけ設定し、10125-10143行目では現在フィルタに存在しない `wordId` を先頭語へ置き換えるか、0件なら空表示にする。
- 内容: 主CTA自体は改善されたが、現在のフィルタ状態に依存するため、期待どおりの「優先語へジャンプ」にはまだなっていない。特に0件検索から全体テストへ入った後は、復習導線まで空表示になる。
- ユーザー影響: 結果から復習へ戻る主導線を押しても、何を見ればよいか分からない。
- 制約環境での影響: 研修中や短時間利用で、結果からすぐ復習へ戻りたい場面で手戻りが増える。
- 期待効果: テスト -> 復習の往復が安定し、苦手語に迷わず戻れる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `start-priority-review` 実行時に `search`, `level`, `status`, `letter` を一時的に `all` / 空へ戻し、対象語を確実に表示する。後続で「優先復習モード」を独立させる。

#### Issue: 20260422-06
- 状態: 更新
- 分類: 追加機能 / UX
- タイトル: 個別例文と補足意味が770語で未投入のまま残り、教材価値の差別化がまだ弱い
- 種別: 事実
- 要約: 30語分の個別データは入ったが、残り770語は依然として汎用フォールバック表示である。量の価値は見えるが、開発文脈の教材としての密度はまだ薄い。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_getMeaningSub()`, `_getExampleEn()`, `_getExampleJa()`; 画面 学習カード; 操作手順 1. 複数語を開く 2. 30語以外は汎用補足意味と汎用例文に落ちる; 該当箇所 9653-9701行目のフォールバック処理。Node確認で `emptyExampleEn: 770`, `emptyExampleJa: 770`, `emptyMeaningSub: 770` を確認した。
- 内容: 先頭30語で改善は見えるが、全800語ツールとしては大半の語で「実務でどこに出るか」がまだ薄い。特に略語/頭字語以外の一般語で、文脈による差別化が弱い。
- ユーザー影響: 初見ユーザーが数語見ただけでは、無料の一般単語帳との差を感じにくい。
- 制約環境での影響: READMEやLPを見ずHTMLだけ受け取る配布では、画面内の例文品質が価値訴求の主材料になる。
- 期待効果: 開発現場で使う教材としての説得力が上がり、継続利用の理由を作りやすい。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 次回1回では全件を狙わず、履歴に出やすい語・検索頻度が高い語・略語/頭字語の重要語から50語分まで拡張する。PRレビュー、障害対応、仕様説明の3カテゴリを先に埋める。

#### Issue: 20260422-07
- 状態: 新規
- 分類: パフォーマンス / UX
- タイトル: 検索入力のたびに保存と全画面再描画が走り、低性能端末で入力遅延の恐れがある
- 種別: 推論
- 要約: 検索欄は1文字ごとに `localStorage` 保存と `_render()` を実行し、学習・テスト・結果・設定・フッターを毎回描画する。800語規模では古い端末や管理ブラウザで遅延要因になり得る。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `document.addEventListener("input")`, `_saveState()`, `_render()`, `_renderLearning()`; 画面 学習ビュー検索; 操作手順 1. 検索入力を連続で行う 2. 入力ごとに保存と再描画が発生する; 該当箇所 10702-10710行目で入力ごとに `_saveState()` と `_render()`、10454-10461行目で全画面描画、10148-10165行目で単語リスト再構築。Node確認で `WORDS` は800件。
- 内容: 実ブラウザでの体感再現は今回未実施だが、同期 `localStorage` 書き込みと全画面描画を毎キー入力で行う設計は、検索の主導線に対して重い。今後語彙や履歴表示が増えるほど影響が出やすい。
- ユーザー影響: 検索が引っかかると、最短で単語を探す体験が崩れる。
- 制約環境での影響: 企業PC、研修用端末、古いブラウザではCPUとストレージ性能が弱いことが多い。
- 期待効果: 検索入力の体感が安定し、主導線のストレスを減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: 検索入力では描画を学習ビューに限定し、保存は `input` ごとではなく `debounce` または `blur` 時に寄せる。次回は実ブラウザで入力体感も確認する。

### 4. 追加機能アイデア
- 機能名: 表示内容準拠検索
  - 解決する課題: カードで見える補足意味や自動例文を検索で再利用できない
  - 概要: 検索対象を生データではなく、実際に画面へ表示する補足意味・例文に揃える
  - ユーザー価値: 見た語句からそのまま引き直せる
  - 制約環境での実現性: 既存関数 `_getMeaningSub()` と `_getExample*()` を使うだけで始められる
  - 差別化ポイント: HTML単体でも辞書的な再検索体験を持てる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: セッションseed付き4択シャッフル
  - 解決する課題: 正解位置固定で位置記憶に寄る
  - 概要: テスト開始時のseedを使って各問題の選択肢順だけを軽くシャッフルする
  - ユーザー価値: 正答率が語彙理解に近づく
  - 制約環境での実現性: 外部依存なし、単一HTMLのまま実装できる
  - 差別化ポイント: 業務学習向けのテスト品質を小変更で上げられる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 履歴スナップショットCSV
  - 解決する課題: 履歴CSVの `status` が出力時の現在値になる
  - 概要: 回答時点のステータスや出題条件をセッションへ保存し、そのままCSVへ出す
  - ユーザー価値: CSVを履歴証跡として使いやすい
  - 制約環境での実現性: 既存 `sessions` 構造に小さな項目追加で始められる
  - 差別化ポイント: 外部LMSなしでも研修記録を持ち出せる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 優先復習モード
  - 解決する課題: 現行フィルタが復習導線を邪魔する
  - 概要: 結果画面からは専用の復習キューで学習カードへ入る
  - ユーザー価値: テスト後すぐ苦手語へ戻れる
  - 制約環境での実現性: 既存の `priorityIds` を再利用できる
  - 差別化ポイント: 結果から復習までの往復が明確になる
  - 実装コスト: 小
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `_getFilteredWords()` を表示準拠検索へ修正し、`略語表現`, `頭字語`, 自動例文フレーズでヒットすることを確認する。
- 2. `_generateChoicesForQuestion()` の正解位置固定をやめ、セッションseed付きの軽いシャッフルへ置き換える。繰り返しテストで同語の正解位置が固定されないことを確認する。
- 3. 履歴CSVへ `statusAfterAnswer` と出題条件スナップショットを保存し、結果画面の `復習を開始する` ではフィルタを解除して優先語へ確実に着地させる。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでの入力遅延体感、CSV二重ダウンロードのブラウザ差、管理端末での `confirm` 表示差
- 仮説のまま残った点: 検索入力ごとの全画面再描画が、どの程度体感遅延になるかは今回未計測
- 次回追加で見るべき箇所: 0件検索からのテスト開始、`復習を開始する` のフィルタ解除後挙動、履歴CSVの列定義、4択シャッフル後の誤答候補品質
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ

## 2026-04-23
### 0. エグゼクティブサマリー
- 今回の結論: 前回までに検索準拠表示や4択シャッフルは進んでいるが、空状態とデータ削除の境界がまだ甘い。特に「検索0件時でも前回単語のステータスを変更できる」「全データ消去後に `localStorage` キーが残る」は、実務運用と信頼性の両方に直結する。
- 最優先課題: 学習ビューの0件状態を完全な非操作状態にし、非表示の前回単語へステータス更新が入らないようにする。
- 次回の狙い: 空状態、ランダム導線、履歴保持、データ削除の挙動を揃え、短時間反復でも誤操作と履歴欠損が起きにくい状態へ寄せる。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 学習ビューの検索0件状態、テスト開始画面、設定モーダルのデータ削除、ランダム移動、最近のテストとCSV履歴
- 主な利用フロー: 初回表示 -> 検索/フィルタ -> 学習カード確認 -> ステータス更新 -> ランダム移動 -> テスト開始 -> 結果確認 -> データ削除 -> 再利用
- 想定ユーザー: 日本語話者の新人/若手エンジニア、短時間学習で語彙確認を回したい個人利用者、外部LMSを使いにくい研修担当者
- ツールの主目的: 1HTMLで開発現場の頻出英語を検索、学習、4択確認、復習、履歴持ち出しまでローカル完結で回すこと
- 確認した観点: 既存Issue重複確認、空状態の誤操作耐性、削除導線の実データ挙動、ランダム/反復の到達範囲、履歴保持上限、README説明との整合

### 2. 既存課題の確認結果
- 継続課題:
  - `20260421-05` CSV出力が1クリックで2ファイルを連続ダウンロードする
  - `20260421-09` 「ヘルプ / 設定」を開いても使い方とデータ仕様が画面内にない
  - `20260421-10` 破壊的操作がすべてブラウザ標準confirmに依存している
  - `20260421-11` ライトテーマのHEX色と負の文字間がルールに残っている
  - `20260422-06` 個別例文と補足意味が770語で未投入のまま残り、教材価値の差別化がまだ弱い
  - `20260422-07` 検索入力のたびに保存と全画面再描画が走り、低性能端末で入力遅延の恐れがある
- 更新した Issue:
  - `20260421-01`: 0件時に全体10問へ切り替わる挙動は解消済み。ただし、開始CTAはまだ有効のままで、READMEの「0件のときは開始不可」までは到達していない。
  - `20260421-02`: テスト反復で同じ10語になりやすい原因は現行版でも残っている。`quiz` 経路では出題カーソルが進まず、`randomCursor` は学習ビューのランダム移動にしか使われていない。
  - 旧 `最大30件のテスト履歴を保存しているが過去回を見返せない`: 最近のテスト表示と履歴CSVで一部解消したが、31件目以降の無通知切り捨ては別問題として残る。
- 重複のため新規登録しなかった論点:
  - `window.confirm()` 依存は `20260421-10` の継続として扱い、新規化しなかった
  - 画面内ヘルプ不足は `20260421-09` の継続として扱い、新規化しなかった
  - 1クリック2ファイルCSVは `20260421-05` の継続として扱い、新規化しなかった

### 3. 発見した課題一覧

#### Issue: 20260423-01
- 状態: 新規
- 分類: バグ / UX
- タイトル: 検索0件状態でも非表示の前回単語にステータス更新が入る
- 種別: 事実
- 要約: 学習ビューが0件のとき、カード本文は「表示対象がありません」に切り替わるが、ステータスボタンは残ったままである。押下時は空状態ではなく `_state.learning.wordId` の前回単語を更新する。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_renderLearning()`, `_handleAction()`; 画面 学習ビューの検索0件状態; 操作手順 1. 任意の単語を表示したまま検索で0件にする 2. 画面中央が「表示対象がありません」になる 3. `覚えた` / `苦手` を押す 4. 非表示の前回単語のステータスが更新される経路になっている; 該当箇所 10163-10172行目は0件時に本文だけ差し替えて return、1106-1110行目のステータスボタンは常時表示、10691-10697行目は `_state.learning.wordId` に対して無条件で状態を書き換える。
- 内容: 0件状態は「何も選ばれていない」ように見えるが、内部では前回単語が生きたままである。さらに 10202-10215行目の `current-status-tag` と `aria-pressed` 更新も 0件分岐では走らないため、視覚上も前回状態が残る。
- ユーザー影響: 検索失敗後に誤って前回単語の状態を変え、進捗の信頼性を損なう。
- 制約環境での影響: 外部ログやUndoがないローカルHTML運用では、誤更新の原因追跡がしにくい。
- 期待効果: 空状態での誤操作を防ぎ、表示内容と内部状態の不一致を解消できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_renderLearning()` の0件分岐で `current-status-tag` とステータスボタンの選択状態をクリアし、各ボタンをdisabledにする。加えて `set-learning-status` 側でも `0件時は更新しない` ガードを入れる。

#### Issue: 20260423-02
- 状態: 新規
- 分類: バグ / セキュリティ / UX
- タイトル: 「すべてのデータを消去」後も `localStorage` キーが再作成される
- 種別: 事実
- 要約: 画面文言は「進捗・履歴・テーマを含む全データを消去」となっているが、実装は初期状態を代入したあと即座に `_saveState()` を呼ぶ。結果として保存キー自体は削除されない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_clearAllData()`, `_saveState()`, `_handleAction()`; 画面 設定モーダルのデータ管理; 操作手順 1. 設定から `すべてのデータを消去` を実行 2. `_clearAllData()` が初期状態を代入 3. `_handleAction()` 末尾で `_saveState()` が必ず走る 4. `localStorage` に初期状態が再保存される; 該当箇所 1301行目のボタン文言、10620-10645行目の初期化、9612-9621行目の `localStorage.setItem(...)`、10748-10749行目の共通保存。本体内に `localStorage.removeItem(STORAGE_KEY)` や `localStorage.clear()` は存在しない。
- 内容: ユーザー視点では「削除」だが、実態は「初期化して再保存」である。学習内容は消える一方、保存キーは残るため、共有端末や削除確認の厳しい現場では説明と挙動がずれる。
- ユーザー影響: 「完全に消した」と思って再配布・共用したときに、保存痕跡が残る。
- 制約環境での影響: 共有PC、研修端末、持ち回り評価端末では、保存痕跡が残らないこと自体が要件になる場合がある。
- 期待効果: 削除対象と保存実態が一致し、データ管理の信頼性が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `clear-all-data` 実行時だけは `_saveState()` を通さず `localStorage.removeItem(STORAGE_KEY)` を呼ぶ。削除直後の再描画はメモリ上の初期状態で行い、再読込後も保存キーが存在しないことを確認する。

#### Issue: 20260423-03
- 状態: 新規
- 分類: バグ / UX
- タイトル: ランダム移動が `+3` 固定のため一部レベルで全語を巡回しない
- 種別: 事実
- 要約: `ランダム` ボタンは乱数ではなく `randomCursor += 3` の巡回式を使っている。語数が3の倍数の集合では到達できる語が一部に固定される。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_jumpRandomWord()`; 画面 学習ビューのランダム移動; 操作手順 1. Basic 300語または Advanced 300語に絞る 2. `ランダム` を繰り返す 3. 理論上 `randomCursor % length` が一部インデックスだけを巡回する; 該当箇所 10118-10127行目。Node確認では現行HTMLの語数が `basic: 300`, `advanced: 300`, `acronym: 30` で、同式の到達件数は `basic: 100`, `advanced: 100`, `acronym: 10` だった。
- 内容: 再現性を意図した巡回式でも、固定ステップと集合サイズの公約数を考慮しないと探索範囲が欠ける。特に30語の頭字語は10語しか回らないため、ランダム導線の名前と実態がずれる。
- ユーザー影響: `ランダム` を押しても学習対象の一部しか見られず、語彙の広さを体感できない。
- 制約環境での影響: 検索よりランダム巡回で短時間学習する運用では、未到達語が長く残りやすい。
- 期待効果: ランダム移動で集合全体を偏りなく回せるようになり、短時間学習の効率が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `+3` 固定をやめ、集合長と互いに素な step を選ぶか、既存 `_shuffleWithSeed()` で現在集合の並びを作ってカーソルを `+1` で進める方式に変える。最低限、300語/30語集合で全件に到達できることを確認する。

#### Issue: 20260423-04
- 状態: 更新
- 分類: UX / 改善
- タイトル: 新しいテストを繰り返しても同じ10語ウィンドウに戻りやすい
- 種別: 事実
- 要約: 4択の正解位置固定は解消したが、出題対象の開始位置は依然として進まない。`quiz` を繰り返しても `_buildQuizOrder()` が同じウィンドウを返しやすい。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_buildQuizOrder()`, `_startQuiz()`, `_finishQuiz()`, `_jumpRandomWord()`; 画面 テスト開始 -> テスト完了 -> 再テスト; 操作手順 1. フィルタなしでテストを完了 2. そのまま `新しいテスト` を開始 3. `randomCursor` が進んでいないため同じ開始位置になりやすい; 該当箇所 9784-9793行目は `randomCursor` を起点に10件切り出し、9822-9843行目と9990-10014行目にはカーソル更新がない。`randomCursor` を増やすのは 10124-10126行目の学習ビュー `ランダム` だけである。
- 内容: 現在は学習ビューの `ランダム` 操作がテスト開始位置に流用されているため、テスト側に独立した進行概念がない。結果として「反復テスト」と「学習カードのランダム移動」が意図せず結びついている。
- ユーザー影響: 800語の広さを活かした反復学習になりにくく、同じ先頭側だけを繰り返す印象が残る。
- 制約環境での影響: 研修や自己学習で短時間セッションを積み重ねる運用では、手動で開始位置をずらす手間が増える。
- 期待効果: テストを繰り返すだけで自然に出題範囲が進み、収録語彙全体に触れやすくなる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 学習カード用 `randomCursor` とテスト用 `quizCursor` を分離し、`_finishQuiz()` で `quizCursor += order.length` を進める。少なくとも連続3回のテストで開始10語が同じ並びにならないことを確認する。

#### Issue: 20260423-05
- 状態: 更新
- 分類: UX / 改善
- タイトル: 0件時のテスト開始CTAが有効のままで「開始不可」になっていない
- 種別: 事実
- 要約: 0件時に全体10問へ切り替わる不具合は解消したが、テスト開始ボタン自体はまだ押せる。READMEの「0件のときは開始不可」と画面挙動が一致していない。
- 証拠: 対象ファイル `README.md`, `MustKnowCodingEnglish.html`; 関数 `_renderTest()`, `_startQuiz()`; 画面 テスト開始画面の0件状態; 操作手順 1. 検索やフィルタで0件にする 2. テスト画面へ移動 3. 「検索やフィルタ条件を変更してから開始してください」と表示される 4. それでも `この条件で開始` ボタンを押せる; 該当箇所 README 25行目は「0件のときは開始不可」、1175-1180行目は常時表示の開始ボタン、10238-10252行目は文言変更のみ、9833-9838行目は0件なら no-op で戻るだけ。
- 内容: ユーザーへの案内は正しい方向に変わったが、UI部品の有効/無効状態が追随していない。開始できない条件なのに押せると、動かない理由を毎回読み直す必要がある。
- ユーザー影響: 0件条件で no-op を繰り返し、検索やテスト導線への信頼が下がる。
- 制約環境での影響: 短時間学習や研修中の説明では、「押せるが始まらない」状態が操作ミスとして残りやすい。
- 期待効果: README、画面文言、実際の操作可否が一致し、0件時の迷いを減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 0件時は開始ボタンをdisabledにし、`aria-disabled` と補足文言を付ける。可能なら検索入力へフォーカスを戻し、次の行動を明示する。

#### Issue: 20260423-06
- 状態: 新規
- 分類: UX / 改善
- タイトル: 31件目以降の履歴が無通知で切り捨てられる
- 種別: 事実
- 要約: 最近のテスト表示と履歴CSVは追加済みだが、保存対象は最新30件に固定されている。上限到達時の通知も、README内の説明もない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`, `README.md`; 関数 `_loadState()`, `_finishQuiz()`, `_exportSessionsCsv()`; 画面 結果ビュー、CSV出力、設定説明; 操作手順 1. テストを31回以上実施 2. `_state.sessions` が `slice(-30)` される 3. 画面表示と履歴CSVの両方から最古セッションが消える; 該当箇所 9353行目の `MAX_STORED_SESSIONS = 30`、9588-9589行目の読込時切り詰め、9998-10006行目の保存時切り詰め、10540-10550行目のCSV出力は `_state.sessions` のみを対象にする。README 34-43行目の保存/CSV説明には30件上限の記載がない。
- 内容: 「最近のテスト」が見えるようになったことで履歴価値は上がったが、長期運用では30件を超えた瞬間に古い証跡が消える。ローカル完結運用ほど、消える条件の明示が重要になる。
- ユーザー影響: 研修記録や自己学習の長期ログを保持しているつもりで、古い履歴を失う。
- 制約環境での影響: 外部LMSやクラウド保存を使わない現場では、HTML内履歴とCSVが唯一の証跡になりやすい。
- 期待効果: 履歴の保持条件が明確になり、長期利用時のデータ喪失感を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: まずは設定または結果画面に「履歴は最新30件まで保持」の明記を追加し、28件超でCSV出力を促す。必要なら後続タスクで保持件数引き上げや自動バックアップ案内へ広げる。

### 4. 追加機能アイデア
- 機能名: 空状態ロック
  - 解決する課題: 検索0件時でも前回単語の状態更新や開始CTA押下が残る
  - 概要: 0件時はステータスボタン、単語移動、テスト開始を一括で無効化し、検索変更だけを促す
  - ユーザー価値: 誤操作で進捗を壊しにくくなる
  - 制約環境での実現性: 既存 `_renderLearning()` と `_renderTest()` の条件分岐追加で実現できる
  - 差別化ポイント: 単一HTMLでも業務用らしい誤操作防止が入る
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 巡回シャッフル学習
  - 解決する課題: ランダム移動と反復テストが一部語に偏る
  - 概要: 現在集合を seed 付きで並べ替え、学習カードとテストで別カーソルを進める
  - ユーザー価値: 800語全体を少しずつ漏れなく確認しやすい
  - 制約環境での実現性: 既存 `_shuffleWithSeed()` を再利用でき、外部依存も不要
  - 差別化ポイント: ランダム見出しと実挙動のズレをなくし、短時間学習でも網羅性を保てる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 履歴保持ガード
  - 解決する課題: 31件目以降の履歴が無通知で消える
  - 概要: 保持上限表示、残件数表示、上限接近時のCSV出力促進を追加する
  - ユーザー価値: ローカル運用でも履歴消失を予防できる
  - 制約環境での実現性: 既存 `sessions.length` を見るだけで実装できる
  - 差別化ポイント: 外部保存なしでも運用ルールを画面内で伝えられる
  - 実装コスト: 小
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. 学習ビューの0件分岐を修正し、ステータスボタンと `current-status-tag` を無効化する。同時に `set-learning-status` 側へ0件ガードを追加し、検索0件で前回単語が変わらないことを確認する。
- 2. 学習用 `randomCursor` とテスト用 `quizCursor` を分離し、`ランダム` と連続テストの両方で集合全体に到達できることを確認する。最低限、Basic 300語と Acronym 30語で全件到達を確認する。
- 3. `clear-all-data` を真の削除に変更し、保存キー不在で再読込できることを確認する。あわせて結果画面または設定に「履歴は最新30件まで保持」を明記する。

### 6. 監査メモ
- 今回の監査で未確認の点: 管理ブラウザ実機での `confirm` 表示差、1クリック2ファイルCSVのブロック挙動、検索入力遅延の体感
- 仮説のまま残った点: フィルタクリック後のフォーカス維持は、カスタムの headless 採証がこの環境制約で通らず未実機確認
- 次回追加で見るべき箇所: 0件状態での各ボタン無効化後挙動、`ランダム` と連続テストのカーソル分離後の到達率、31件超の履歴保持表示、ヘルプ文言、CSV二重ダウンロードの扱い
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ

## 2026-04-24
### 0. エグゼクティブサマリー
- 今回の結論: 現行版は `localStorage` 保存失敗時の一時利用モードや初期分野100問化が進んでいる一方、データ削除の実態、進行中セッション保護、履歴可視化の整合がまだ弱い。特に「すべてのデータを消去」の範囲と、設定操作が進行中演習を巻き込む挙動は、共有端末運用と信頼性に直結する。
- 最優先課題: `clear-all-data` を真の削除に寄せ、`v1` / `v2` / 移行判定キーまで含めて削除対象を一致させる。
- 次回の狙い: データ管理操作の説明と実態を揃えたうえで、進行中演習を守る確認導線と、保持済み履歴を見返せる導線を足す。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 演習ビュー、成績/履歴ビュー、設定モーダル、旧データ移行モーダル
- 主な利用フロー: 初回表示 -> 分野選択 -> 演習開始 -> 回答 -> 成績/履歴確認 -> 設定から進捗/履歴/全データ操作 -> 再利用
- 想定ユーザー: 基本情報技術者試験の頻出英語を短時間で反復したい学習者、共有PCや研修端末でもローカル完結で運用したい担当者
- ツールの主目的: 単一HTMLで、分野別4択演習と結果確認をローカル保存付きで回すこと
- 確認した観点: 既存Issue重複確認、データ削除の実態、進行中演習の保護、分野切替時の状態整合、履歴保持上限、履歴の画面可視性、READMEとの整合

### 2. 既存課題の確認結果
- 継続課題:
  - `20260421-09` 「ヘルプ / 設定」を開いても使い方とデータ仕様が画面内にない
  - `20260421-10` 破壊的操作が `window.confirm()` に依存している
  - `20260422-06` 個別例文と補足意味が770語で未投入のまま残っている
- 更新した Issue:
  - `20260423-02`: 「すべてのデータを消去」の問題は継続。今回は `v2` キー再生成だけでなく、`v1` データと移行判定キーも削除対象外のまま残ることを追加確認した。
  - `20260423-06`: 履歴保持上限は現行版で `30` 件ではなく `12` 件になっていた。説明不足という根本原因は継続している。
  - `20260420-07`: 直近履歴一覧は追加済みだが、保持済みセッション全件を見返せる状態にはなっていない。現行UIでは保持済み12件のうち最大2件が画面上に現れない。
- 重複のため新規登録しなかった論点:
  - 破壊的操作の確認UIがブラウザ標準 `confirm` である点そのものは `20260421-10` の継続として扱い、新規化しなかった
  - 例文未投入は `20260422-06` の継続として扱い、新規化しなかった
  - 実ブラウザでの保存容量限界は未実機再現のため、今回の履歴上限論点では事実として扱わず、補助的な推論に留めた

### 3. 発見した課題一覧

#### Issue: 20260424-01
- 状態: 更新
- 分類: バグ / セキュリティ / UX
- タイトル: 「すべてのデータを消去」が `v1` / `v2` / 移行判定キーを消さない
- 種別: 事実
- 要約: `clear-all-data` はメモリ上の `_state` を初期化するだけで、削除後に共通保存が走るため `v2` キーが再生成される。さらに `v1` データと `must-know-coding-english-v2-migration-decided` も削除していない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`, `README.md`; 関数 `_clearAllData()`, `_handleAction()`, `_setMigrationDecision()`, `_detectMigrationCandidate()`; 画面 設定モーダルの「すべてのデータを消去」、旧データ移行モーダル; 操作手順 1. 旧 `v1` データまたは移行判定キーがある状態を想定 2. 設定から「すべてのデータを消去」を実行 3. `_clearAllData()` は 9985-9990行目でメモリ初期化のみ行う 4. `_handleAction()` が 10072-10073行目で `_saveState()` を必ず呼ぶ 5. `_saveState()` は 9250-9258行目で `STORAGE_KEY_V2` を再保存する; 該当箇所 README 28-31行目は `v2` 保存キーと `v1` 移行を説明、HTML 936-938行目で3キーを定義、9262-9264行目で移行判定を書き込み、9270-9281行目でそれらを読み込む。`removeItem` と `localStorage.clear` はコード内に存在しない。
- 内容: 事実: 画面文言は「すべてのデータを消去」だが、実装は「初期状態を再保存」に近い。事実: 移行判定キーが残るため、削除後に再度 `v1` データを読み込みたい場面でも移行確認は再表示されない。推論: 共有端末や配布確認では、ブラウザ内に何も残らないこと自体が運用要件になりやすい。
- ユーザー影響: 「完全に消した」と思って端末を次利用者へ渡しても、保存痕跡や移行状態が残る。
- 制約環境での影響: クラウドや管理コンソールを使わないローカルHTML運用では、ブラウザストレージの中身が唯一の保存実体であり、削除範囲の曖昧さがそのまま運用不安になる。
- 期待効果: 画面文言と実データの削除範囲が一致し、共有端末や再配布時の信頼性が上がる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `clear-all-data` 実行時だけは `STORAGE_KEY_V2`, `STORAGE_KEY_V1`, `MIGRATION_DECISION_KEY` を `removeItem` で削除し、`_runtime._migrationCandidate` と `_migrationPrompted` を初期化する。同アクション内では自動再保存を抑止し、再読込後も3キーが存在しないことを確認する。

#### Issue: 20260424-02
- 状態: 新規
- 分類: バグ / UX
- タイトル: 進捗リセットと履歴消去が進行中の演習も破棄する
- 種別: 事実
- 要約: フッターと設定モーダルの文言は「進捗」または「履歴」だけを消すように読めるが、実装はどちらも `_state.quiz = _createInitialQuizState()` を実行する。進行中の4択演習は別確認なしで失われる。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_resetProgress()`, `_clearHistory()`, `_startQuiz()`; 画面 フッターの「進捗リセット（履歴は残る）」、設定モーダルの「学習進捗のリセット（履歴は残る）」「履歴の消去」、演習中画面; 操作手順 1. 任意の分野で演習を開始して数問回答する 2. 設定を開き「履歴の消去」または「学習進捗のリセット」を実行する 3. confirm 文言は履歴または進捗だけを案内する 4. その後、進行中演習は初期状態に戻る; 該当箇所 876行目と907-908行目のボタン文言、9398-9427行目で演習状態を生成、9955-9963行目で進捗リセット時に `_state.quiz` を初期化、9966-9977行目で履歴消去時にも `_state.quiz` を初期化。
- 内容: 事実: 「履歴の消去」は結果履歴だけの操作名だが、実際には現在の演習セッションも同時に破棄する。事実: 「学習進捗のリセット」も同様に進行中演習を破棄するが、confirm 文言にその範囲が書かれていない。推論: 100問や300問の途中で設定を触るユーザーほど、想定外の損失になる。
- ユーザー影響: 長い演習の途中で進捗や履歴を整理しようとしただけで、その場の回答履歴を失う。
- 制約環境での影響: 研修端末や休憩時間の学習では、やり直し時間を確保しにくく、誤操作の回復手段もない。
- 期待効果: データ管理操作の影響範囲が明確になり、長時間セッションの途中離脱や誤消去を減らせる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_state.quiz.active && _state.quiz.answers.some(Boolean)` のときは、各 confirm に「進行中の演習も破棄します」を追加する。可能なら `clear-history` は `_state.sessions` のみ消し、進行中演習を巻き込まないよう責務を分ける。

#### Issue: 20260424-03
- 状態: 新規
- 分類: バグ / UX
- タイトル: 演習中も分野チップが有効で、表示サマリーと出題分野が食い違う
- 種別: 事実
- 要約: 演習中でも分野チップは押せるため、`selectedCategory` だけが更新される。一方、現在の出題分野は `quiz.category` のまま固定されるため、画面内で「現在の分野」と問題ヒントが異なる状態を作れる。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`; 関数 `_startQuiz()`, `_renderCategoryFilter()`, `_renderPracticeStats()`, `_renderPractice()`, `_handleAction()`; 画面 演習ビューの分野チップ、進捗サマリー、出題中の問題カード; 操作手順 1. `前提` で演習開始 2. 出題中に `アドバンスト` チップを押す 3. 左側サマリーは `アドバンスト 300問` 側へ更新 4. 問題ヒントは `前提` のまま進行する; 該当箇所 740行目のチップ表示領域、9417-9420行目で `quiz.category` に開始時分野を固定、9652-9657行目で分野チップを常時再描画、9660-9670行目でサマリーは `selectedCategory` を使用、9706-9708行目で問題ヒントは `quiz.category` を使用、10018-10022行目で `set-category` が無条件に `selectedCategory` を更新。
- 内容: 事実: 現在の演習スコープと、次回開始予定の分野が同じUI領域で混在している。推論: 学習者は「分野を切り替えたつもり」で回答を続けやすく、成績評価や進捗解釈を誤る。
- ユーザー影響: いま何の分野を解いているかを誤認し、結果画面の解釈や次の演習計画を誤りやすい。
- 制約環境での影響: 研修担当者が画面を見ながら口頭案内する場面で、UI表示だけでは現在スコープを説明しにくい。
- 期待効果: 演習中の分野が一貫して見え、誤認による学習ロスを減らせる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 演習中は分野チップを disabled にするか、「現在の演習分野」と「次回開始分野」を分けて表示する。最低限、`quiz.active` 中に `selectedCategory` を変えたら confirm を出し、即時反映しない形にする。

#### Issue: 20260424-04
- 状態: 更新
- 分類: UX / 改善
- タイトル: 履歴保持上限が12件に縮小されているが画面とREADMEで告知がない
- 種別: 事実
- 要約: 現行版の `MAX_STORED_SESSIONS` は `12` だが、画面内にも README にも保持上限の説明がない。長期利用者は保存され続ける前提で運用しやすい。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`, `README.md`; 関数 `_loadStateV2()`, `_buildMigratedState()`, `_finishQuiz()`; 画面 成績/履歴ビュー、設定モーダル、READMEの使い方; 操作手順 1. セッションを繰り返し完了する 2. 13件目以降は古い履歴が切り捨てられる 3. 画面や README に上限説明はない; 該当箇所 944行目の `MAX_STORED_SESSIONS = 12`、9179行目と9244行目の読込/移行時切り詰め、9591-9592行目の保存時切り詰め、README 38行目は「最新結果と直近履歴を確認」とのみ記載。補助採証として、実データに近い全分野800問セッション12件の JSON は Node試算で約1.24MBだった。
- 内容: 事実: 保持上限は `12` 件で固定され、超過時は自動で古い履歴が落ちる。推論: 上限縮小はストレージ安全性を意識した可能性があるが、現状はその意図がユーザーへ伝わっていない。
- ユーザー影響: 長期学習の途中で古い履歴が消えても理由が分からず、学習記録を失ったと感じやすい。
- 制約環境での影響: ローカル完結運用ではブラウザ内履歴が唯一の証跡になりやすく、保持条件の不明瞭さがそのまま運用事故になる。
- 期待効果: いつ何件まで残るかが明確になり、履歴消失の誤解を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 成績/履歴または設定に「履歴は最新12件まで保持」を明記し、10件以上で上限接近表示を出す。必要なら後続で JSON エクスポートや保持件数設定へ広げる。

#### Issue: 20260424-05
- 状態: 更新
- 分類: UX / 追加機能
- タイトル: 保持12件のうち最大2件がUIから見えず、過去セッション詳細も再表示できない
- 種別: 事実
- 要約: 履歴一覧は `slice(-10)` で最新10件だけを要約表示し、詳細表は常に最新1件しか出さない。そのため保持12件運用では、最古の2件が画面上に現れず、非最新セッションの誤答詳細も見返せない。
- 証拠: 対象ファイル `MustKnowCodingEnglish.html`, `README.md`; 関数 `_renderRecentSessions()`, `_getLatestSession()`, `_renderHistory()`; 画面 成績/履歴ビュー; 操作手順 1. 12件のセッションを保存する 2. 成績/履歴画面を開く 3. 詳細表は最新1件固定 4. 直近履歴は `slice(-10)` の要約のみ 5. 保持済みのうち最古2件は一覧に出ず、どの過去回も詳細展開できない; 該当箇所 944行目で保持上限12件、9749行目で直近10件へ制限、9781-9834行目で詳細表は `_getLatestSession()` のみ使用、README 38行目は「最新結果と直近履歴を確認」と説明。
- 内容: 事実: 保持している件数と、画面から到達できる件数が一致していない。事実: 直近履歴の各項目は `<li>` 要約で、セッション選択や詳細表示の導線がない。推論: 「履歴を残す」価値より「最新1回を見る」価値に偏っており、復習用途では保持データを活かし切れていない。
- ユーザー影響: 以前のミス一覧を見返したいとき、次のセッションを1回でも実施すると直前の詳細を失いやすい。
- 制約環境での影響: 外部LMSやクラウド履歴を併用しない現場では、ローカル画面内で履歴比較できないことがそのまま運用制約になる。
- 期待効果: 保持済みセッションを実際に見返せるようになり、学習の振り返りと説明責任が取りやすくなる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 直近履歴の各行をボタン化し、選択したセッションを KPI と詳細表へ再描画できるようにする。あわせて「さらに2件保持中」など、未表示分の存在を画面内で明示する。

### 4. 追加機能アイデア
- 機能名: 完全削除モード
  - 解決する課題: 全データ削除の文言と実態がずれ、旧キーや移行状態が残る
  - 概要: `v1` / `v2` / 移行判定キーをまとめて削除し、削除完了後は「現在は未保存状態です」を明示する
  - ユーザー価値: 共有端末や再配布前の初期化を1操作で実行できる
  - 制約環境での実現性: `localStorage.removeItem` と既存フッター文言の分岐だけで実現できる
  - 差別化ポイント: 単一HTMLでも、データ削除の境界が運用要件に耐える
  - 実装コスト: 小
  - 優先度: 高
- 機能名: セッション保護ガード
  - 解決する課題: 進捗リセットや履歴消去で進行中演習まで消える
  - 概要: 進行中演習があるときは操作影響を明示し、必要なら「演習は残す / 演習も破棄」の二択に分ける
  - ユーザー価値: 長い演習の途中で誤操作しても損失を避けやすい
  - 制約環境での実現性: 既存 `_state.quiz.active` と confirm 分岐を使えば実装できる
  - 差別化ポイント: ローカル完結ツールでも中断耐性がある
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 履歴スロット可視化
  - 解決する課題: 保持上限12件と、UIから見える件数が一致していない
  - 概要: 履歴一覧の各回を選択可能にし、残り保持枠と未表示件数も表示する
  - ユーザー価値: 画面だけで過去回を見返せるため、復習と研修報告に使いやすい
  - 制約環境での実現性: 既存 `sessions` 配列を選択表示するだけで実現でき、外部依存も不要
  - 差別化ポイント: 外部LMSなしでも学習ログの説明力を持てる
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `clear-all-data` を真の削除に変更し、`v1` / `v2` / 移行判定キーが再読込後も残らないことを確認する。あわせて削除直後は自動再保存しない。
- 2. `reset-progress` と `clear-history` の影響範囲を整理し、進行中演習があるときは「現在の演習も失われる」ことを明示する。可能なら履歴消去はセッションだけを消す実装に分離する。
- 3. 演習中の分野チップをロックし、成績/履歴画面では任意セッションを選んで詳細表を切り替えられるようにする。最低限、保持上限12件と未表示件数は画面内に出す。

### 6. 監査メモ
- 今回の監査で未確認の点: Safari/Chrome 実機での `confirm` 表示差、実ブラウザでの `localStorage` 容量到達挙動、モーダル表示中に支援技術が背景要素へ到達するか
- 仮説のまま残った点: 履歴保持上限を12件へ下げた理由は、実装上は明記されていない。Node試算では全分野12セッションで約1.24MBだったため、保存サイズ対策の可能性はあるが、作者意図は未確認
- 次回追加で見るべき箇所: `clear-all-data` 後の移行モーダル再表示条件、進行中演習を保持したまま履歴だけ消せるか、履歴一覧の選択UI、画面内ヘルプ、770語分の例文未投入
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ
- 補足確認: `MustKnowCodingEnglish.html` のインラインスクリプトはローカル構文解析で通過した。実ブラウザのコンソールはこの環境では未確認

## 2026-04-27
### 0. エグゼクティブサマリー
- 今回の結論: 現行版は外部参照排除、保存不可時の一時利用モード、モーダルのフォーカス管理など前回までの高優先度改善が進んでいる。一方で、移行時の不可逆操作、履歴12件上限の黙殺、バックアップ導線不在が残っており、ローカル運用の信頼性と実務適合性をまだ下げている。
- 最優先課題: `migrate-reset` / `clear-all-data` / `migrate-keep` を同じ「データ管理境界」の問題として整理し、削除・初期化・引き継ぎの実態を文言と一致させる。
- 次回の狙い: まずデータ保全と再開性を整え、その上で開始導線と履歴導線の誤解を減らす。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 成績/履歴ビュー、演習ビューのヘッダーCTA、設定モーダル、旧データ移行モーダル
- 主な利用フロー: 旧 `v1` データ検出 -> 引き継ぐ/初期化を選択 -> 演習開始 -> 成績/履歴を見る -> 再開またはデータ削除 -> 再利用
- 想定ユーザー: 基本情報技術者試験向けの英語演習を、個人PCや管理端末でローカル完結運用したい学習者・研修担当者
- ツールの主目的: 単一HTMLで分野別4択演習と履歴確認を回し、ブラウザ内に学習状態を保持すること
- 確認した観点: 既存Issueの重複確認、旧データ移行の不可逆性、履歴保持上限の説明、成績画面からの開始導線、バックアップ不在、ローカル構文確認

### 2. 既存課題の確認結果
- 継続課題:
  - `20260424-01` 「すべてのデータを消去」が `v1` / `v2` / 移行判定キーを消さない
  - `20260424-02` 進捗リセットと履歴消去が進行中の演習も破棄する
  - `20260424-03` 演習中も分野チップが有効で、表示サマリーと出題分野が食い違う
  - `20260424-05` 保持12件のうち最大2件がUIから見えず、過去セッション詳細も再表示できない
  - `20260421-09` 画面内に使い方とデータ仕様の説明が不足している
- 更新した Issue:
  - `20260424-04`: 履歴12件上限は通常運用だけでなく、旧 `v1` データを「進捗を引き継ぐ」初回移行時にも無告知で適用されることを追加確認した。
  - `20260424-01`: `clear-all-data` が未解決のままに加え、現行版にはバックアップ/復元導線がないため、誤操作時の回復手段がさらに弱いことを追加確認した。
- 重複のため新規登録しなかった論点:
  - 演習中の分野チップ有効化は `20260424-03` の継続として扱い、新規化しなかった。
  - 画面内ヘルプ不足は `20260421-09` の継続として扱い、新規化しなかった。
  - 履歴の再表示不足は `20260424-05` の継続として扱い、新規化しなかった。

### 3. 発見した課題一覧

#### Issue: 20260427-01
- 状態: 更新
- 分類: UX / 改善
- タイトル: 履歴保持上限が12件に縮小されているが画面とREADMEで告知がない
- 種別: 事実
- 要約: この上限は日常利用時だけでなく、旧 `v1` データを「進捗を引き継ぐ」初回移行時にも即時適用される。移行モーダルにも README にもその説明がない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`, `README.md`
  - 関数名または要素名: `MAX_STORED_SESSIONS`, `_buildMigratedState()`, `#migration-modal`
  - 画面名: 旧データ移行モーダル、成績/履歴ビュー
  - 操作手順: 1. 旧 `v1` データに13件以上の履歴がある状態を想定 2. 初回起動で「進捗を引き継ぐ」を選ぶ 3. `_buildMigratedState()` が `slice(-MAX_STORED_SESSIONS)` を適用する 4. 古い履歴が無告知で落ちる
  - 該当箇所: `MustKnowCodingEnglish.html` 919-924行目の移行案内、944行目の `MAX_STORED_SESSIONS = 12`、9234-9244行目の移行時切り詰め、`README.md` 28-39行目に保持上限の記載なし
- 内容: 事実: 履歴上限は12件固定で、移行時にも同じ制限が働く。事実: 移行モーダルの文言は「進捗を引き継ぐ」とだけ書かれており、何件残るかを示していない。推論: 既存ユーザーほど「旧版の履歴も引き継がれる」と受け取りやすく、アップグレード時に履歴喪失へ気づきにくい。
- ユーザー影響: 旧版から移行した長期利用者が、アップグレード初回で古い履歴を失っても理由を説明できない。
- 制約環境での影響: ローカルHTML運用では履歴が唯一の証跡になりやすく、移行時の無告知切り捨てはそのまま運用事故になる。
- 期待効果: 引き継がれる件数を事前に理解した上で移行でき、履歴喪失の誤解を減らせる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 移行モーダルに「履歴は最新12件まで引き継ぎます」を明記する。可能なら保持件数を数えて、超過時だけ警告を出す。

#### Issue: 20260427-02
- 状態: 新規
- 分類: バグ / UX
- タイトル: 旧データ移行の「初期化して開始」が旧データを残したまま再選択不能にする
- 種別: 事実
- 要約: `migrate-reset` は旧 `v1` データを削除せず、移行判定キーだけを保存してモーダルを閉じる。誤って「初期化して開始」を押すと、旧データは残るのにアプリ側から再インポートできなくなる。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: `#migration-modal`, `_handleMigrationReset()`, `_setMigrationDecision()`
  - 画面名: 旧データ移行モーダル
  - 操作手順: 1. 旧 `v1` データがある初回起動を想定 2. モーダルで「初期化して開始」を押す 3. `_handleMigrationReset()` は初期状態を保存し移行判定キーを書くだけで `v1` を削除しない 4. 次回以降は「この確認は初回のみ表示されます」の通り再選択できない
  - 該当箇所: `MustKnowCodingEnglish.html` 921-924行目の文言、936-938行目の保存キー定義、9262-9265行目の移行判定保存、10003-10008行目の `_handleMigrationReset()`、コード全体に `removeItem` が存在しないことを確認
- 内容: 事実: UIは「初期化して開始」を1回限りの選択として扱う。事実: 実装は旧 `v1` データを削除せず、単に以後の移行確認を抑止する。推論: 誤操作や判断保留のまま初期化したユーザーは、保存されている旧データを持ちながらアプリ内からは復旧できない。
- ユーザー影響: 旧版の学習履歴を引き継ぐつもりだったユーザーが、1回の誤操作で実質的にアクセス不能な状態に陥る。
- 制約環境での影響: 管理端末や学習者端末では `localStorage` を手動編集できないことが多く、回復は実質不可能になる。
- 期待効果: 初期化の意味が明確になり、誤操作時の不可逆な取り返しのつかなさを下げられる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `migrate-reset` では本当に `v1` を削除するか、逆に `v1` が残る間は設定から「旧データを再読み込み」を実行できるようにする。最低限、モーダル文言に「この選択は元に戻せません」を追加する。

#### Issue: 20260427-03
- 状態: 新規
- 分類: UX / 改善
- タイトル: 成績/履歴画面の「演習を開始」が見えない分野設定で即実行される
- 種別: 事実
- 要約: ヘッダーの「演習を開始」は成績/履歴画面でも常に表示されるが、そこで使われる分野設定と問題数は演習画面側にしか見えていない。履歴を見た直後に押すと、想定外の100問/300問/800問演習を始めやすい。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: ヘッダーの `data-action="start-quiz"`, `#category-summary`, `#category-count`, `_getQuizPlan()`, `_startQuiz()`
  - 画面名: 成績/履歴ビュー、演習ビュー
  - 操作手順: 1. 演習画面で `全分野` を選ぶ 2. 成績/履歴へ移動する 3. 画面上では現在の分野と問題数が見えない 4. ヘッダーの「演習を開始」を押す 5. `_state.selectedCategory` に基づく全800問演習が始まる
  - 該当箇所: `MustKnowCodingEnglish.html` 725-730行目のヘッダーCTA、739-742行目の分野表示が演習ビュー内のみである点、9364-9369行目の `_getQuizPlan()`、9398-9427行目の `_startQuiz()`
- 内容: 事実: 成績/履歴ビューには開始対象の分野も問題数も表示されない。事実: それでもグローバルCTAは押せてしまい、内部では直前の `selectedCategory` を使って即開始する。推論: 履歴確認後に「軽く再開する」つもりで押したユーザーほど、長い演習を誤って始めやすい。
- ユーザー影響: 意図しない長時間セッションが始まり、短時間学習や直前確認の流れが途切れる。
- 制約環境での影響: 休憩時間や授業内の短時間運用では、開始対象が見えないCTAは実務上の誤操作になりやすい。
- 期待効果: 開始前の見通しが立ち、履歴確認から再開までの導線が素直になる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 成績/履歴ビューではヘッダーCTAを非表示にするか、「前提 100問を開始」のように分野名と問題数を明示する。最小対応なら押下時に演習画面へ戻してから確認付きで開始する。

#### Issue: 20260427-04
- 状態: 新規
- 分類: 追加機能 / UX
- タイトル: バックアップ/復元導線がなく、進捗がブラウザプロファイルに閉じる
- 種別: 事実
- 要約: 現行版の保存先は `localStorage` のみで、設定モーダルにもコード上にもエクスポート/インポート導線がない。ブラウザ移行、端末交換、削除前退避のいずれにも対応できない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`, `README.md`
  - 関数名または要素名: `STORAGE_KEY_V2`, 設定モーダルの `data-actions`
  - 画面名: 設定モーダル、READMEのデータ保存説明
  - 操作手順: 1. 設定モーダルを開く 2. 進捗リセット、履歴消去、全データ削除しか存在しない 3. コード検索でも `Blob`, `URL.createObjectURL`, `FileReader`, `type="file"`, export/import 系の処理が存在しない
  - 該当箇所: `README.md` 28-31行目で `localStorage` のみ説明、`MustKnowCodingEnglish.html` 904-909行目の設定操作、936-937行目の保存キー定義、コード検索で export/import 実装が見当たらないことを確認
- 内容: 事実: 現在の保存はブラウザプロファイル内だけで完結する。事実: 削除や移行の前にユーザーが自力で退避する公式導線はない。推論: ローカル完結を強みとして出すほど、持ち運び手段がないことが現場導入の弱点になる。
- ユーザー影響: ブラウザ初期化、PC交換、共有端末のプロファイル消去で学習進捗と履歴を一括喪失する。
- 制約環境での影響: 管理端末や研修PCではプロファイルが定期消去されやすく、外部クラウドも使えないため、バックアップ不在がそのまま運用不能要因になる。
- 期待効果: 進捗の持ち運びと退避が可能になり、ローカル運用の安心感が上がる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: まずはJSONのエクスポート/インポートを追加し、読み込み時に `version`, `theme`, `selectedCategory`, `statuses`, `sessions` を検証する。CSVより先に丸ごと退避を優先する。

#### Issue: 20260427-05
- 状態: 更新
- 分類: バグ / セキュリティ / UX
- タイトル: 「すべてのデータを消去」が `v1` / `v2` / 移行判定キーを消さない
- 種別: 事実
- 要約: 問題自体は前回から継続して未解決であり、現行版にはバックアップ/復元もないため、削除前退避も削除後回復もできないままになっている。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`, `README.md`
  - 関数名または要素名: `_clearAllData()`, `_handleAction()`, `STORAGE_KEY_V1`, `STORAGE_KEY_V2`, `MIGRATION_DECISION_KEY`
  - 画面名: 設定モーダル
  - 操作手順: 1. 設定モーダルで「すべてのデータを消去」を選ぶ 2. `_clearAllData()` はメモリ状態だけを初期化する 3. `_handleAction()` が直後に `_saveState()` を呼び `v2` を再保存する 4. 事前退避用の公式導線も存在しない
  - 該当箇所: `MustKnowCodingEnglish.html` 9980-9990行目の `_clearAllData()`、10056-10073行目の `clear-all-data` 後の共通保存、936-938行目の3キー定義、`README.md` 28-31行目の保存説明
- 内容: 事実: UIは「すべてのデータを消去」と書くが、実際には保存キー削除ではなく初期状態の再保存になっている。事実: 現行版には事前バックアップも事後復元もない。推論: データ管理操作として最も強い文言のボタンが、もっとも結果を読み違えやすい状態にある。
- ユーザー影響: 「完全削除」と信じて端末を渡したり、削除前に退避せず実行してしまったりする。
- 制約環境での影響: ローカルHTML運用ではブラウザストレージが唯一の保存実体であり、削除境界の曖昧さと回復不能性が同時に残るのは運用リスクが高い。
- 期待効果: 削除の意味と回復手段が明確になり、共有端末や再配布前の初期化作業に使いやすくなる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `clear-all-data` 実行時は `removeItem` で3キーを削除し、同アクションでは自動再保存を抑止する。バックアップ機能を後続追加するまでの暫定策としても、削除前に「現在の進捗を退避する手段はまだありません」を表示する。

### 4. 追加機能アイデア
- 機能名: 移行アシスタント
  - 解決する課題: 初期化と引き継ぎの境界が曖昧で、移行時に履歴切り捨てや不可逆操作が起きる
  - 概要: 旧データ件数、引き継ぎ後の保持件数、初期化の不可逆性をモーダル内で明示する
  - ユーザー価値: アップグレード時に何が残り何が消えるかを判断しやすい
  - 制約環境での実現性: 既存モーダルと `legacy.sessions.length` の集計だけで段階的に実装できる
  - 差別化ポイント: 単一HTMLでもバージョン移行の説明責任を持てる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: JSONバックアップ/復元
  - 解決する課題: 進捗がブラウザプロファイルに閉じ、削除前退避も端末移行もできない
  - 概要: 保存状態をJSONで出力し、読み込み時にバージョンと型を検証してから反映する
  - ユーザー価値: 端末交換、共有PC運用、削除前退避に対応できる
  - 制約環境での実現性: ローカルファイル入出力だけで完結し、外部通信を増やさない
  - 差別化ポイント: ローカル完結ツールでも継続利用の安心感を持てる
  - 実装コスト: 中
  - 優先度: 高
- 機能名: 開始コンテキスト付きCTA
  - 解決する課題: 成績/履歴画面から見えない分野設定で演習が始まる
  - 概要: ヘッダーCTAに現在の分野名と問題数を出すか、成績/履歴画面ではCTAを演習画面への遷移に限定する
  - ユーザー価値: 開始前に負荷感が分かり、誤操作を減らせる
  - 制約環境での実現性: 既存 `selectedCategory` と `_getQuizPlan()` の表示変更だけで実現できる
  - 差別化ポイント: 短時間学習向けツールとしての再開しやすさが上がる
  - 実装コスト: 小
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `migrate-reset` と `clear-all-data` を分けて整理し、初期化・削除・再インポート可否が文言と一致する状態にする。最低限、`removeItem` と不可逆説明のどちらを採るかを決める。
- 2. `migrate-keep` 前に「履歴は最新12件まで引き継ぐ」を表示し、上限超過があるときだけ警告する。並行してJSONバックアップ/復元の最小仕様を切る。
- 3. 成績/履歴画面の開始CTAを見直し、現在分野と問題数を見せるか、演習画面へ戻してから開始する形に変える。

### 6. 監査メモ
- 今回の監査で未確認の点: 旧 `v1` データ実サンプルでの移行件数表示、実ブラウザでの移行モーダル体験、管理端末でのローカルファイル入出力制限
- 仮説のまま残った点: `localStorage` 読み取りはできるが書き込みだけ失敗する環境では、移行判定キーが保存できず、初回モーダルが毎回出る可能性がある。ただし実機未再現のため仮説に留める。
- 次回追加で見るべき箇所: `20260424-03` の演習中分野チップ無効化、`20260424-05` の履歴詳細切替UI、`20260421-09` の画面内ヘルプ、JSON復元時の境界検証
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ
- 補足確認: `MustKnowCodingEnglish.html` のインラインスクリプトは抽出して `node --check` を通過した。Chromeヘッドレス起動はこの環境で異常終了したため、実ブラウザのコンソールエラー有無は未確認

## 2026-04-29
### 0. エグゼクティブサマリー
- 今回の結論: 現行版は単一HTML運用と保存不可時の一時利用モードを維持できているが、旧データ移行は「成功確認前に確定する」経路が残っており、壊れた保存データからの回復が弱い。加えて、進行中演習から履歴へ退避した時の見え方と、長い演習時の再描画負荷が実務利用の阻害要因になっている。
- 最優先課題: 旧 `v1` / `v2` が壊れているケースでも、移行モーダルを閉じる前に回復可能性を失わないよう、移行判定の書き込み条件を見直す。
- 次回の狙い: まず移行の安全性と保留導線を整え、その後に「途中経過の見え方」と「低性能端末での遅延」を抑える。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`
- 対象画面: 旧データ移行モーダル、演習中カード、成績/履歴ビュー、設定モーダル
- 主な利用フロー: 旧 `v1` / `v2` 保存データを持つ初回起動 -> 移行判断 -> 演習開始 -> 数問解答 -> 履歴ビューへ移動 -> 履歴削除または再開
- 想定ユーザー: 基本情報技術者試験の英語演習を、個人PC・研修PC・管理端末でローカル完結運用したい学習者と研修担当者
- ツールの主目的: 分野別の全問4択演習を単一HTMLで実行し、ブラウザ内に学習状態と直近履歴を保持すること
- 確認した観点: 既存Issue重複確認、壊れた保存データからの回復、進行中演習の見え方、履歴画面の状態整合、低性能端末での保存/再描画負荷、移行判断の保留可否

### 2. 既存課題の確認結果
- 継続課題:
  - `20260427-02` 旧データ移行の「初期化して開始」が旧データを残したまま再選択不能にする
  - `20260427-04` バックアップ/復元導線がなく、進捗がブラウザプロファイルに閉じる
  - `20260427-05` 「すべてのデータを消去」が `v1` / `v2` / 移行判定キーを消さない
  - `20260424-04` 履歴保持上限12件の告知不足
  - `20260424-05` 成績/履歴ビューが最新1件固定で、保持済み履歴や詳細を十分に見返せない
- 更新した Issue:
  - `20260424-05`: 直近履歴の詳細切替不足は既知だったが、今回は演習中カードに `成績/履歴へ` ボタンがあることで、進行中セッションでも「今の結果が見える」と誤解しやすいことを追加確認した。
- 重複のため新規登録しなかった論点:
  - 壊れた `v2` が移行モーダルを抑止する経路と、壊れた `v1` で「進捗を引き継ぐ」を押した時に空状態を確定する経路は、どちらも「移行成功前に回復経路を閉じる」同一問題として `20260429-01` に統合した。
  - 履歴削除や進捗リセットで進行中演習が消える論点は `20260424-02` の継続として扱い、今回は「削除結果をその場で確認できない」点だけを別論点に絞った。

### 3. 発見した課題一覧

#### Issue: 20260429-01
- 状態: 新規
- 分類: バグ / UX / 技術負債
- タイトル: 壊れた保存データに対する移行救済がなく、`v1` 回復経路が閉じる
- 種別: 事実
- 要約: `v2` が壊れていても `_detectMigrationCandidate()` は生文字列の存在だけで移行モーダルを抑止する。さらに `v1` が壊れた状態で「進捗を引き継ぐ」を押すと、初期状態を `v2` に保存して移行完了扱いにするため、再試行できない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: `_loadStateV2()`, `_loadLegacyStateV1()`, `_detectMigrationCandidate()`, `_handleMigrationKeep()`, `#migration-modal`
  - 画面名: 旧データ移行モーダル、起動時保存処理
  - 操作手順: 1. 壊れた `v2` と有効な `v1` が同居した状態で起動する 2. `_loadStateV2()` は初期状態と警告を返す 3. ただし `_detectMigrationCandidate()` は `_v2Raw` が非空のため `false` を返し、移行モーダルが出ない 4. 別経路として、壊れた `v1` の状態で移行モーダルの「進捗を引き継ぐ」を押す 5. `_loadLegacyStateV1()` は `null` を返すが、`_handleMigrationKeep()` はそのまま `_setMigrationDecision()` と `_saveState()` を実行し、空の `v2` を保存する
  - 該当箇所: `MustKnowCodingEnglish.html` 9110-9195行目の `v2` 読み込み、9198-9211行目の `v1` 読み込み、9270-9281行目の移行候補判定、9993-10000行目の引き継ぎ処理
  - 補助採証: Nodeで現行ロジックを模した確認では、`broken v2 + valid v1` で `migrationCandidate: false`、`broken v1 + migrate-keep` で `must-know-coding-english-state-v1`, `...-state-v2`, `...-migration-decided` の3キーが残ったまま `decisionValue: done` になった
- 内容: 事実: 移行候補判定は `v2` の「正しく読めるか」ではなく「文字列があるか」だけを見る。事実: `migrate-keep` は `v1` 読み込み成功を確認せずに移行完了フラグを書き込む。推論: 保存データが壊れた利用者ほど、本来必要な救済導線が最初に閉じられる。
- ユーザー影響: 起動時に進捗が消えたように見え、旧データが残っていてもアプリ内から回復できない。
- 制約環境での影響: 管理端末や配布PCでは `localStorage` を手動で修復しにくく、一度回復経路を失うと再配布やサポート対応が必要になる。
- 期待効果: 壊れた保存データに遭遇しても、回復・初期化・保留のどれを取るかを利用者が判断できる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: `_detectMigrationCandidate()` は `v2` のパース成功後だけ移行抑止する。`_handleMigrationKeep()` は `v1` のパース成功と `v2` 保存成功を確認してから `MIGRATION_DECISION_KEY` を書き込む。失敗時はモーダルを閉じず、「旧データを読めませんでした。初期化するか後で再試行してください」を表示する。

#### Issue: 20260429-02
- 状態: 更新
- 分類: UX / 改善
- タイトル: 成績/履歴ビューが最新完了セッション固定で、進行中演習から入ると現在結果が見えない
- 種別: 事実
- 要約: 演習中カードには `成績/履歴へ` ボタンがあるが、履歴ビューは常に `_getLatestSession()` だけを描画する。未完了の現在セッションは反映されず、空状態または直前の完了セッションだけが見える。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: 演習中の `data-action="switch-view" data-view="history"`, `_renderHistory()`, `_getLatestSession()`
  - 画面名: 演習中カード、成績/履歴ビュー
  - 操作手順: 1. 分野を選んで演習を開始し、数問回答する 2. 演習中カードの `成績/履歴へ` を押す 3. 初回セッションなら「まだ演習結果がありません」が出る 4. 過去セッションがある場合は、現在ではなく直前完了セッションの KPI と表だけが表示される
  - 該当箇所: `MustKnowCodingEnglish.html` 806-808行目のボタン、9781-9804行目の空状態描画、9806-9832行目の最新完了セッション描画、9357-9361行目の `_getLatestSession()`
- 内容: 事実: UIには「履歴を見る」ではなく「成績/履歴へ」と書かれている。事実: その遷移先は現在の `_state.quiz.answers` を一切参照しない。推論: 100問や300問の途中で中断確認したい利用者ほど、「今の結果が消えた」と受け取りやすい。
- ユーザー影響: 長い演習の途中経過を確認できず、再開時にどこまで進んだかの見通しを失う。
- 制約環境での影響: 授業時間や休憩時間の中断再開では、現在セッションの扱いが分からないだけで利用継続の負荷が上がる。
- 期待効果: 「完了済み履歴」と「進行中の演習」を分けて理解でき、途中離脱時の不安を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `_state.quiz.active && _state.quiz.answers.length > 0` のときは、履歴ビュー上部に暫定KPIと「現在の演習は未完了のため保存履歴には未反映です」を出す。最小対応ならボタン文言を `履歴を見る（現在の演習結果は完了後に反映）` に変える。

#### Issue: 20260429-03
- 状態: 新規
- 分類: パフォーマンス / UX
- タイトル: すべての操作で全状態保存と非表示の履歴再描画が走り、長い演習で遅延しやすい
- 種別: 推論
- 要約: `set-category`, `switch-view`, `open-settings`, `quiz-answer` など多くの操作が `_saveState()` と `_render()` を毎回実行する。`_render()` は現在ビューに関係なく `_renderHistory()` を呼ぶため、最新セッションが大きいほど不要なDOM再構築と `localStorage` 書き込みが増える。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: `_handleAction()`, `_saveState()`, `_render()`, `_renderHistory()`
  - 画面名: 演習ビュー、成績/履歴ビュー、設定モーダル
  - 操作手順: 1. `全分野` を完了して800回答の最新セッションを作る 2. 次の演習を開始して回答または分野切替を行う 3. 画面は演習ビューのままでも `_saveState()` と `_renderHistory()` が走り、最新800行の履歴表を再構築する
  - 該当箇所: `MustKnowCodingEnglish.html` 10012-10073行目の共通保存・再描画、10076-10082行目の全画面描画、9781-9832行目の履歴表再構築、9250-9258行目の状態保存
  - 補助採証: Node試算では、全800語の進行中セッションを半分まで解いた状態の `JSON.stringify(_state)` は約203,744 bytes、履歴12件保持時の状態は約1,312,794 bytes だった
- 内容: 事実: 保存と描画の呼び出しは操作種別で細分化されていない。事実: 履歴表は非表示時も毎回作り直される。推論: 低性能端末では、回答のたびに保存と履歴再描画が重なることで、クリック反応やキーボード操作が重くなる可能性が高い。
- ユーザー影響: 4択のテンポが落ちると、短時間演習の継続率が下がる。
- 制約環境での影響: 研修PC、節電モード、古いブラウザでは、1HTMLでも反応遅延がそのまま教材品質の評価につながる。
- 期待効果: 保存が必要な時だけ保存し、見えている画面だけ描画することで、長い演習でも軽さを保てる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 中
- 次の行動: 第1段階として、`_render()` をビュー単位に分けて履歴ビュー非表示時は `_renderHistory()` を呼ばない。あわせて `switch-view` と `open-settings` / `close-settings` では `_saveState()` を省き、回答・設定変更・削除操作だけ保存する。

#### Issue: 20260429-04
- 状態: 新規
- 分類: UX / 改善
- タイトル: 旧データ移行モーダルに保留と退避後判断の導線がない
- 種別: 事実
- 要約: 旧データ移行モーダルには `初期化して開始` と `進捗を引き継ぐ` の2択しかない。文言は「この確認は初回のみ表示」となっており、判断保留やバックアップ後の再判断ができない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: `#migration-modal`, `data-action="migrate-reset"`, `data-action="migrate-keep"`, keydown handler
  - 画面名: 旧データ移行モーダル
  - 操作手順: 1. 旧 `v1` データがある状態で初回起動する 2. モーダルには2ボタンしか表示されない 3. `Escape` では閉じられず、背景クリックでのクローズもない 4. どちらかを選ぶまで先へ進めない
  - 該当箇所: `MustKnowCodingEnglish.html` 915-925行目のモーダル構造、921行目の「初回のみ」文言、10143-10145行目で `Escape` は設定モーダルしか閉じない点
- 内容: 事実: 利用者は移行内容を確認する前に2択を迫られる。事実: その場でバックアップを取るUIも、後で決めるUIもない。推論: READMEや運用手順を読まずに開く利用者ほど、時間制約の中で不可逆な判断をしやすい。
- ユーザー影響: 初回起動時に判断を誤ると、履歴切り捨てや初期化を意図せず受け入れやすい。
- 制約環境での影響: 管理端末では `localStorage` 内容を外で確認しづらく、「まず保留」ができない設計は運用導入に不向き。
- 期待効果: 移行判断を急がせず、データ保全を優先した導入手順を取りやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `後で決める` か `バックアップ後に再開` の導線を追加し、その経路では `MIGRATION_DECISION_KEY` を書かない。最低限、モーダル内に「判断を保留したい場合はこのファイルを閉じてからバックアップしてください」を表示する。

#### Issue: 20260429-05
- 状態: 新規
- 分類: UX / 改善
- タイトル: 履歴削除後に履歴画面へ留まれず、削除結果をその場で確認できない
- 種別: 事実
- 要約: `clear-history` は履歴を空にした後、履歴ビュー表示中なら自動で演習ビューへ戻す。履歴画面には空状態UIがあるのに、削除成功の確認をその場でできない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: `_clearHistory()`, `_switchView("practice", false)`, `_renderHistory()`
  - 画面名: 成績/履歴ビュー、設定モーダル
  - 操作手順: 1. 成績/履歴ビューを開く 2. 設定から `履歴の消去` を実行する 3. `_state.sessions = []` の直後に `history` 表示中なら `practice` へ切り替える 4. 履歴が空になった画面を見ないまま演習ビューへ戻される
  - 該当箇所: `MustKnowCodingEnglish.html` 9966-9977行目の `_clearHistory()`、9789-9802行目の履歴空状態描画
- 内容: 事実: 削除後に表示されるべき「履歴なし」状態は実装されている。事実: しかし削除フロー自体が履歴画面から離脱させる。推論: 共有端末で履歴を消してから引き渡す運用では、利用者が「本当に空になったか」を追加操作なしで確認したい。
- ユーザー影響: 削除が成功したか分からず、同じ操作を繰り返したり、引き渡し前確認に手間が増えたりする。
- 制約環境での影響: 研修PCや共用PCでは、短時間で初期化確認を済ませたい場面が多く、確認導線の遠さが運用ミスにつながる。
- 期待効果: 履歴削除の成否を即確認でき、共有端末のクリーンアップ作業が簡単になる。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: 履歴削除後は `history` ビューに留まり、既存の空状態を表示する。進行中演習を巻き込む場合の警告は `20260424-02` と合わせて別途明記する。

### 4. 追加機能アイデア
- 機能名: 移行リカバリーモード
  - 解決する課題: 壊れた `v1` / `v2` 保存データからの回復経路が弱い
  - 概要: `v2` 読み込み失敗時に `v1` の再検査、初期化、保留を選べる復旧モーダルを出す
  - ユーザー価値: 進捗消失時でも、その場で回復策を選べる
  - 制約環境での実現性: 既存の移行モーダルと `_storageLoadWarning` を拡張するだけで段階的に実装できる
  - 差別化ポイント: 単一HTMLでも壊れた保存データに対する説明責任を持てる
  - 実装コスト: 中
  - 優先度: 高
- 機能名: 途中経過サマリー
  - 解決する課題: 進行中演習から履歴へ入ると現在セッションが見えない
  - 概要: 未完了セッションの正解数、ミス数、現在位置を履歴ビュー上部に暫定表示する
  - ユーザー価値: 長い演習を中断しても、再開判断がしやすい
  - 制約環境での実現性: 既存の `_state.quiz.answers` から計算でき、保存形式を増やさず導入できる
  - 差別化ポイント: 1HTML教材でも「途中で閉じても進捗が見える」安心感を出せる
  - 実装コスト: 小
  - 優先度: 中
- 機能名: 軽量レンダリングモード
  - 解決する課題: 全操作で全状態保存と非表示履歴再描画が走る
  - 概要: 現在ビューだけを描画し、保存対象操作を回答・設定変更・削除系に限定する
  - ユーザー価値: 低性能端末でも回答テンポが落ちにくい
  - 制約環境での実現性: 単一HTML内の描画関数分離だけで着手でき、外部依存を増やさない
  - 差別化ポイント: 大量語彙でも軽く動く教材として配布しやすい
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `_detectMigrationCandidate()` と `_handleMigrationKeep()` を見直し、壊れた `v2` / `v1` でも回復・保留・初期化を選べる移行境界にする。
- 2. 演習中から履歴へ移動した時の見え方を直し、未完了セッションの暫定KPIか注意文を追加する。あわせて履歴削除後は空状態を同画面で確認できるようにする。
- 3. `_render()` と `_handleAction()` の責務を軽くし、非表示履歴の再描画と不要な `localStorage` 保存を止める。最初の検証対象は `switch-view`, `open-settings`, `close-settings`。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでの回答レイテンシ実測、実サンプル `v1` データを使った移行体験、Chrome/Edge 系での `file://` 起動時コンソールログ
- 仮説のまま残った点: 保存サイズ試算とコード経路から遅延リスクは高いが、何ms悪化するかは実機未計測のため仮説に留める
- 次回追加で見るべき箇所: `20260427-04` のバックアップ/復元仕様、`20260424-04` の12件上限告知、`20260424-05` の履歴詳細切替UI、移行後に旧 `v1` を削除するかどうかの保存容量影響
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ
- 補足確認: in-app browser の `iab` backend はこの環境で利用できず、Chrome headless も異常終了したため、画面挙動の一部はコード経路確認とNodeでの状態モデル確認で裏取りした

## 2026-04-30
### 0. エグゼクティブサマリー
- 今回の結論: 現在の最重要課題はUI改善ではなく、本体 [`MustKnowCodingEnglish.html`](/Users/arita-ritsuki/Documents/プログラミング/個人開発/Arts1-ToolBox/MustKnowCodingEnglish/MustKnowCodingEnglish.html) が `0 bytes` になっており、ツール自体が起動不能なこと。4月29日まで監査していた学習フローや保存挙動は、この状態では再検証できない。
- 最優先課題: `MustKnowCodingEnglish.html` を非空の起動可能状態へ戻し、少なくとも `<!DOCTYPE html>` を持つHTMLシェルと主要UIが再表示されることを先に確認する。
- 次回の狙い: 本体復旧後に、4月29日時点の継続課題を優先度順で再監査し直す。特に `20260429-01`、`20260429-02`、`20260429-03`、`20260427-04` を再開対象にする。
- 今回の監査では実装は行っていない: 行っていない。今回追記したのは `REPORT.md` のみ。

### 1. 今回の監査対象
- 対象ファイル: `REPORT.md`, `README.md`, `MustKnowCodingEnglish.html`, `git diff` 上の `MustKnowCodingEnglish.html`
- 対象画面: 起動画面、READMEの利用手順、配布対象ファイル構成
- 主な利用フロー: `README.md` の手順に従って `MustKnowCodingEnglish.html` を開く -> 現行ワークツリーでは本体ファイルが空で、学習画面・履歴画面・設定画面まで到達できない
- 想定ユーザー: 基本情報技術者試験向けの英語演習を、個人PC・研修PC・管理端末でローカル完結運用したい学習者と研修担当者
- ツールの主目的: 分野別の4択演習と履歴確認を単一HTMLで完結させること
- 確認した観点: 起点HTMLの実在性、READMEと現物の整合、配布構成、過去Issueとの重複有無、制約環境での復旧可能性

### 2. 既存課題の確認結果
- 継続課題:
  - `20260429-01` 壊れた保存データに対する移行救済がなく、`v1` 回復経路が閉じる
  - `20260429-02` 成績/履歴ビューが最新完了セッション固定で、進行中演習から入ると現在結果が見えない
  - `20260429-03` すべての操作で全状態保存と非表示の履歴再描画が走り、長い演習で遅延しやすい
  - `20260427-04` バックアップ/復元導線がなく、進捗がブラウザプロファイルに閉じる
- 更新した Issue:
  - なし。4月29日までのUI・状態遷移・保存周辺の課題は、本体HTML欠落のため再検証保留とした。
- 重複のため新規登録しなかった論点:
  - 「開くと空白になる」「学習画面に入れない」「ボタンや保存機能を試せない」は、根本原因が同じため `20260430-01` に集約した。
  - READMEの誤誘導と配布前チェック不足は関連するが、前者はユーザー向け説明の問題、後者は再発防止の運用問題なので分けて記録した。
  - 既存のアクセシビリティ・CSV・履歴UI課題は、現行ワークツリーでは対象画面自体が存在しないため新規更新しなかった。

### 3. 発見した課題一覧

#### Issue: 20260430-01
- 状態: 新規
- 分類: バグ / UX
- タイトル: 本体 `MustKnowCodingEnglish.html` が `0 bytes` で、ツールが起動しない
- 種別: 事実
- 要約: 現在のワークツリーでは起点HTMLが空ファイルになっている。READMEが案内する学習画面・履歴画面・設定画面まで到達できず、改善候補の検証以前にツール利用が止まる。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`
  - 関数名または要素名: なし。ファイル全体が空で、DOM・CSS・JavaScriptの定義が存在しない
  - 画面名: 起動画面
  - 操作手順: 1. リポジトリ直下のファイルを確認する 2. `MustKnowCodingEnglish.html` の行数とサイズを確認する 3. 同ファイルが `0 lines / 0 bytes` であることを確認する
  - 該当箇所: ファイル全体。`wc -l MustKnowCodingEnglish.html` は `0`、`file MustKnowCodingEnglish.html` は `empty`、`ls -l` は `0 bytes`。`git diff -- MustKnowCodingEnglish.html` では `@@ -1,10162 +0,0 @@` と出ており、前回監査対象だったHTML全体が消えている。`git show HEAD:MustKnowCodingEnglish/MustKnowCodingEnglish.html` では `<!DOCTYPE html>` とアプリのUI/CSS定義が残っている
- 内容: 事実: 現行ファイルは空であり、単一HTMLツールとして成立していない。事実: 現行ディレクトリには代替の起点HTMLも存在しない。推論: ワークツリー上の偶発的な truncate か未保存状態の混入である可能性が高いが、原因自体は今回未特定。
- ユーザー影響: 学習開始、保存、履歴確認、設定変更、移行確認のすべてが実行不能になる。
- 制約環境での影響: ローカル配布・オフライン配布では単一HTMLが唯一の実体なので、空ファイル化はそのまま全面停止になる。
- 期待効果: 本体を復旧できれば、4月29日まで積み上げた改善課題を再度実機レベルで評価し直せる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: `HEAD:MustKnowCodingEnglish/MustKnowCodingEnglish.html` と現行ワークツリーを比較し、意図した変更があるかを切り分けたうえで、まず非空の起動可能HTMLへ戻す。復旧後は「起動」「演習開始」「履歴表示」の3操作だけを先に確認する。

#### Issue: 20260430-02
- 状態: 新規
- 分類: UX / 訴求 / 技術負債
- タイトル: READMEの利用手順と現物が乖離し、配布先で空白画面を正常と誤解させる
- 種別: 事実
- 要約: READMEは単一HTMLの演習ツールとして具体的な画面構成・保存仕様・利用手順を記載しているが、現物の起点HTMLは空である。利用者は「読み込み失敗なのか、想定仕様なのか」をREADMEだけでは判断できない。
- 証拠:
  - 対象ファイル名: `README.md`, `MustKnowCodingEnglish.html`
  - 関数名または要素名: `README.md` の「ファイル」「画面構成」「演習仕様」「データ保存」「使い方」節
  - 画面名: README上の利用案内、起動画面
  - 操作手順: 1. `README.md` を読む 2. 35-39行目の手順どおり `MustKnowCodingEnglish.html` を開く 3. 画面構成・保存状態表示・設定モーダルなど、READMEにある要素へ到達できない
  - 該当箇所: `README.md` 3-39行目で「単一HTMLで動作」「演習・成績/履歴・設定を統合した本体」「保存不可環境では一時利用モード」「外部通信なし」「ブラウザで開く」と説明。対して `MustKnowCodingEnglish.html` は `0 bytes`。同階層のファイルは `MustKnowCodingEnglish.html`, `README.md`, `REPORT.md` の3件のみ
- 内容: 事実: READMEはかなり具体的な使用説明を持つ。事実: 現物はその説明を満たしていない。推論: 初見ユーザーは自分のブラウザ環境かファイル破損かを切り分けられず、サポート依頼や離脱が起きやすい。
- ユーザー影響: ダウンロード直後の第一印象が空白画面になり、ツール価値の判断前に信頼を失う。
- 制約環境での影響: 社内端末や研修PCではインターネット検索や再ダウンロードに頼りにくく、READMEと実体の不整合がそのまま運用停止になる。
- 期待効果: 本体復旧後にREADMEを実体に合わせて棚卸しすれば、配布後の誤解とサポート工数を減らせる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 本体復旧後に `README.md` 5-6行目、8-32行目、34-39行目を現物と照合し、「今このファイルを開けば何ができるか」を検証済みの内容だけに絞る。復旧前の配布は止める。

#### Issue: 20260430-03
- 状態: 新規
- 分類: 技術負債 / 改善
- タイトル: 配布前の本体整合チェックがなく、`0 byte` 本体でも監査対象に残る
- 種別: 推論
- 要約: 空の起点HTML、機能を説明するREADME、継続監査中のREPORTが同時に存在している。少なくともこのスナップショットでは、本体欠落を止める有効な整合チェックが働いていない。
- 証拠:
  - 対象ファイル名: `MustKnowCodingEnglish.html`, `README.md`, `REPORT.md`
  - 関数名または要素名: 配布対象ファイル全体
  - 画面名: 配布前確認フロー
  - 操作手順: 1. 現行ディレクトリのファイル一覧を確認する 2. `MustKnowCodingEnglish.html` が空であることを確認する 3. それでも `README.md` は利用手順を保持し、`REPORT.md` は4月29日まで通常監査を継続していることを確認する
  - 該当箇所: `find . -maxdepth 1 -type f` では `MustKnowCodingEnglish.html`, `README.md`, `REPORT.md` のみ。`MustKnowCodingEnglish.html` は空。`README.md` 34-39行目は利用手順を保持。`REPORT.md` 2235行目以降の4月29日分は起動・履歴・移行・描画の詳細監査を継続している
- 内容: 事実: この監査対象スナップショットには空の本体と通常運用を前提にした文書が同居している。推論: 本体のファイルサイズや先頭行を確認する最低限の配布前チェックがないか、あっても現状の運用では効いていない。推論: 単一HTML配布のプロダクトでは、この種の破損をUI改善より先に止める必要がある。
- ユーザー影響: 同じ欠落状態のままZIP配布やレビュー依頼を繰り返すと、ユーザーは毎回空白画面を受け取る。
- 制約環境での影響: ローカル完結配布では、1ファイル破損を配布後に自動修復できないため、事前検知の価値が高い。
- 期待効果: 低コストの整合チェックを先に置くことで、今後の監査や改善提案が「起動可能な実体」を前提に進められる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: ビルド導入ではなく、まずは配布前チェックを1回分の実装で追加する。具体的には `test -s MustKnowCodingEnglish.html`、先頭行に `<!DOCTYPE html>` があること、ブラウザでヘッダーが見えることの3点だけを機械または手順書で固定する。

### 4. 追加機能アイデア
- 機能名: 起動時セルフチェック画面
  - 解決する課題: 起点HTMLが壊れた時に、利用者が空白画面と読み込み失敗を切り分けられない
  - 概要: 本体復旧後、初期化失敗時には「データ破損」「保存不可」「必要ファイル欠落」を明示するフェールセーフ表示を最優先で出す
  - ユーザー価値: 空白画面のまま止まらず、次に何を確認すべきかが分かる
  - 制約環境での実現性: 単一HTMLのまま実装でき、外部依存を増やさない
  - 差別化ポイント: ローカル配布前提でも、障害時の説明責任を持てる
  - 実装コスト: 小
  - 優先度: 高
- 機能名: 配布前サニティチェック
  - 解決する課題: 空の本体がREADME付きで残っても検知しにくい
  - 概要: ファイルサイズ、DOCTYPE、起動画面確認の3点だけを通す軽量な出荷確認フローを持つ
  - ユーザー価値: 壊れた配布物を受け取るリスクが減る
  - 制約環境での実現性: ビルドや外部サービスなしで導入できる
  - 差別化ポイント: 単一HTMLプロダクトの運用事故を小さくできる
  - 実装コスト: 小
  - 優先度: 高

### 5. 次回おすすめタスク
- 1. `MustKnowCodingEnglish.html` を復旧し、少なくとも `<!DOCTYPE html>`、ヘッダー、主要ビュー切替が表示される状態へ戻す。
- 2. 復旧直後に `README.md` のファイル説明・画面構成・使い方を現物と照合し、配布前に事実とずれる記述を残さない。
- 3. 配布前サニティチェックとして「非空ファイル」「DOCTYPE存在」「ブラウザ起動確認」の3項目を固定し、今後の監査前提を守る。

### 6. 監査メモ
- 今回の監査で未確認の点: 復旧後の学習フロー、履歴画面、移行モーダル、CSV出力、保存失敗時の一時利用モード
- 仮説のまま残った点: `0 bytes` 化の原因がエディタ操作か途中保存か意図的なリセットかは未特定
- 次回追加で見るべき箇所: 本体復旧後に `20260429-01`, `20260429-02`, `20260429-03`, `20260427-04` を優先して再監査する
- 今回の監査では実装していないこと: アプリ本体、README、設定、画像、テスト、ビルド設定は変更していない
- 今回変更したファイル: `REPORT.md` のみ
- 補足確認: in-app browser の `iab` backend はこの環境で利用できず、視覚確認はできなかった。今回はファイル実体、README、`git diff`、`HEAD` 上の前回HTMLを証拠として優先した
