# REPORT

## 2026-04-27
### 0. エグゼクティブサマリー
- 今回の結論: 単一 HTML で Trello 風操作を広くカバーしている一方、実務運用では `複製時の整合性` `保存失敗時の復旧性` `構造不正データの防御` が不足しており、データ欠損や空画面につながる経路が残っている。
- 最優先課題: `20260427-01` ボード複製時のラベル欠落、`20260427-02` インポート時の構造検証不足、`20260427-03` 保存容量超過時の状態乖離。
- 次回の狙い: まずデータ破損・欠損に直結する箇所を 1 回の実装で塞ぎ、その次に制約環境での配布耐性と誤操作回復を整える。
- 今回の監査では実装は行っていない: はい。`REPORT.md` の記録のみ実施。

### 1. 今回の監査対象
- 対象ファイル: `Local-Trello.html`。`README.md` は未作成のため確認対象なし。`REPORT.md` は今回新規作成。
- 対象画面: トップバー、ボード一覧ドロワー、リスト/カード一覧、カード詳細モーダル、ラベル/期限/添付/カバーの各ポップオーバー、インポート/エクスポート導線。
- 主な利用フロー: 初回表示 -> ボード選択/作成 -> リスト追加 -> カード追加/編集 -> ラベル/期限/添付設定 -> 検索/フィルター -> アーカイブ -> JSON エクスポート/インポート。
- 想定ユーザー: ローカル完結で個人タスクや小規模案件を管理したい単独利用者。テンプレート複製、週次運用、オフライン配布を行う利用者も含む。
- ツールの主目的: Trello 風のボード管理をブラウザ `localStorage` と JSON 入出力だけで完結させること。
- 確認した観点: データ整合性、保存/読込、入力検証、誤操作回復、状態遷移、アクセシビリティ、オフライン運用、価値訴求不足、制約環境での配布適性。

### 2. 既存課題の確認結果
- 継続課題: なし。`REPORT.md` が未作成だったため、今回が初回登録。
- 更新した Issue: なし。
- 重複のため新規登録しなかった論点: ラベル操作の文言ずれは `20260427-06` に統合。保存失敗後のデータ消失と再描画時の再保存は `20260427-03` と `20260427-04` に分離して管理。

### 3. 発見した課題一覧

#### Issue: 20260427-01
- 状態: 新規
- 分類: バグ
- タイトル: ボード複製でカードのラベル参照が切れる
- 種別: 事実
- 要約: ボード複製処理が `labels` の ID だけを再採番し、各カードの `labelIds` を新 ID に差し替えていない。複製後のボードでラベル表示とラベルフィルターが壊れる。
- 証拠: `Local-Trello.html:2855-2863` のボード複製処理で `copy.labels.forEach(l => l.id = uid())` を実行しているが、カード側の `labelIds` 更新がない。`Local-Trello.html:1376-1377` と `1794` は `board.labels.find(...)` で描画しているため、旧 ID は解決できない。操作手順: ボード一覧 -> 対象ボードをコピー -> コピー先を開く -> 既存カードのラベル表示とラベル絞り込みを確認。
- 内容: 週次テンプレートや案件複製の主要導線で、分類情報だけが欠落する。コピー自体は成功して見えるため、利用者が欠落に気付きにくい。
- ユーザー影響: 複製後の整理状態を維持できず、再度ラベル付けが必要になる。ラベルで回す運用では複製価値が大きく下がる。
- 制約環境での影響: ローカル単体運用ではテンプレート複製が代替手段になりやすく、この欠陥がそのまま定型業務の手戻りになる。
- 期待効果: テンプレート複製後もカード分類とラベルベース運用を維持できる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: ボード複製時に `oldLabelId -> newLabelId` の対応表を作り、複製ボード内の全カード `labelIds` を一括で差し替える。

#### Issue: 20260427-02
- 状態: 新規
- 分類: バグ
- タイトル: インポート時の構造検証が弱く空画面や起動不能状態を保存できる
- 種別: 事実
- 要約: インポートは `boards` 配列の存在だけを確認して `state = data` を実行している。`currentBoardId` や `lists` `labels` が欠けた JSON でも保存され、空画面や描画失敗を起こしうる。
- 証拠: `Local-Trello.html:1128-1135` の `loadState()` は `parsed.boards` の有無しか見ていない。`Local-Trello.html:2952-2966` の `importData()` も `boards` 配列だけ確認してそのまま保存する。操作手順: `{"boards":[{"id":"b1","name":"x"}],"currentBoardId":"b1"}` のような不完全 JSON をインポート。
- 内容: JSON 形式が「配列はあるが内部形状が欠けている」ケースを弾けない。保存後に再読込しても migration が働かず、壊れた状態が残る。
- ユーザー影響: 既存データを上書きした直後に空画面または描画エラーになると、手元バックアップがなければ復旧が難しい。
- 制約環境での影響: オフライン配布や手動バックアップでは JSON の手編集・他ブラウザ間移動が起きやすく、構造検証不足がそのまま事故率を上げる。
- 期待効果: 壊れた JSON の流入を保存前に遮断し、復旧不能な上書きを防げる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: インポート前に `currentBoardId` `boards[].lists` `boards[].labels` など必須構造を検証し、不足項目は拒否または明示的 migration を通してから保存する。

#### Issue: 20260427-03
- 状態: 新規
- 分類: バグ
- タイトル: 保存容量超過時に画面状態と永続化状態が乖離する
- 種別: 推論
- 要約: 添付やカバー画像を data URL で `localStorage` に入れており、容量超過時は `saveState()` が toast を出すだけで変更を巻き戻さない。保存に失敗した変更が画面内だけ残り、再読込で消える恐れがある。
- 証拠: `Local-Trello.html:1142-1148` は保存失敗時に `showToast('保存に失敗しました')` だけを実行する。`Local-Trello.html:2504-2524` と `2611-2624` は 3MB 以下なら data URL を state に格納する。操作手順: 大きめ画像を複数枚追加し続けて `saveState failed` 経路に入れる。今回の監査では実機再現は未実施。
- 内容: 1 ファイルごとの上限はあるが、総量上限の見積りがない。失敗後も state は書き換わったままなので、利用者は「保存済み」と誤認しやすい。
- ユーザー影響: 添付を含む案件ほど、再読込時に直前作業が失われる危険が高い。
- 制約環境での影響: 低容量ブラウザ、共有 PC、ストレージ制限付き環境では quota 到達が早く、ローカル完結運用の信頼性を損なう。
- 期待効果: 保存不能状態を事前に止め、作業消失を防げる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 中
- 次の行動: 添付/カバー追加前に保存サイズを概算し、閾値超過見込みなら state を変更せず中断する。保存失敗時のロールバック方針も併せて決める。

#### Issue: 20260427-04
- 状態: 新規
- 分類: パフォーマンス
- タイトル: 再描画のたびに全 state を保存しており大きいボードで重くなりやすい
- 種別: 事実
- 要約: `renderAll()` の最後で毎回 `saveState()` を呼んでいるため、検索入力やモーダルの開閉でも `localStorage` 全体の再シリアライズが走る。
- 証拠: `Local-Trello.html:1198-1221` の `renderAll()` 末尾に `saveState()` がある。`Local-Trello.html:3363-3368` の検索入力や `1781-1785` のモーダル終了など、非データ変更操作でも `renderAll()` が呼ばれる。
- 内容: 描画関数が永続化責務まで持っており、状態変更の有無に関係なく同期 I/O が発生する。容量が増えるほど検索やフィルターの反応が落ちる構造になっている。
- ユーザー影響: カード数や添付が増えた後に、検索やモーダル操作のレスポンスが鈍くなる可能性が高い。
- 制約環境での影響: 低速端末や省電力ブラウザでは同期 `localStorage` 書き込みが体感差になりやすい。
- 期待効果: 入力反応を落とさず、保存コストを実際の変更時だけに限定できる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: `renderAll()` から保存呼び出しを外し、state を変更した関数だけが明示的に保存する形へ責務を戻す。

#### Issue: 20260427-05
- 状態: 新規
- 分類: バグ
- タイトル: ボード名変更後にドロワーが二重生成される可能性が高い
- 種別: 事実
- 要約: ボード一覧ドロワー内の rename 処理で `openBoardsDrawer() -> closeDrawer() -> renderAll() -> openBoardsDrawer()` を実行しており、ドロワーとバックドロップが重複生成されるコードになっている。
- 証拠: `Local-Trello.html:2846-2849`。同関数の `closeDrawer()` は `document.querySelector('.drawer')` と `'.drawer-backdrop'` の先頭要素しか消さないため、複数生成時に取り残しが発生しうる。今回の監査では UI 実機再現は未実施。
- 内容: 一覧を最新化したい意図は読めるが、開く/閉じる順序が逆転している。重なったオーバーレイはクリック不能や閉じ忘れの原因になる。
- ユーザー影響: 名前変更後に画面が重なって見えたり、背後クリックで意図しない閉じ方をする可能性がある。
- 制約環境での影響: 小さい画面やタッチ操作ではオーバーレイ重複の復帰が難しい。
- 期待効果: ボード管理の基本導線が安定し、一覧更新時の副作用を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: rename 後の一覧更新は `closeDrawer()` 後に 1 回だけ再描画し、必要なら最後に `openBoardsDrawer()` を 1 回だけ呼ぶ構成へ整理する。

#### Issue: 20260427-06
- 状態: 新規
- 分類: UX
- タイトル: 期限なしカードでも完了操作が可能で見えない状態が生まれる
- 種別: 事実
- 要約: カードモーダルのクイックアクションに完了ボタンが常時表示され、期限がないカードでも `dueComplete` を切り替えられる。カード面には表示されないため、状態が見えない。
- 証拠: `Local-Trello.html:1873-1880` は期限の有無に関係なく完了ボタンを描画する。`Local-Trello.html:1555-1558` は単純に `dueComplete` を反転し、`1516-1517` のフィルターはこの値だけを見る。`1438-1464` のカード表示は `dueDate` がないと完了状態を出さない。操作手順: 期限未設定カードを開く -> 完了ボタンを押す -> 一覧上の見た目とフィルター結果を確認。
- 内容: 期限管理の概念と完了管理の概念がずれており、利用者が自分で作った状態を一覧上で追えない。
- ユーザー影響: 完了フィルターに出るのにカード面では判別できず、タスク消失や見落としに近い混乱が起きる。
- 制約環境での影響: 口頭運用や短時間確認が多い現場ほど、一覧で見えない状態は実務ミスにつながる。
- 期待効果: 完了状態の意味を期限付きカードに限定し、一覧表示とフィルターの整合を保てる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 期限がないカードでは完了 UI を出さないか、期限と独立した完了概念を一覧面まで含めて明示する方針を選ぶ。

#### Issue: 20260427-07
- 状態: 新規
- 分類: バグ
- タイトル: フィルター中のドラッグで非表示カードの順序まで変わる
- 種別: 事実
- 要約: フィルター適用中に見えているカードだけをドラッグすると、見えていないカードは `remaining` として末尾に再付与される。元の相対位置が保存されない。
- 証拠: `Local-Trello.html:1524-1549` の `relocateCard()` は `orderedIds` に含まれた可視カードだけで `rebuilt` を作り、その後 `remaining` を末尾連結している。操作手順: 非表示カードを含むリストでフィルターをかける -> 可視カードを並び替える -> フィルター解除後の全体順を見る。
- 内容: 表示中のカードだけ動かしたつもりでも、非表示カードの位置まで変わる。フィルターを一時ビューとして使いたい運用と相性が悪い。
- ユーザー影響: フィルター解除後にリスト全体の並びが想定とずれ、優先順位管理を壊す。
- 制約環境での影響: リスト数が少なく検索・フィルターを多用する現場ほど、誤並び替えの復旧コストが高い。
- 期待効果: フィルター中の操作が安全になり、見えていないカードを巻き込まなくなる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: フィルター中はドラッグを無効化するか、全カード順を保持したまま可視カードだけの挿入位置計算に切り替える。

#### Issue: 20260427-08
- 状態: 新規
- 分類: アクセシビリティ
- タイトル: キーボード操作と支援技術向けの導線が不足している
- 種別: 推論
- 要約: アイコンボタンの多くが `title` のみで、クリック可能なボード名は `div` のまま、モーダルもフォーカストラップを持たない。支援技術やキーボード主体の操作で迷いやすい構造である可能性が高い。
- 証拠: `Local-Trello.html:907-944` のトップバーは `aria-label` なしのアイコンボタンが中心。`Local-Trello.html:922` のボード名は `div` で、`3214-3218` で click に反応する。`Local-Trello.html:1768-1773` のモーダル生成後に初期フォーカス設定やフォーカストラップがない。今回の監査ではスクリーンリーダー実機確認は未実施。
- 内容: 見た目では操作できても、フォーカス順・要素種別・アクセシブルネームが不足しているため、利用可能性が閲覧者の環境依存になっている。
- ユーザー影響: キーボードだけでの作業や支援技術利用時に、主要操作へ到達しづらい。
- 制約環境での影響: VDI、貸与 PC、ポインティングデバイス制約のある現場では使える人が限定される。
- 期待効果: 操作経路が安定し、利用者属性に依存しにくい単体配布ツールになる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 中
- 次の行動: トップバーのアイコンボタンへ `aria-label` を付与し、ボード名を button 化し、モーダル/ドロワーに初期フォーカスとフォーカストラップを追加する。

#### Issue: 20260427-09
- 状態: 新規
- 分類: 改善
- タイトル: 外部フォント依存のためローカル完結配布でも表示品質が環境依存になる
- 種別: 事実
- 要約: 単一 HTML 配布を前提に見えるが、実際には Google Fonts と Material Symbols CDN に依存している。オフラインや社内ネットワーク制限下でアイコン・文字幅が崩れる。
- 証拠: `Local-Trello.html:16-19`。`fonts.googleapis.com` と `fonts.gstatic.com` への外部アクセスが必須。今回の監査ではネットワーク遮断下での実機再現は未実施。
- 内容: ファイルを配るだけで使えるように見えて、実際には外部読み込みが必要な半ローカル構成になっている。
- ユーザー影響: 初回表示でアイコン欠落や文字詰まりが起きると、操作理解と信頼性が下がる。
- 制約環境での影響: オフライン現場、学内端末、企業内プロキシ環境で使えない可能性がある。
- 期待効果: 配布先ネットワークに依存しない見た目と操作性を確保できる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: 外部アイコンを inline SVG へ置き換えるか、少なくともオフライン時のフォールバックを同梱して単体配布の前提を揃える。

#### Issue: 20260427-10
- 状態: 新規
- 分類: UX
- タイトル: README 不在で保存場所と復旧手順の価値が伝わっていない
- 種別: 事実
- 要約: リポジトリ直下に `README.md` がなく、ブラウザごとの `localStorage` 保存、JSON バックアップ、外部フォント依存といった運用前提が利用前に伝わらない。
- 証拠: リポジトリ内ファイル確認結果は `Local-Trello.html` のみ。UI 上のバックアップ導線は `Local-Trello.html:2806-2823` のボード一覧ドロワー内にあり、初見では見つけにくい。
- 内容: 実装機能そのものよりも、「どこに保存されるか」「いつエクスポートすべきか」「オフラインで何が制限されるか」の説明不足が実務導入の障壁になっている。
- ユーザー影響: ブラウザ変更やストレージ削除時にデータ消失を想定できず、運用事故が起きやすい。
- 制約環境での影響: 共有 PC、一時アカウント、配布利用では導入担当が利用者へ口頭説明し続ける必要がある。
- 期待効果: ツールの使い始めと運用保守の不安を下げ、単体配布の再利用性を高められる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 次回は `README.md` を新規作成し、保存先、バックアップ推奨タイミング、インポート上書き注意、オフライン制約を 1 画面で把握できる形にする。

### 4. 追加機能アイデア
- 機能名: 保存容量メーター
  - 解決する課題: `20260427-03` `20260427-04`
  - 概要: 添付追加前と通常利用時に、現在の保存使用量と推定上限を表示する。
  - ユーザー価値: 失敗してから気付くのではなく、事前に画像整理やエクスポート判断ができる。
  - 制約環境での実現性: `localStorage` の保存サイズ概算だけでも実現可能。外部通信不要。
  - 差別化ポイント: 単一 HTML のローカルツールで「容量警告まで内蔵」は実務向け価値が高い。
  - 実装コスト: 中
  - 優先度: 高

- 機能名: インポート事前診断
  - 解決する課題: `20260427-02`
  - 概要: JSON 読込後すぐに上書きせず、ボード数・現在ボード・不足フィールドを一覧で確認してから適用する。
  - ユーザー価値: 壊れたバックアップを保存前に検知でき、復旧不能な上書きを避けられる。
  - 制約環境での実現性: ファイル読込済みテキストをローカルで検証するだけなので高い。
  - 差別化ポイント: オフライン前提ツールでも「復旧しやすさ」を提供できる。
  - 実装コスト: 中
  - 優先度: 高

- 機能名: 週次テンプレート複製
  - 解決する課題: `20260427-01` `20260427-10`
  - 概要: ボード複製時にラベル・期限・チェックリストの引継ぎ範囲を選べる簡易ウィザードを付ける。
  - ユーザー価値: 定例タスク運用を安全に複製でき、個人利用でも業務利用でも再利用しやすい。
  - 制約環境での実現性: 既存の複製処理拡張で対応可能。外部依存不要。
  - 差別化ポイント: Trello 風 UI でありながら、ローカル単体利用に最適化した複製導線を作れる。
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `20260427-01` を修正し、ボード複製後のラベル表示とフィルター動作を手動確認する。
- 2. `20260427-02` と `20260427-03` をまとめて着手し、インポート前検証と容量超過前ブロックを先に入れる。
- 3. `20260427-04` と `20260427-05` を処理し、保存責務の整理とドロワー更新順の不整合を解消する。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでの console error、モバイル幅での表示崩れ、ネットワーク遮断下でのフォント欠落時の見た目。
- 仮説のまま残った点: `20260427-03` の quota 到達タイミング、`20260427-08` の支援技術上の読まれ方は実機確認未了。
- 次回追加で見るべき箇所: 期限関連の UTC 変換、アーカイブ済み一覧からの復元後の順序、空ボード時の導線、`README` 追加時の価値訴求文言。

## 2026-04-28
### 0. エグゼクティブサマリー
- 今回の結論: 現行コードでは `日付再編集時の前日化` `インポート後の Undo/Redo 破綻` `保存失敗を成功として扱う導線` が、実務運用での信頼性を最も下げている。前回最優先に置かれていた `20260427-01` は、今回確認したコード断面では機能入口自体を再確認できなかった。
- 最優先課題: `20260428-01` 日付ポップオーバーの UTC 変換ずれ、`20260428-02` インポート後の Undo/Redo スタック不整合、`20260427-03` 保存失敗を呼び出し元が検知できない構造。
- 次回の狙い: 日付入力のローカル時刻化、全 state 差し替え時の履歴初期化、保存失敗時に成功 UI を出さない制御へ絞って 1 回で着手する。
- 今回の監査では実装は行っていない: はい。`REPORT.md` の追記のみ実施。

### 1. 今回の監査対象
- 対象ファイル: `Local-Trello.html`、`REPORT.md`。`README.md` は引き続き未作成のため確認のみ。
- 対象画面: トップバー、ボード一覧ドロワー、カード詳細モーダル、日付ポップオーバー、インポート導線、アーカイブ済み一覧。
- 主な利用フロー: 初回表示 -> ボード/カード編集 -> 日付設定 -> 並び替え -> JSON インポート/復元 -> アーカイブ復元。
- 想定ユーザー: ローカル完結で個人タスクや小規模案件を管理し、手動バックアップや JSON 復元も行う単独利用者。
- ツールの主目的: Trello 風の操作感を保ちつつ、ブラウザ内だけで継続利用できるボード管理を提供すること。
- 確認した観点: 状態整合性、保存失敗時の挙動、日付処理、復元フロー、アーカイブ復元、入力値検証、制約環境での運用耐性。

### 2. 既存課題の確認結果
- 継続課題: `20260427-02` `20260427-04` `20260427-05` `20260427-06` `20260427-07` `20260427-08` `20260427-09` `20260427-10` は、現行コードでも引き続き成立する。
- 更新した Issue: `20260427-03` は「添付容量超過」より広く、「saveState 失敗を呼び出し元が検知できず成功扱いする構造」が本質だと解像度を更新した。`20260427-01` は今回のコード断面でボード複製導線自体を再確認できず、旧証拠の行番号も現行コードと一致しなかったため、次回実装前に再現前提の再確認が必要。
- 重複のため新規登録しなかった論点: 期限なしカードの完了状態と文言ずれは `20260427-06` に統合。インポートで `labels` や `lists` 欠落時に後続 UI が壊れる論点は `20260427-02` に統合。保存失敗後でも成功 toast や再描画が進む論点は `20260427-03` に統合。

### 3. 発見した課題一覧

#### Issue: 20260428-01
- 状態: 新規
- 分類: バグ
- タイトル: 日付ポップオーバーを開き直すと UTC 正方向環境で前日の日付が再表示される
- 種別: 事実
- 要約: 日付入力欄の初期値に `toISOString().slice(0, 10)` を使っているため、UTC+9 などの環境では保存済みの開始日・期限日を開き直した時に 1 日前で表示される。
- 証拠: `Local-Trello.html:2159-2162` で `Date` から日付入力値を `toISOString()` で生成し、`2190-2193` で保存時も `toISOString()` に戻している。今回の監査で `2026-04-28T00:00:00` を保存すると `2026-04-27T15:00:00.000Z` になり、再表示値が `2026-04-27` になる Node 実行結果を確認した。`2026-04-28 08:00` でも同様に再表示日付が `2026-04-27` になった。
- 内容: 利用者は 4 月 28 日に設定したつもりでも、日付を開き直しただけで 4 月 27 日に見える。再保存すると意図せず期限を前倒しする事故が起きる。
- ユーザー影響: 期限管理の信頼性が崩れ、締切の誤認や再保存による誤更新につながる。
- 制約環境での影響: 日本時間など UTC 正方向の現場では再現率が高い。ローカル配布でタイムゾーン設定がばらつく環境ほど説明コストも増える。
- 期待効果: 日付の再編集が安全になり、カードの期限を安心して見直せる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: 日付入力欄の表示値生成をローカル日付ベースへ切り替え、`2026-04-28` と `2026-04-28 08:00` の 2 例で保存->再表示->再保存を手動確認する。

#### Issue: 20260428-02
- 状態: 新規
- 分類: バグ
- タイトル: インポート後も Undo/Redo が旧 state を参照し続ける
- 種別: 事実
- 要約: JSON インポートは `state = data` で全 state を差し替えるが、`undoStack` `redoStack` は初期化されない。インポート前のクロージャが残り、`元に戻す` と表示されても現在のボードには効かない。
- 証拠: `Local-Trello.html:1091-1111` の Undo/Redo はクロージャをそのまま実行し、`2490-2505` の `importData()` は `state = data` 後も履歴配列を触っていない。今回の監査で、旧オブジェクト参照を保持したクロージャは `state` 差し替え後の新オブジェクトを書き換えない最小 Node 例を確認した。
- 内容: 復元直後に `Z` / `Shift+Z` を押すと、利用者は「戻せた」と思うが、実際には現在のデータへ反映されない可能性が高い。
- ユーザー影響: バックアップ復元後の確認操作が信用できず、誤った状態のまま作業を続ける危険がある。
- 制約環境での影響: オフライン運用では JSON インポートが唯一の復旧手段になりやすく、ここで履歴が壊れると運用担当が復元後の安全確認をしにくい。
- 期待効果: 復元直後の操作が一貫し、Undo/Redo の期待を裏切らなくなる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `state` を丸ごと差し替える導線では `undoStack` `redoStack` を空にする。少なくとも import 後は履歴破棄を明示し、ショートカット動作を手動確認する。

#### Issue: 20260427-03
- 状態: 更新
- 分類: バグ
- タイトル: 保存失敗時に画面状態と永続化状態が乖離し、成功扱いの UI まで出る
- 種別: 事実
- 要約: `saveState()` は失敗しても toast を出すだけで呼び出し元へ失敗を返さない。そのため import を含む各導線が保存成否に関係なく再描画や成功表示を進める。
- 証拠: `Local-Trello.html:1082-1087` の `saveState()` は例外を握り潰す。`1143-1160` の `renderAll()` は毎回 `saveState()` を呼ぶため、非更新操作でも失敗経路が走る。`2499-2505` の `importData()` は `saveState()` の戻り値確認なしで `showToast('データをインポートしました')` を出す。
- 内容: 前回は容量超過を主因としていたが、現行コードで確認できた本質は「保存失敗を構造的に上位へ伝えないこと」だった。容量、ブラウザ制限、ストレージ遮断のどれでも同じ誤認が起こる。
- ユーザー影響: 保存に失敗しても成功したように見え、再読込後に変更が消えた時点で初めて気付く。
- 制約環境での影響: `file://` 実行、共有 PC、ストレージ制限環境では保存可否が不安定になりやすく、ローカル完結ツールとしての信頼性を損なう。
- 期待効果: 保存失敗を即時に伝え、成功 UI と実際の永続化結果を一致させられる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: `saveState()` を成功/失敗を返す関数へ変え、import・新規作成・編集完了の各導線で失敗時は success toast と再描画を抑止する。

#### Issue: 20260428-03
- 状態: 新規
- 分類: バグ
- タイトル: 可視リストだけを並び替えるとアーカイブ済みリストの順序が前詰めされる
- 種別: 事実
- 要約: リスト drag-drop 後の並び替えは DOM 上の可視リスト ID だけで `board.lists.sort(...)` を行うため、`ids` に存在しないアーカイブ済みリストは `indexOf === -1` となり先頭側へ寄る。
- 証拠: `Local-Trello.html:1154-1156` は描画時にアーカイブ済みリストを DOM へ出さない。`2349-2351` は可視リスト ID だけで全 `board.lists` を sort している。今回の監査で `visible-a, archived-x, visible-b` という配列へ同ロジックを適用すると `archived-x` が先頭へ移動する最小 Node 例を確認した。復元導線は `2871-2877`。
- 内容: 利用者は見えているリストだけを並び替えたつもりでも、アーカイブ済みリストの復元位置まで変わる。
- ユーザー影響: 一時退避としてアーカイブを使う運用で、戻した時の位置が崩れ、進行中リストとの文脈が失われる。
- 制約環境での影響: 小画面や単一画面運用ではアーカイブを簡易退避として使いがちで、復元位置のズレがそのまま手作業の並べ直しになる。
- 期待効果: アーカイブ復元後も元の構造文脈を保ちやすくなる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 可視リストの並び替え結果を全配列へ適用する際、アーカイブ済みリストは元の相対順を保持したまま差し込む方式へ変える。

#### Issue: 20260428-04
- 状態: 新規
- 分類: UX
- タイトル: 新規ボード作成だけ空白名を許しており一覧識別が崩れる
- 種別: 事実
- 要約: 新規ボード作成は `if (!name) return` のみで、空白だけ入力した場合でも `name.trim()` の結果が空文字のまま保存される。他の名称変更導線は `name.trim()` を必須にしており挙動が不一致。
- 証拠: `Local-Trello.html:2403-2406` は空白のみ入力を弾かない。一方で `2434-2435` と `2755-2756` は `name && name.trim()` 条件で空白を拒否している。
- 内容: ボード数が増えた時に無題行が並び、どのボードが何用かを見分けにくくなる。
- ユーザー影響: 複数ボード運用で誤選択や整理ミスが起きやすい。
- 制約環境での影響: README 不在の現状では画面内ラベルが唯一の手掛かりに近く、名前欠落の影響が相対的に大きい。
- 期待効果: ボード一覧の識別性が安定し、操作ミスを減らせる。
- 緊急度: 低
- 実装コスト: 小
- 確信度: 高
- 次の行動: 新規ボード作成でも `trim()` 後の空文字を拒否し、キャンセル時と同様に state を変更しない。

### 4. 追加機能アイデア
- 機能名: インポート前の一時スナップショット保持
  - 解決する課題: `20260428-02` `20260427-03`
  - 概要: インポート直前の state を別キーへ 1 世代だけ退避し、失敗時や直後の取り消しに使えるようにする。
  - ユーザー価値: JSON 復元を試しやすくなり、上書き系操作への不安が減る。
  - 制約環境での実現性: ローカル保存だけで完結する。外部通信不要。
  - 差別化ポイント: 単一 HTML ツールでも「復旧しやすさ」を明示できる。
  - 実装コスト: 中
  - 優先度: 高

- 機能名: タイムゾーン安全確認付き日付編集
  - 解決する課題: `20260428-01`
  - 概要: 日付ポップオーバー内に保存前プレビューを置き、ローカル日時として何が保存されるかを明示する。
  - ユーザー価値: 締切事故を防ぎ、日時入力に対する不安を減らせる。
  - 制約環境での実現性: 既存 UI 内の計算表示だけで実装できる。
  - 差別化ポイント: ローカル業務ツールで見落とされがちなタイムゾーン事故を先回りできる。
  - 実装コスト: 中
  - 優先度: 中

- 機能名: アーカイブ復元位置の明示
  - 解決する課題: `20260428-03`
  - 概要: アーカイブ一覧に元の並び位置または復元先候補を表示し、復元時の文脈を保持する。
  - ユーザー価値: 一時退避を安心して使える。
  - 制約環境での実現性: 既存のアーカイブ一覧と並び順情報の利用で実現可能。
  - 差別化ポイント: 「消すためのアーカイブ」ではなく「退避して戻すためのアーカイブ」として使いやすくなる。
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `20260428-01` を着手し、日付入力の表示値をローカル日付へ直したうえで `2026-04-28` と `2026-04-28 08:00` の再編集を確認する。
- 2. `20260428-02` と `20260427-03` をまとめて着手し、import 後の履歴初期化と save 成否の呼び出し元伝播を同時に入れる。
- 3. `20260428-03` を処理し、アーカイブ済みリストを含む並び替え後も復元位置が崩れないことを確認する。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザでの console error、in-app browser backend 不在時の UI 実機再現、支援技術での読み上げ。
- 仮説のまま残った点: `20260427-08` のスクリーンリーダー上の読まれ方と `20260427-09` のオフライン時の視覚崩れは、コード上の懸念までで実機再現は未了。
- 次回追加で見るべき箇所: `20260427-01` の再現前提確認、空 `boards` インポート時の救済導線、ショートカットモーダル記載内容と実装の整合。

## 2026-04-29
### 0. エグゼクティブサマリー
- 今回の結論: 現行コードでは `削除済みラベルのフィルター残留` と `アーカイブ済みカードを含むリストへの移動位置ずれ` が、実務での「空画面に見える」「順序が意図通りにならない」を起こしやすい。既存のアクセシビリティ課題も、今回の確認で `hover` 前提がより明確になった。
- 最優先課題: `20260429-02` 削除済みラベルのフィルター残留、`20260429-01` アーカイブ済みカード混在時の移動位置ずれ、`20260427-08` キーボード/タッチ非対応の操作前提。
- 次回の狙い: hidden state を UI 上の見え方と一致させる修正を優先し、その後にフォーカス基準の操作導線を入れて制約環境耐性を上げる。
- 今回の監査では実装は行っていない: はい。`REPORT.md` の追記のみ実施。

### 1. 今回の監査対象
- 対象ファイル: `Local-Trello.html`、`REPORT.md`。`README.md` は引き続き未作成のため確認のみ。
- 対象画面: ラベル管理ポップオーバー、フィルターポップオーバー、カード移動ポップオーバー、トップバー、ショートカットモーダル。
- 主な利用フロー: ラベルで絞り込み -> ラベル削除 -> ボード再確認、カード移動 -> アーカイブ混在リストへの挿入、ショートカット確認 -> キーボード操作、初回起動確認。
- 想定ユーザー: ローカル完結で単独運用しつつ、アーカイブやラベルを一時整理に使う個人利用者。共有 PC やオフライン配布先で短時間に再確認する利用者も含む。
- ツールの主目的: Trello 風のボード管理を単一 HTML と `localStorage` だけで継続運用できるようにすること。
- 確認した観点: hidden state を含む順序整合性、フィルター整合性、ショートカットの到達性、起動時の構文健全性。

### 2. 既存課題の確認結果
- 継続課題: `20260427-02` `20260427-03` `20260427-04` `20260427-05` `20260427-06` `20260427-07` `20260427-08` `20260427-09` `20260427-10` `20260428-01` `20260428-02` `20260428-03` `20260428-04` は現行コードでも未解決と判断した。`20260427-01` は前回同様、現行コードで対象導線を再確認できていない。
- 更新した Issue: `20260427-06` は完了トグル文言の不整合を追加証拠として更新。`20260427-08` は `hoveredCardId` がマウスイベント依存である点を追加証拠として更新した。
- 重複のため新規登録しなかった論点: hover 前提ショートカットの到達不能は `20260427-08` に統合。期限完了ボタンの文言逆転は `20260427-06` に統合。起動時確認は抽出スクリプトの `node --check` 成功まで確認できたが、実ブラウザ console は環境制約で未確認のため新規 Issue 化しなかった。

### 3. 発見した課題一覧

#### Issue: 20260429-02
- 状態: 新規
- 分類: バグ
- タイトル: 削除したラベルのフィルター条件が残りボードが空に見える
- 種別: 事実
- 要約: ラベル削除時に `board.labels` と各カードの `labelIds` は更新されるが、`filterCriteria.labelIds` は更新されない。削除済みラベルで絞り込んでいた場合、フィルター UI から外せない隠れ条件だけが残る。
- 証拠: `Local-Trello.html:2137-2145` のラベル削除処理は `filterCriteria` を触らない。`Local-Trello.html:1413-1414` は `filterCriteria.labelIds` の全件一致を要求する。`Local-Trello.html:2545-2564` のフィルター UI は現存ラベルだけを再描画するため、削除済み ID は画面上に出ない。操作手順: ラベルで絞り込み -> ボード設定「ラベルを管理」からそのラベルを削除 -> 一覧とフィルター UI を確認。最小再現として `filterCriteria.labelIds = ['deleted-label']` と `card.labelIds = []` の一致判定が常に `false` になることを Node で確認した。
- 内容: 利用者には「データが消えた」ように見えるが、実際は除去不能なフィルター条件が裏で残る。原因が画面上に出ないため自己復旧しにくい。
- ユーザー影響: ラベル整理直後にボードが空に見え、作業継続を止めやすい。初見ユーザーほどデータ消失と誤認しやすい。
- 制約環境での影響: README 不在かつローカル単体運用ではサポート導線がなく、共有 PC や短時間利用環境では原因切り分けが難しい。
- 期待効果: ラベル整理後も一覧が意図通り残り、フィルター状態の理解コストを下げられる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: ラベル削除時に `filterCriteria.labelIds` から対象 ID も除去し、削除後に有効なラベル条件が 0 件ならフィルター表示を更新して手動確認する。

#### Issue: 20260429-01
- 状態: 新規
- 分類: バグ
- タイトル: アーカイブ済みカードを含むリストへ移動すると指定位置より手前に入る
- 種別: 事実
- 要約: カード移動ポップオーバーは位置候補を可視カード数だけで作る一方、実際の挿入は `dest.cards` 全体の配列 index に対して行う。アーカイブ済みカードが途中にあると、見えている位置番号と実際の挿入位置がずれる。
- 証拠: `Local-Trello.html:2254-2258` は `target.cards.filter(c => !c.archived).length` で位置候補を生成し、`Local-Trello.html:1509-1517` は `dest.cards.splice(pos, 0, card)` で全配列に挿入する。操作手順: 対象リスト内の中間カードをアーカイブ -> 別リストのカードを「カードを移動」から同リストの末尾へ移動 -> 可視順を確認。最小再現として `[a(表示), x(アーカイブ), b(表示)]` に対して UI 上の末尾相当 `position = 2` を挿入すると、可視順が `a, 新カード, b` になることを Node で確認した。
- 内容: 利用者は「3番目に入れる」「末尾に送る」と理解して操作するが、hidden state が混ざると結果が前倒しになる。アーカイブを一時退避に使う運用と相性が悪い。
- ユーザー影響: 優先順位や工程順を守ってカードを動かしたつもりでも、見える順番が崩れる。復旧には再移動かドラッグ確認が必要になる。
- 制約環境での影響: 小画面やオフライン単体運用ではアーカイブを簡易退避として使いがちで、hidden state による誤挿入がそのまま手作業の見直しになる。
- 期待効果: 見えている位置番号と実際の挿入結果が一致し、アーカイブ併用時の並び替え信頼性が上がる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 移動時の `position` を可視カード基準の index から全配列 index へ変換するか、可視カードとアーカイブ済みカードを分けて再合成する方式へ切り替える。

#### Issue: 20260427-08
- 状態: 更新
- 分類: アクセシビリティ
- タイトル: キーボード操作と支援技術向けの導線が不足している
- 種別: 事実
- 要約: 前回の `title` 依存や `div` クリックに加えて、カードショートカット全体が `mouseover` でしか対象カードを持てない構造だった。キーボード主体・タッチ主体では、ショートカット一覧に出ている操作へ到達できない。
- 証拠: `Local-Trello.html:835-872` のトップバー主要ボタンは `title` 中心で `aria-label` が付いていない。`Local-Trello.html:850` のボード名は `div` で、`2752-2756` で click にのみ反応する。`Local-Trello.html:2624-2629` は `hoveredCardId` を `mouseover` / `mouseout` だけで管理し、`2683-2719` の `Enter` `E` `T` `C` `D` `L` `N` はこの値がないと動かない。今回の監査では支援技術実機確認は未実施だが、コード上はフォーカス起点の代替経路がない。
- 内容: ショートカット一覧は存在するが、利用前提がポインティングデバイスに寄っている。視覚上見えているカードでも、キーボードだけでは「今どのカードが対象か」を持てない。
- ユーザー影響: キーボードだけで作業したい利用者、トラックパッドやマウス制約がある利用者は主要操作を再現しにくい。
- 制約環境での影響: VDI、貸与 PC、タッチ操作中心環境では操作可能な人が限定され、単体配布ツールとしての汎用性が落ちる。
- 期待効果: フォーカスとアクセシブルネームを基準にすれば、入力機器に依存しない操作経路を用意できる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: ボード名を button 化し、トップバー主要ボタンへ `aria-label` を付与したうえで、ショートカット対象カードを `hover` だけでなく `focus` でも追跡する。

#### Issue: 20260427-06
- 状態: 更新
- 分類: UX
- タイトル: 期限なしカードでも完了操作が可能で見えない状態が生まれる
- 種別: 事実
- 要約: 前回の「期限なしでも完了状態を持てる」に加え、カードモーダルのクイックアクション文言も現在状態と操作内容が一致していない。完了前カードに `未完了にする` と表示され、押下結果を予測しづらい。
- 証拠: `Local-Trello.html:1755-1762` のクイックアクションは `card.dueComplete ? '完了済み' : '未完了にする'` を表示しており、未完了時の表示が反転している。`Local-Trello.html:1461-1464` は実際には単純な toggle。`Local-Trello.html:1759-1761` のボタンは期限有無に関係なく描画されるため、前回確認した「見えない完了状態」に文言の混乱が重なる。
- 内容: 状態表示なのか操作ラベルなのかが曖昧で、特に期限なしカードでは押す前に結果を判断しにくい。UI の言葉だけで行動を決める初見ユーザーに不利。
- ユーザー影響: 完了トグルを誤解して押し、フィルター結果やカード表示との不一致で混乱しやすい。
- 制約環境での影響: 口頭引き継ぎや短時間確認が多い現場ほど、文言が操作予測を支えるため影響が大きい。
- 期待効果: 完了 UI の意味が明確になり、期限管理と完了管理の混線を減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 高
- 次の行動: 期限なしカードでは完了操作を隠すか、完了ボタン文言を常に操作ベースへ統一し、押下後の一覧表示も含めて整合を確認する。

### 4. 追加機能アイデア
- 機能名: フィルター整合性ガード
  - 解決する課題: `20260429-02`
  - 概要: ラベル削除やラベル統合時に、現在のフィルター条件へ影響する項目を事前表示し、削除後に残る条件を自動整理する。
  - ユーザー価値: 「空になった理由が分からない」を防ぎ、ラベル整理を安心して行える。
  - 制約環境での実現性: 既存 state とフィルター条件の照合だけで実現でき、外部通信は不要。
  - 差別化ポイント: ローカル単体ツールでも整理操作の副作用まで面倒を見られる。
  - 実装コスト: 小
  - 優先度: 高

- 機能名: 可視順プレビュー付きカード移動
  - 解決する課題: `20260429-01` `20260427-07`
  - 概要: 移動先ポップオーバー内で、hidden state を除いた可視順プレビューを表示し、挿入結果を操作前に確認できるようにする。
  - ユーザー価値: 並び替え事故を減らし、アーカイブやフィルターを併用しても安心して移動できる。
  - 制約環境での実現性: 既存リスト配列のローカル計算だけで実装可能。
  - 差別化ポイント: 単一 HTML の軽量ツールでも「見えている順序を守る」実務品質を打ち出せる。
  - 実装コスト: 中
  - 優先度: 中

- 機能名: フォーカス起点ショートカット
  - 解決する課題: `20260427-08`
  - 概要: hover 中だけでなく、Tab で到達したカードにもショートカット対象を持たせる。
  - ユーザー価値: キーボードだけでカード操作を完結しやすくなる。
  - 制約環境での実現性: 既存ショートカット処理に focus 状態の追跡を足すだけで始められる。
  - 差別化ポイント: ローカル配布の業務ツールでも入力機器制約に強い点を価値として出せる。
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `20260429-02` を処理し、ラベル削除後にフィルター条件と一覧表示が崩れないことを手動確認する。
- 2. `20260429-01` を処理し、アーカイブ済みカードが途中にあるリストでも「位置」選択どおりに可視順が決まることを確認する。
- 3. `20260427-08` を最小範囲で着手し、トップバーのアクセシブルネーム付与と focus 起点ショートカットの成立を確認する。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザ console error、スクリーンリーダー実機、オフライン遮断下でのフォント欠落表示。
- 仮説のまま残った点: `20260427-09` の視覚崩れは今回もコード上の懸念までで、実機遮断確認は未了。ラベル編集ポップオーバーの入れ子 button 構造も挙動影響の実機確認までは進めていない。
- 次回追加で見るべき箇所: ラベル削除後のフィルター表示状態、移動ポップオーバーとショートカット移動の位置計算差、ショートカットモーダル文言と実装の完全一致。

## 2026-04-30
### 0. エグゼクティブサマリー
- 今回の結論: 4/27-4/29 に挙がっていた高優先課題の一部は現行コードで解消されている一方、`保存失敗後の整合性` `hidden state を含む並び替え/移動` `復旧手順の見えにくさ` がまだ実務運用の信頼性を下げている。特に `saveState()` の失敗返却を無視する導線が多く、README の説明ともずれている。
- 最優先課題: `20260427-03` 保存失敗時の成功フロー遮断未完了、`20260430-01` 保存失敗後の Undo/Redo stale 化、`20260429-02` 削除済みラベルのフィルター残留。
- 次回の狙い: まず保存失敗時の UI と履歴を壊さない形へ揃え、その次に hidden state を巻き込む移動/並び替えを止める。最後に README と起動時の復旧導線を詰める。
- 今回の監査では実装は行っていない: はい。`REPORT.md` の追記のみ実施。

### 1. 今回の監査対象
- 対象ファイル: `Local-Trello.html`、`README.md`、`REPORT.md`。
- 対象画面: ボード一覧ドロワー、カード詳細モーダル、クイック編集ポップオーバー、日付/ラベル/移動ポップオーバー、アーカイブ済み一覧、ショートカットモーダル。
- 主な利用フロー: 起動 -> 既存ボード選択/新規ボード作成 -> カード編集 -> ラベル/期限/チェックリスト編集 -> カード移動/並び替え -> アーカイブ復元 -> JSON エクスポート/インポート。
- 想定ユーザー: ブラウザ単体で個人タスクや小規模案件を回し、キーボード操作や一時アーカイブ、JSON バックアップも使う単独利用者。
- ツールの主目的: Trello 風の操作感を保ちながら、`localStorage` と JSON 入出力だけでローカル完結運用を成立させること。
- 確認した観点: 保存失敗時の状態整合性、Undo/Redo 一貫性、hidden state を含む順序処理、README の運用説明、制約環境での復旧性。

### 2. 既存課題の確認結果
- 継続課題: `20260427-03` `20260427-08` `20260427-09` `20260427-10` `20260429-01` `20260429-02` は現行コードでも未解決または部分解消に留まる。
- 更新した Issue: `20260427-03` は `saveState()` が false を返しても後続 UI が継続する導線が多数残っている点を追加証拠として更新した。`20260429-01` はキーボード移動ショートカットも同じ hidden index 問題を踏むことを追加した。`20260427-10` は README 新規作成で「不在」は解消したが、復旧手順と配布制約の不足が残るため解像度を更新した。
- 重複のため新規登録しなかった論点: クイック編集、日付編集、アーカイブ復元、新規ボード作成での `saveState()` 失敗後の挙動は、個別 Issue を乱立させず `20260427-03` に統合した。ショートカット `,` `.` の移動位置ずれは `20260429-01` に統合した。README の存在有無ではなく運用説明不足の論点は `20260427-10` に統合した。
- 現行コードで解消を確認した課題: `20260427-02` インポート構造検証、`20260427-04` `renderAll()` 毎の自動保存、`20260427-05` rename 後のドロワー重複、`20260427-06` 期限なしカードの完了 UI、`20260428-01` 日付の前日化、`20260428-02` import 後の履歴初期化、`20260428-03` 可視リスト並び替え時のアーカイブ済みリスト相対順、`20260428-04` 空白ボード名許容は現行コードで改善を確認した。
- 現行コードで再確認できなかった論点: `20260427-01` は現行コードにボード複製導線を見つけられず、現ビルドでは適用対象外の可能性が高い。

### 3. 発見した課題一覧

#### Issue: 20260427-03
- 状態: 更新
- 分類: バグ
- タイトル: 保存失敗時の成功フロー遮断が一部導線で未完了
- 種別: 事実
- 要約: `saveState()` は失敗時に `false` を返すが、その戻り値を無視する書き込み導線が多数残っている。保存済み state へロールバックした後でも、ポップオーバーを閉じる、再描画する、ボードを切り替えるなどの成功フローが続く。
- 証拠: `Local-Trello.html:1272-1286` の `saveState()` は失敗時に `return false` を返す。今回の確認で `saveState()` 戻り値未確認の呼び出しが 33 箇所残っていることを shell で集計した。代表例は `Local-Trello.html:1870-1874` のクイック編集保存、`2426-2440` の日付保存/削除、`2644-2648` の新規ボード作成、`3117-3132` のアーカイブ復元。`README.md:16-18` は「保存失敗時は成功フローを中断」と書いているが、現行コードはそこまで揃っていない。
- 内容: 4/28 時点の修正で `saveState()` の戻り値は導入されたが、呼び出し側の統一が未完了である。保存失敗を UI 上の失敗として扱う経路と、保存失敗でも成功したかのように見せる経路が混在している。
- ユーザー影響: 変更が保存されていないのに、モーダルやドロワーが閉じたり、別ボードへ移れたりする。再読込後や後続操作の時点で初めて欠落に気づくため、作業喪失の体感が強い。
- 制約環境での影響: `file://` 実行、ストレージ制限付きブラウザ、低容量端末では保存失敗が起きやすい。ローカル完結ツールとして最も避けたい「保存済みだと思ったのに残っていない」を招く。
- 期待効果: 保存成否と UI の見え方を一致させ、保存失敗時の誤認を減らせる。
- 緊急度: 高
- 実装コスト: 中
- 確信度: 高
- 次の行動: `saveState()` を呼ぶ全書き込み導線を棚卸しし、少なくとも `クイック編集` `日付編集` `新規ボード作成/切替` `アーカイブ復元` では `if (!saveState()) return;` へ統一する。

#### Issue: 20260430-01
- 状態: 新規
- 分類: バグ
- タイトル: 保存失敗後の Undo/Redo 履歴が stale object を保持する
- 種別: 事実
- 要約: 保存失敗時のロールバックは `state = restored` で新しいオブジェクトへ差し替えるが、`undoStack` `redoStack` はそのまま残る。過去のクロージャが古い参照を握ったままになり、`Z` `Shift+Z` が現在 state を変えない履歴になる。
- 証拠: `Local-Trello.html:1278-1280` は保存失敗時に `state = restored` を実行するが、`1290-1298` の履歴配列は触らない。`1301-1317` の Undo/Redo は履歴クロージャをそのまま実行する。履歴を積む導線は `1401-1407` `1496-1500` `1700-1704` `1763-1767` `1837-1843`。今回の Node 再現では、保存失敗を模したロールバック後に `undo()` `redo()` を実行しても `currentStateCards` は `c1` のままで、stale 側だけ `c1,c2` に変化することを確認した。
- 内容: 4/28 の `import` 後履歴初期化は入っているが、保存失敗ロールバック経路には同等の履歴整理がない。保存を守るためのロールバックが、その後の履歴信頼性を壊している。
- ユーザー影響: 保存失敗後に `元に戻す` が成功 toast を出しても、実データが変わらない可能性がある。利用者は「Undo まで壊れた」状態で作業継続を迫られる。
- 制約環境での影響: ストレージ制限に近い現場ほど保存失敗と履歴破綻が連鎖しやすい。ローカル単体ツールでの復旧手段がさらに減る。
- 期待効果: 保存失敗後でも履歴の挙動を予測でき、復旧操作の信頼性を保てる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `saveState()` がロールバックした時点で `undoStack` `redoStack` を破棄する。可能なら長期的にはクロージャではなく snapshot か ID ベースの履歴へ寄せる。

#### Issue: 20260429-01
- 状態: 更新
- 分類: バグ
- タイトル: アーカイブ済みカードを含むリストへの移動位置ずれがショートカット経路でも残る
- 種別: 事実
- 要約: 位置選択 UI は可視カード数だけで候補を作る一方、実挿入は `dest.cards` 全体 index に対して行っている。さらにキーボードショートカット `,` `.` も `target.cards.length` をそのまま使うため、hidden state を含むリストでは経路を変えても位置ずれが残る。
- 証拠: `Local-Trello.html:2490-2499` は位置候補を `target.cards.filter(c => !c.archived).length` 基準で生成する。実挿入は `1730-1738` の `dest.cards.splice(pos, 0, card)`。ショートカット移動は `2976-2984` で `moveCardTo(card, target.id, target.cards.length)` を呼ぶ。今回の Node 再現では `[a(表示), x(アーカイブ), b(表示)]` に UI 上の末尾相当 `position = 2` を挿入すると `a > x > n > b` になった。
- 内容: 4/29 時点ではポップオーバー移動の問題として整理していたが、現行コードではキーボード移動も同じ根本原因を共有している。対症療法ではなく index 変換ロジックの一本化が必要である。
- ユーザー影響: 見えている「末尾」や「次のリストの最後」に送ったつもりのカードが、可視順では途中に割り込む。優先順位や工程順の誤解を生む。
- 制約環境での影響: 小画面やキーボード中心運用ではショートカット移動が便利なだけに、同じ hidden state 問題を広い操作経路で踏む。
- 期待効果: 移動経路に関係なく、可視順と実際の挿入結果を一致させられる。
- 緊急度: 高
- 実装コスト: 小
- 確信度: 高
- 次の行動: `moveCardTo()` の前段で「可視 index -> 全配列 index」の変換 helper を作り、ポップオーバー移動とショートカット移動の両方で同じ helper を使う。

#### Issue: 20260430-02
- 状態: 新規
- 分類: バグ
- タイトル: リスト並び替えが非表示カードまで再ソートする
- 種別: 事実
- 要約: リストメニューの並び替えは `list.cards.sort(order)` を全カードへ適用している。一方で画面に出るのは `!archived && matchesFilter(c)` のカードだけであり、利用者が見えていないカードの順序まで動く。
- 証拠: `Local-Trello.html:1373` は可視カードを `list.cards.filter(c => !c.archived && matchesFilter(c))` で作る。`1619-1637` の `matchesFilter()` によりテキスト/ラベル/期限でさらに hidden state が増える。対して `1791-1806` の `sortList()` は `list.cards.sort(order)` を全件へ実行する。今回の Node 再現では `B(表示), A(アーカイブ), C(表示)` を名前順ソートすると `A(アーカイブ), B(表示), C(表示)` へ変化した。
- 内容: フィルター中のドラッグ並び替え問題は過去に指摘済みだが、メニュー経由の sort は別経路でまだ hidden state を巻き込む。アーカイブやフィルターを「一時的に隠す」用途で使う運用と相性が悪い。
- ユーザー影響: いま見えているカードだけ整理したつもりでも、後でフィルター解除や復元をした時に並びが崩れて見える。
- 制約環境での影響: リストを整理しながら hidden state で作業面積を減らす運用では、復帰時の手直しがそのまま手作業コストになる。
- 期待効果: 並び替え結果を利用者の見え方に一致させ、hidden state を使った運用でも順序事故を減らせる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: フィルター有効時やアーカイブ混在時は sort を止めるか、可視カードだけ sort して hidden state を元位置へ戻すマージ方式へ切り替える。

#### Issue: 20260430-03
- 状態: 新規
- 分類: UX
- タイトル: Undo/Redo の説明と実際の対応範囲がずれている
- 種別: 事実
- 要約: ショートカットモーダルは `Z` `Shift+Z` を汎用の元に戻す/やり直すとして案内しているが、実際に `pushUndo()` を積むのは一部の追加・削除・タイトル変更だけである。カード本文、ラベル、日付、チェックリストなど多くの編集は即保存され、履歴対象外のまま終わる。
- 証拠: `Local-Trello.html:1403` `1496` `1700` `1763` `1839` が `pushUndo()` の全呼び出し。ショートカットモーダルは `2858-2859` で一般的な `元に戻す` `やり直す` と説明している。履歴対象外の代表例は `1993-2002` のカードタイトル、`2103-2105` の説明保存、`2295-2299` のラベル切替、`2426-2435` の日付保存、`2198-2200` のチェックリスト項目追加。
- 内容: 利用者から見ると「どの変更なら Z で戻せるのか」が画面上で分からない。特にタイトルや期限など高頻度の編集が対象外で、ショートカット一覧の約束とずれている。
- ユーザー影響: 編集ミスの復旧期待を裏切りやすく、キーボード主体での作業効率も下がる。保存済みかつ元に戻せない変更を誤って残しやすい。
- 制約環境での影響: マウスよりショートカットに寄る利用者ほど影響を受ける。ローカル単体ツールでは「取り消せるか」が安全性の一部になる。
- 期待効果: Undo の期待値を揃えられ、ショートカット案内の信頼性が上がる。
- 緊急度: 中
- 実装コスト: 中
- 確信度: 高
- 次の行動: まず `カードタイトル` `説明` `日付` の 3 経路へ `pushUndo()` を足すか、先にショートカット説明へ「一部編集のみ対応」を明記して期待値を下げる。

#### Issue: 20260427-10
- 状態: 更新
- 分類: UX
- タイトル: README は作成されたが復旧手順と配布制約の説明がまだ不足している
- 種別: 推論
- 要約: 4/27 時点の「README 不在」は解消したが、現行 README は通常利用の入口だけに留まり、`ブラウザ/プロファイル単位で保存されること` `破損時の復旧手順` `外部 CDN 依存` を説明していない。コード上は `loadState()` が失敗時に初期ボードへフォールバックするため、利用者には空の新規状態に見える可能性がある。
- 証拠: `README.md:6-8` は起動方法と `localStorage` 保存、エクスポート/インポートのみ記載し、`26-29` も定期エクスポート注意に留まる。`Local-Trello.html:1255-1268` は `loadState()` 失敗時に `defaultState()` を返し、UI 上の復旧案内は出さない。外部依存は `Local-Trello.html:16-20` の Google Fonts / Material Symbols CDN。今回、inline script の構文自体は `syntax-ok` を確認したが、sandbox 制約で実ブラウザ console の自動採取までは完了できなかった。
- 内容: README ができたことで導入ハードルは下がったが、障害時の運用説明はまだ足りない。ローカル完結ツールとして期待される「壊れた時にどう戻すか」が、画面内にも README にも十分出ていない。
- ユーザー影響: 保存データ破損やブラウザ切替時に、利用者は「ボードが消えた」と受け取りやすい。ネットワーク制限環境ではフォント/CDN 依存も導入時の説明不足になる。
- 制約環境での影響: 共有 PC、学内端末、社内制限ネットワーク、プロファイル削除ポリシーのある環境では、導入担当の口頭サポートが前提になってしまう。
- 期待効果: トラブル時の自己復旧率が上がり、配布先環境とのミスマッチを減らせる。
- 緊急度: 中
- 実装コスト: 小
- 確信度: 中
- 次の行動: `README.md` へ「保存はブラウザ/プロファイル単位」「壊れた時は JSON バックアップから復元」「CDN 非到達時は見た目が崩れる可能性がある」を追記し、可能なら起動時にも復旧案内を 1 画面出す。

### 4. 追加機能アイデア
- 機能名: 保存状態インジケータ
  - 解決する課題: `20260427-03` `20260430-01`
  - 概要: 画面上に「保存済み / 保存失敗 / 直前保存時刻」を出し、ロールバック時は Undo 履歴破棄も同時に明示する。
  - ユーザー価値: `いまの変更が残ったか` を操作直後に判断できる。
  - 制約環境での実現性: `localStorage` の結果と時刻表示だけで実装でき、外部通信不要。
  - 差別化ポイント: 単一 HTML の軽量ツールでも、保存の信頼性を UI として可視化できる。
  - 実装コスト: 中
  - 優先度: 高

- 機能名: 可視順基準の移動/並び替えエンジン
  - 解決する課題: `20260429-01` `20260430-02` `20260427-07`
  - 概要: hidden state を含む配列処理を 1 つの helper に寄せ、移動・sort・drag がすべて同じ可視順ルールを使うようにする。
  - ユーザー価値: フィルターやアーカイブを併用しても、見えている順番どおりに操作できる。
  - 制約環境での実現性: 既存 state 配列の再構成だけで対応可能。外部依存不要。
  - 差別化ポイント: ローカル業務ツールで起きやすい hidden state 事故を構造的に減らせる。
  - 実装コスト: 中
  - 優先度: 高

- 機能名: 起動時セルフチェックと復旧ガイド
  - 解決する課題: `20260427-10` `20260427-09`
  - 概要: 起動時に保存データ読込結果とフォント/CDN 到達状況を点検し、異常時はバックアップ復元や制限事項を案内する。
  - ユーザー価値: 「何が壊れたか分からない」を減らし、初見ユーザーでも自己復旧しやすくなる。
  - 制約環境での実現性: ローカル読込結果と DOM 状態のチェックだけで始められる。
  - 差別化ポイント: 単一 HTML 配布でも、障害時の運用支援まで内包できる。
  - 実装コスト: 中
  - 優先度: 中

### 5. 次回おすすめタスク
- 1. `20260427-03` と `20260430-01` を同時に着手し、保存失敗時に `UI 継続なし` `履歴破棄あり` を最低条件として揃える。
- 2. `20260429-02` と `20260429-01` を続けて処理し、ラベル削除後の空画面化と hidden index による移動位置ずれをまとめて止める。
- 3. `20260430-03` と `20260427-10` を小さく着手し、Undo 対応範囲の明示または拡張と、README の復旧手順追記を同じ回で終える。

### 6. 監査メモ
- 今回の監査で未確認の点: 実ブラウザ console error の自動採取、スクリーンリーダー実機、ネットワーク遮断下でのフォント/CDN 欠落表示。
- 仮説のまま残った点: `20260427-10` の「初回起動で本当にデータ消失に見えるか」は code path からの推論までで、GUI 実機確認は未了。`20260427-09` の視覚崩れも同様。
- 次回追加で見るべき箇所: `20260429-02` の現行 line 再取得、`saveState()` 失敗後に stale 履歴をどこで破棄するのが最小差分か、ショートカット `Z` の対象拡張範囲。
